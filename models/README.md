Weights for the newsfeed text-tagger SLM live in Backblaze B2 under
`radon-models/slm-tagger/v1/`. See `models/slm-tagger/v1/manifest.json` for
sha256s. Never commit `.gguf` or `.safetensors` binaries.

Radon SLM tagger: internal specialist, not a SotA replacement for open research.
