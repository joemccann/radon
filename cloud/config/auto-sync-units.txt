# Units CI may publish via `radon-deploy-root sync-scheduled-units`.
# Content is taken from git objects at the GitHub main tip, not the live checkout.
# Control-plane units are rejected. The helper never starts, stops, or enables.
radon-signals-refresh.service
