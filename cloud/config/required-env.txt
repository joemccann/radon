# Production variables required before bootstrap or deployment may start units.
TWS_USERID
TWS_PASSWORD
TRADING_MODE
TURSO_DB_URL
TURSO_AUTH_TOKEN
# Isolated demo Turso. radon-demo-mirror.service carries an ExecStartPre that
# runs `migrate.py --demo`, and resolve_target() sys.exit(2)s when either is
# unset — so without them the unit fails on EVERY fire and the demo schema
# silently stops migrating. Required here so the deploy preflight fails closed
# rather than shipping a unit that cannot start. R-300.
TURSO_DEMO_DB_URL
TURSO_DEMO_AUTH_TOKEN
# Backblaze B2 (S3 API) — portfolio_snapshots cold-archive off-box store.
# Required for radon-portfolio-archive.service (fails closed without these).
RADON_ARCHIVE_S3_ENDPOINT
RADON_ARCHIVE_S3_BUCKET
RADON_ARCHIVE_S3_ACCESS_KEY_ID
RADON_ARCHIVE_S3_SECRET_ACCESS_KEY
RADON_ARCHIVE_S3_REGION
UW_TOKEN
# Equibles — the five radon-equibles-*.timer oneshots (short-crowding, filing
# forensics, 13F, ATS venue share, COT) construct EquiblesClient(), which
# raises EquiblesAuthError when this is unset. Required here so the deploy
# preflight fails closed instead of shipping units that die on every fire.
EQUIBLES_API_KEY
IB_FLEX_TOKEN
IB_FLEX_QUERY_ID
IB_FLEX_NAV_QUERY_ID
PUSHOVER_USER
PUSHOVER_TOKEN
ANTHROPIC_API_KEY
CEREBRAS_API_KEY
THEMARKETEAR_EMAIL
THEMARKETEAR_PASSWORD
RADON_MEDIA_REMOTE
MENTHORQ_USER
MENTHORQ_PASS
CLERK_SECRET_KEY
CLERK_ISSUER
CLERK_JWKS_URL
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
ALLOWED_USER_IDS
# Fail-closed interlock (REL-029/R-054): with this set to 1, a blanked or
# typo'd ALLOWED_USER_IDS rejects every principal instead of admitting any
# valid Clerk JWT. Enforced here so the deploy preflight refuses to ship
# without it.
RADON_REQUIRE_OPERATOR_ALLOWLIST
RADON_PROBE_FRESHNESS_TOKEN
# RADON_HEALTH_STATUS_TOKEN — shared bearer that buys full /status detail from
# the health daemon THROUGH the public edge. Deliberately NOT a required key:
# every production consumer (watchdogs, deploy health-gate, the Next.js admin
# proxy) reads 127.0.0.1:8330 unproxied and already gets the full body, so a
# missing token degrades the edge to the redacted aggregate rather than breaking
# anything. Listing it here would fail the deploy preflight on a value nothing
# needs. Set it in /home/radon/radon-cloud/.env (loaded by radon-health.service
# and radon-nextjs.service) when an off-box consumer needs detail; documented in
# the root .env.example.
IB_GATEWAY_MODE
IB_GATEWAY_HOST
IB_GATEWAY_PORT
RADON_MODE
DOMAIN
NODE_ENV
