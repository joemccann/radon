# Production variables required before bootstrap or deployment may start units.
TWS_USERID
TWS_PASSWORD
TRADING_MODE
TURSO_DB_URL
TURSO_AUTH_TOKEN
# Backblaze B2 (S3 API) — portfolio_snapshots cold-archive off-box store.
# Required for radon-portfolio-archive.service (fails closed without these).
RADON_ARCHIVE_S3_ENDPOINT
RADON_ARCHIVE_S3_BUCKET
RADON_ARCHIVE_S3_ACCESS_KEY_ID
RADON_ARCHIVE_S3_SECRET_ACCESS_KEY
RADON_ARCHIVE_S3_REGION
UW_TOKEN
IB_FLEX_TOKEN
IB_FLEX_QUERY_ID
IB_FLEX_NAV_QUERY_ID
PUSHOVER_USER
PUSHOVER_TOKEN
ANTHROPIC_API_KEY
CEREBRAS_API_KEY
THEMARKETEAR_EMAIL
THEMARKETEAR_PASSWORD
RADON_MEDIA_REMOTE
MENTHORQ_USER
MENTHORQ_PASS
CLERK_SECRET_KEY
CLERK_ISSUER
CLERK_JWKS_URL
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
ALLOWED_USER_IDS
RADON_PROBE_FRESHNESS_TOKEN
# RADON_HEALTH_STATUS_TOKEN — shared bearer that buys full /status detail from
# the health daemon THROUGH the public edge. Deliberately NOT a required key:
# every production consumer (watchdogs, deploy health-gate, the Next.js admin
# proxy) reads 127.0.0.1:8330 unproxied and already gets the full body, so a
# missing token degrades the edge to the redacted aggregate rather than breaking
# anything. Listing it here would fail the deploy preflight on a value nothing
# needs. Set it in /home/radon/radon-cloud/.env (loaded by radon-health.service
# and radon-nextjs.service) when an off-box consumer needs detail; documented in
# the root .env.example.
IB_GATEWAY_MODE
IB_GATEWAY_HOST
IB_GATEWAY_PORT
RADON_MODE
DOMAIN
NODE_ENV
