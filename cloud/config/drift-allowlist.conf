# Deliberate, operator-acknowledged config drift (read by scripts/drift_audit.py).
#
# Format: one entry per line -- `<drift-id> expires=YYYY-MM-DD <reason...>`.
# The drift-id is the exact id the audit reports (e.g.
# `unit-mismatch:radon-api.service`). Matched, UNEXPIRED drifts are reported
# as "allowlisted-pending" and do NOT flip the config-drift service_health
# row to error.
#
# R-058 ratchet -- an entry is a bounded acknowledgment, never a permanent
# silencer:
#   * past its expiry date the underlying drift SURFACES again (state=error
#     with the pending unit named); renew the date deliberately or ship the
#     install.
#   * an entry without a valid expires= is never honored.
#   * an entry matching no observed drift raises `stale-allowlist:<id>`;
#     remove it the moment the underlying drift is reconciled.
#
# radon-beta-* unit files are NOT listed here -- the audit hardcodes them as
# "known-untracked" (beta keep-vs-delete is an open operator decision).

# Repo unit adds tailscaled.service ordering (After/Wants). Installing it
# means restarting radon-ib-gateway -> a 2FA push; apply at the next planned
# gateway window, then drop this line.

# Repo timer modernised Description text only; OnCalendar semantics are
# identical. Converges at the next unit reinstall.

# Repo service carries [Install] WantedBy=multi-user.target (the test suite
# requires the section); the live install dropped it -- the unit is fired by
# radon-vcg-refresh.timer, so enablement semantics are unaffected.

# llm-token-index is gated on the Artificial Analysis signup (see main repo
# memory project_llm_token_index_2026_05_19) -- units committed, not installed.
not-installed:radon-llm-index.service expires=2026-12-31 gated on Artificial Analysis signup; deliberately not installed
not-installed:radon-llm-index.timer expires=2026-12-31 gated on Artificial Analysis signup; deliberately not installed
not-installed:radon-flow-refresh.service expires=2026-09-30 hourly scanner/discover/flow producer; root install-copy owed
not-installed:radon-flow-refresh.timer expires=2026-09-30 hourly scanner/discover/flow producer; root install-copy owed
unit-mismatch:radon-skew.timer expires=2026-09-30 5-minute RTH cadence; root install-copy owed


# skew2d units land in this commit; root install-copy still owed at next VPS
# unit reinstall (setup-vps.sh SERVICE_FILES already lists them).

# Security remediation changes these checked-in units. The live install-copy
# is intentionally a separate privileged deployment step; acknowledge that
# pending convergence so CI and drift telemetry remain explicit. R-058: this
# backlog includes radon-api/monitor/relay/health/drift-audit/
# nextjs-db-watchdog -- privileged units the host still runs OLD copies of.
# The 2026-09-15 expiry forces the install (or a deliberate renewal) instead
# of hiding it forever.



# 1c3f6ae9 widened TimeoutStartSec on both units (240/360 -> 3900) for the
# NDX+SPX+RUT universe, but its CI run failed on this very contract, so the
# deploy never ran and the host checkout is still at 1bed485e with the old
# values. Acknowledging the pending window rather than hot-installing content
# that has not yet shipped through a green deploy.
# radon-leap.service also carries the security-remediation delta above; ONE
# entry per drift-id (last line wins), so this line acknowledges both pending
# deltas until the same privileged unit reinstall lands them together.

# a1f3f746 added a same-evening 23:30 UTC Yahoo catch-up. CI deploy does not
# install timers; host still has the pre-catch-up hash until the next root
# install-copy.
# R-071 bounds the service budget under the 23:30 catch-up gap (9000->6900)
# so a slow 21:30 run can no longer swallow the catch-up fire; installs
# together with the timer above.

# CREDIT units land in this commit; root install-copy still owed at next VPS
# unit reinstall (setup-vps.sh SERVICE_FILES already lists them).
not-installed:radon-credit-spread.service expires=2026-12-31 new CREDIT indicator; root install-copy owed after merge
not-installed:radon-credit-spread.timer expires=2026-12-31 new CREDIT indicator; root install-copy owed after merge

# IV RANK units land in this commit; root install-copy still owed at next VPS
# unit reinstall (setup-vps.sh SERVICE_FILES already lists them).
not-installed:radon-ivrank.service expires=2026-12-31 new IV RANK indicator; root install-copy owed after merge
not-installed:radon-ivrank.timer expires=2026-12-31 new IV RANK indicator; root install-copy owed after merge

# TimeoutStartSec 450 -> 1050 so sequential curl -m 490s POSTs can finish.
# Host unit stays stale until one bootstrap publishes the sync verb, then
# the next green deploy runs sync-scheduled-units.
unit-mismatch:radon-signals-refresh.service expires=2026-09-15 curl deadline now covers FastAPI 420/480s children; pending first bootstrap plus deploy sync
