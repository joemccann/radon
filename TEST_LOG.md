
| T-350 | DONE | (this commit) | Rail 7 ("never post the run-log tail to the public dead-man") was two negative substring greps over the security wrapper's text. Four wire cases now run `security_nightly.sh audit` in a two-marker clone with a stub `claude` that prints `CANARY-7f3a` (exit 0, exit 7, `timeout` 124 with agent output in the log, TRUNCATED via `BG_CEILING_MARKER`) and assert the canary appears in NO `gh` argv / `--body` while `issue comment` + the status DO. RED: `report "$status" "$(tail -c 800 "$RUN_LOG")"` on all four `run_phase` branches → `4 failed / 2 passed` (the two greps stayed green = the finding); reverted. GREEN 6. Greps kept as sibling copy-paste tripwires. |
| T-351 | DONE | (this commit) | Rail 5 ("credential-free") was enforced only by grepping the setup script, while the wrapper's notifier child imported EVERY key of `~/radon-weekend/.env` into `os.environ`. **Fixed at the source:** `weekend_notify._load_env_file` now imports only `ENV_FILE_KEYS = PUSHOVER_USER / PUSHOVER_TOKEN` (all five wrapper callers need only those — `_creds`). RED (defect): `main([... --loop security --env-file f])` with `IB_FLEX_TOKEN` / `TURSO_AUTH_TOKEN` / `CLERK_SECRET_KEY` in `f` → `1 failed / 16 passed`; GREEN 17. Plus an executed `setup_security_nightly.sh` run (stubbed `gh`/`launchctl`/`python3.13`/`git`, tmp `WEEKEND_ROOT`) asserting no `web/.env` lands in the clone: RED under a deliberate `cp "$SRC_REPO/web/.env"` mutation that passes all three greps → `1 failed / 12 passed`; reverted. `_clone` hoisted to module level; imports `_stub_bin`/`DUMMY` from `test_weekend_runner_env_provisioning.py`. Lead re-ran the three files in the landed tree: see commit. |
