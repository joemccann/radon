
| T-346 | DONE | (this commit) | **A real product defect.** The middleware withheld the loopback bypass for app-role Gateway mutations at `server.py:873` but then fell through to `verify_clerk_jwt`, which re-grants the same loopback trust and returned `{"sub": "localhost"}` with no token — so on `RADON_HOST_ROLE=app` a Clerk-less 127.0.0.1 caller reached all three Gateway handlers (200). Every existing test forced `is_trusted_local_request` True and stubbed the predicate open. Fix: `auth.verify_clerk_bearer` (JWT-only tail, no shortcut), used by the middleware for those requests; `Depends(verify_clerk_jwt)` sites unchanged. New `TestAppRoleGateAtTheWire`: loopback trust unstubbed, real RS256 token, no bearer → 401 and `remote_gateway_action` never awaited; operator JWT → 200 and awaited once; `/health` and a non-gateway unit keep the bypass; predicate test extended to `/ib/reset-backoff`, the `radon-ib-gateway.service/` prefix, GET and a non-gateway unit. RED at HEAD `3 failed / 17 passed`; bypass-clause mutant `3 failed`; GREEN 20; auth combo 199. Lead re-ran the file plus the auth suites in the landed tree: see commit. Agent incident: a recon probe of `/ib/restart` ran in docker mode and attempted `docker compose up` on this laptop (daemon not running, nothing started; the 2FA push-lock file it created was released by its own `/ib/reset-backoff` and is gone) — rail 1 brushed, no effect, committed tests pin `GATEWAY_MODE=cloud` and stub the backoff. |
