
| T-347 | DONE | (this commit) | The app→broker mTLS client (`_remote_url_allowed`, `_remote_ssl_context`, `_remote_http`, the 409/lease mapping) had zero coverage — every test patched `remote_gateway_action` wholesale. New `scripts/api/tests/test_remote_gateway_client.py` drives it against the real `ib_gateway_remote.serve` daemon over loopback mTLS with a stub helper (certs minted once per module via the broker-side harness): status running; restart ok; helper rc 75 → `PUSH_LOCK_HELD_RC` and route 409; rc 1 → route 502; disallowed verb never leaves the host; URL allowlist positives/negatives. **Real defect fixed:** a transport failure returned `-1`, which the route maps to 400 (its "refused on this host" bucket), so a broker outage was a client error to the admin UI; now `REMOTE_UNREACHABLE_RC = 502` (the route's existing upstream-failure code; `-1` keeps meaning refused locally). RED at HEAD `2 failed / 20 passed`; mutants: `method="GET"` 5 failed, `load_cert_chain` dropped 6 failed, status as POST 1 failed (`path=f"/{verb}"` for status is an equivalent mutant, reported not forced). GREEN 22; 120 with the sibling files. Lead re-ran in the landed tree: see commit. |
