# TEST_LOG.md — testing remediation execution log

**Contract:** `TEST_AUDIT.md` §9 (frozen backlog T-001…T-054) plus any T-### rows appended by weekend delta audits. Every fix ships with its own proof: red before the fix (a new test failing against the defect, or catching a deliberate source mutation when the code is currently correct), green after, full suite green before commit. BLOCKED requires a root-cause hypothesis. New discoveries → `TEST_AUDIT.md` NEW_FINDINGS, not mid-loop chases. Append-only; maintained by the weekend loop (`.claude/skills/testing-weekend/`).

**Prior work (pre-log):** the original PART B waves landed via PRs #13/#14 (`test-hardening-wave2`, merged 2026-08-08 as `d681d247`) before this log existed; their evidence lives in the PR bodies and `TEST_AUDIT.md` NEW_FINDINGS. Open stragglers at log creation: T-050 (coverage-ratchet honesty — needs a maintainer threshold decision).

| Task | Status | Commits | Evidence |
|---|---|---|---|
| T-055 | DONE | `3aff07db` | Broker-truth index keyed on exit identity (limit price), not just contract. Red: working TARGET adopted as the STOP, `place_order.call_count == 0`, stop never reaches IB. Green: `place_order.call_count == 1`, `exit_orders["stop"]["order_id"] != 99`. Row backfilled 2026-08-16 — full proof in the commit body. |
| T-056 | DONE | `1c150528` | Backfill fingerprint counts coverage instead of set membership, so two equal same-day partials no longer collide. Red: `["skipped"]`, 1 journal row. Green: `["inserted_from_eo", "inserted_from_eo"]`, 2 rows. Row backfilled 2026-08-16 — full proof in the commit body. |
| T-057 | DONE | `843fe2da` | `/orders/modify` surfaces the indeterminate replace state instead of a flat 500; `error` stays a string on the `RadonApiError` branch. Red: bare `{error:"Order modification failed"}` 500 on `AbortError`, object `error` on 502. Green: named indeterminate state + `replacementOrderRef` in a string body. Row backfilled 2026-08-16 — full proof in the commit body. |
| T-058 | DONE | `39d33b95` | `.pi/tests/**` ported to vitest `test()` and collected by the gate (a dot-directory needs its own include entry). Collected files 626 → 628; `.pi` tests 28 passed. Mutation: neutering the browser verb allowlist reds the gate (1 failed), reverted. One assertion corrected, not weakened (a pre-fix `git show HEAD:` snapshot that pinned the security fix as ABSENT). Gate: vitest 6612 passed. Row backfilled 2026-08-16 — full proof in the commit body. |
| T-062 | DONE | `2f01d6f4` | Gateway-power settle assertion moved inside `waitFor` and the double now holds its resolution open, so invoke and settle are distinct moments. Red: deferred double + the old synchronous read → 1 failed. Green: 30 passed in file. Gate: vitest 6612 passed. Row backfilled 2026-08-16 — full proof in the commit body. |
| T-059 | DONE | (this commit) | Six hand-computed cases with literal expectations, one per risk_profile bucket, replace the self-asserting `expect(f(pos)).toBe(<expression calling f>)` shape; the two catalog sweeps now expect against the fixture-declared `pos.entry_cost` and the catalog's own leg actions instead of calling back into `positionUtils`. Red: dropping the `Math.abs` in `resolveEntryCost`/`resolveMarketValue` → `expected 4400 to be 1200` (1 failed / 257 passed). Green after revert: 258 passed in file. Gate: vitest 6618 passed, 0 failed; pytest 6828 passed, 1 skipped, 90 deselected. |
| T-060 | DONE | (this commit) | New `web/tests/route-authz-runtime.test.ts` (81 cases): imports all 53 protected route modules + 5 admin + 5 share, invokes EVERY exported HTTP method with `requireRouteAccess` denying, and asserts 401/403 plus `radonFetch` / `readOrdersSnapshotFromDb` never reached. Options are read off `mock.calls[0][1]`, and the operator scope is asserted per METHOD (mutations only) — the whole-file substring check could not tell GET from POST. Red ×2: (a) moving the guard below `radonFetch` in `orders/place` POST → "reached radonFetch after a denied authorization", (b) adding an unguarded `export async function DELETE` → "expected [401,403] to include 200" + "passed no options object". `route-local-authz-matrix.test.ts` stayed fully green under BOTH mutations. Green after revert: 81 passed. Gate: vitest 6699 passed, 0 failed. |
| T-061 | DONE | (this commit) | `_mock_fill` now sets `contract.multiplier` explicitly (a bare MagicMock answered the getattr with a Mock, so the isinstance guard sent every fill down the sec_type default and the IB branch was unreachable). New `TestExecutionCostBasis` (9 cases) on `_fill_to_entry` — the audit named it `_execution_to_entry`, which does not exist. Red: `multiplier="0"` → `total_cost == 1.0` ("premium vanished — basis collapsed to commission"), `multiplier=-100` → `-5999.0`. Source fix: `_contract_multiplier` falls back to the sec_type default for non-positive / non-finite / unparseable values. Green: 55 passed in file. Mutation proof for the IB read: reverting to the hardcoded `100 if sec_type in ("OPT","BAG")` → `assert 6001.0 == 601.0`, 1 failed. Gate: pytest 6837 passed, 1 skipped, 90 deselected. |
| T-063 | DONE | (this commit) | The sweep dates a journal row by `et_session_date(fill_time)`, so comparing it to `datetime.now(ET)` was deterministically red whenever ET local time is 00:00-02:59 (the fill is 3h back, on the previous ET day) and wider on the spring-forward Sunday. The test now binds `fill_time`, passes it as `when=`, and asserts against `et_session_date(fill_time)`. Red proof (no faketime/freezegun on this runner, so the divergence was reproduced by moving the fixture fill onto the previous ET day, which is exactly the 01:30 ET shape): old assertion → `AssertionError: assert '2026-08-15' == '2026-08-16'`; new assertion on the identical fill → 1 passed. Green with the 3h fixture restored: 15 passed in file. Gate: pytest 6837 passed, 1 skipped, 90 deselected. |
| T-064 | DONE | (this commit) | `_await_slots_held` replaces both fixed fall-through loops (the helper at :56-60 and the inline copy at :143-147) with a deadline wait that ASSERTS the general lane is actually full, and raises the budget from a fixed 2s to a 20s deadline so a cold runner spawning CPython interpreters cannot decide the outcome. Red: with the saturation budget forced to zero, the new form fails in 0.1s with "general lane never saturated: 0 of 1 slots held" on all three admission tests; the old fall-through form reached the admission assertions and failed 10s later as `assert 'capacity exhausted' in 'script timed out after 5s'` / `assert True is False`, which names neither the precondition nor the reservation. Sensitivity re-proved: `RESERVED_ORDER_SLOTS = 0` → 2 failed. Green: 6 passed in file. **Audit correction:** T-064 claims the fall-through lets the suite stay GREEN on spare capacity. It does not — the paired `refused` assertion reds whenever saturation is partial. The real damage is a slow, misattributed failure and a 2s budget that flakes on a cold runner. Fix stands; the finding's failure mode is narrower than written. Gate: pytest 6837 passed, 1 skipped, 90 deselected. |
| T-065 | DONE | (this commit) | `TestSlotAccountingUnderCancellation` parametrizes `run_script` / `run_script_raw` / `run_module` over a `_BlockingProcess` double (mirrors `test_ib_gateway_subprocess_cleanup`): start the task, wait for the child, assert the slot is held, cancel, then assert the child was killed AND the slot returned to 0. Red: `run_script_raw orphaned the child on cancellation` + `run_module orphaned the child on cancellation` (2 failed, 7 passed); `run_script` passed, pinning a previously-unpinned regression. Source fix: both runners now kill and reap the child in `except asyncio.CancelledError` before the `finally` releases the slot — `except Exception` never caught it, since `CancelledError` is a `BaseException` since 3.8. Green: 9 passed in file, 32 with `test_api_subprocess.py`. Gate: pytest 6840 passed, 1 skipped, 90 deselected; cloud 858 passed, 4 skipped. |
| T-066 | DONE | (this commit) | `route-authz-runtime.test.ts` extended with a `CONTROL_PLANE_ROUTES` group — `admin/services`, `admin/services/[unit]/[action]`, `admin/ib/restart`, `admin/ib/reset-backoff`, `admin/stack/restart` — each invoked under deny (asserting `radonFetch` was never reached) and each asserted to pass `operatorOnly: true` from `mock.calls[0][1]`. `preferences` is deliberately not in this group: its GET is authenticated but not operator-scoped, and its mutations are already covered. Red: `operatorOnly: false` in the live call of `admin/ib/restart` with the phrase `// operatorOnly: true` left in the file → "admin/ib/restart POST operatorOnly: expected false to be true"; `admin-page-gate.test.ts` and `route-local-authz-matrix.test.ts` both stayed GREEN, which is the whole finding. Green: 91 passed in file. Gate: vitest 6709 passed, 0 failed. |
| T-069 | DONE | (this commit) | Threshold extracted to the exported `CRASH_TRIGGER_CORRELATION_THRESHOLD`; `RegimePanel.tsx:733` and the second grep in `regime-market-closed.test.ts` now interpolate it instead of carrying the literal, so the label and `resolveCrashTriggerState` cannot disagree. Boundary table (59.9 / 60 / 60.1) plus `liveCorrelation: false` and `correlation: null` falling through to `cachedCorrelationMet`, plus `spxBelowMa` / `realizedVolMet` each independently suppressing `triggered`. **The boundary rows are HARD LITERALS on purpose**: the first draft derived them from the constant and the 60 → 50 mutation passed 18/18 — the same self-asserting trap the finding describes. Red with literals: `> 60` → `> 50` gives 3 failed / 16 passed ("expected 50 to be 60", plus both 59.9 and 60 now firing). Green: 19 passed in file. Gate: vitest 6715 passed, 0 failed. |
| T-067 | DONE | (this commit) | The deadline check now asserts the COMPLEMENT: `countUpstreamFetchCallSites` (a `(?<![.\w])fetch\(` scan that excludes `radonFetch`/`.fetch(`) must equal `countDeadlinedFetches`, per route, instead of pinning the timed count at 5/3. The CSP slice guards its own extent (`start >= 0`, `end > start`, `length > 200`, `toContain("<html")`) so a marker rename cannot shrink it into a vacuous pass. Red ×2 against the exact defects the finding names: (a) a 6th UNTIMED `fetch()` added to `web/app/api/ticker/info/route.ts` → "ticker/info: every upstream fetch must carry AbortSignal.timeout — 6 call sites found: expected 5 to be 6"; (b) `# ── Main` renamed to `# -- Entrypoint ` → the slice collapses and the length guard fires. 2 failed / 6 passed. **The pre-fix file was 8/8 GREEN under BOTH mutations** — that is the finding. Green after revert: 8 passed. Gate: vitest 6716 passed, 0 failed. |
| T-068 | DONE | (this commit) | Unit assertions now parse systemd semantics instead of substrings: `readUnitEnvironment` strips `#`/`;` comments, unquotes, and takes the LAST assignment per key, then asserts `WS_BIND_HOST === "127.0.0.1"` / `RADON_WS_REQUIRE_CLERK === "1"` AND feeds the parsed map through the real `resolveRelaySecurityConfig` (`bindHost`, `requireClerk`). The Host-poisoning fix moved out of source-grep: `resolveUpgradeTarget` + `UPGRADE_TARGET_BASE` are now exported from `scripts/lib/wsTrust.js` and consumed by the relay's upgrade handler, so the test calls it with `{url:"/ws?ticket=abc123", headers:{host:"evil.example"}}` and asserts `origin === "http://relay.invalid"` plus the ticket still parses. Red ×2: (a) a LATER `Environment=WS_BIND_HOST=0.0.0.0` + `RADON_WS_REQUIRE_CLERK=0` appended to `cloud/services/radon-relay.service` → "expected '0.0.0.0' to be '127.0.0.1'"; (b) the base rewritten to `` `http://${req.headers.host}` `` → "expected 'http://evil.example' to be 'http://relay.invalid'". 2 failed / 3 passed. **The pre-fix file was 4/4 GREEN with the relay bound to all interfaces, Clerk off, and the URL authority attacker-controlled** — the entire finding, reproduced. Green after revert: 5 passed. Gate: vitest 6716 passed, 0 failed. |
| T-070 | DONE | (this commit) | `ticker-depth-props-wiring` converted from 10 source regexes over 3 `.tsx` + 1 `.css` file into a rendered chain: the real memoized `WorkspaceSections` → `TickerWorkspace` with `TickerDetailContent` stubbed to echo `Object.keys(depths)` and the inside bid. 4 behavioral cases (delivery, re-render on a changed snapshot, prop-over-context precedence, context fallback) + the retained CSS check. RED: dropping `depths={depths}` at `WorkspaceSections.tsx:4166` → 3 failed / 2 passed (`expected 'symbols= insideBid=null …' to contain 'symbols=SPY'`). GREEN restored: PASS (5) FAIL (0). Rename-immunity proved: renaming `depthsProp`→`shellDepths` and `tapeProp`→`shellTape` keeps PASS (5) — the old file pinned both spellings and would have gone red on that pure refactor. Full vitest gate: 6716 pass / 0 fail. |
| T-071 | DONE | (this commit) | `regime-tab-routes` no longer accepts a `/regime/*` page that 404s. The 15 subroute cases now `import` each page module and render it with `@/components/WorkspaceShell` stubbed via `vi.hoisted`, asserting the stub was constructed with `section: "regime"`; the two redirect pages are INVOKED and assert `redirect` received `/regime/cri` and `/scanner?mode=vol-cone`; the permanently-passing `not.toMatch(/setActiveTab.*useState/)` pair is replaced by a behavioral case (clicking VCG pushes the URL but must NOT swap the view while the pathname is unchanged). RED 1: `skew2d/page.tsx` returning `null` with a dead `<WorkspaceShell section="regime" />` branch → 1 failed / 65 passed (the old regex form stayed green on exactly that shape). RED 2: retargeting the vol-cone redirect to `/scanner` → red at `:52`. GREEN restored: PASS (66) FAIL (0). Full vitest gate: 6716 pass / 0 fail. |
| T-074 | DONE | (this commit) | `test_an_in_flight_refresh_cannot_roll_back_a_newer_write` no longer lets a 50 ms sleep decide the race. An `entered` Event is set inside `_slow_query` (which `refresh_snapshot` calls strictly AFTER capturing `generation_at_start` at `app_preferences.py:363-364`), and the test does `assert entered.wait(5.0)` before writing. RED: with the old fixed `time.sleep(0.05)` and a worker that takes 200 ms to enter `refresh_snapshot` (the cold/contended-runner case), the worker captures the POST-write generation, adopts the stale `500` row → `FAILED ... test_an_in_flight_refresh_cannot_roll_back_a_newer_write` at `:614`. GREEN: the event-gated form under the SAME 200 ms delayed start → `1 passed`. Determinism: `test_app_preferences.py` 5× consecutive, 55 passed each. Full pytest gate: 6843 passed, 1 skipped, 90 deselected. |
| T-076 | DONE | (this commit) | `test_sigkill_mid_run_resumes_exactly_once` kill point is now data-driven: `KILL_WORKER` takes `STOP_AFTER` + a sentinel path, writes the sentinel after N NEW findings and parks; the test polls for the sentinel (30 s bound, `pytest.fail` if it never appears), SIGKILLs, and asserts `0 < len(read_findings(job_dir)) < 200` after each of the three kills (5/20/60). RED (the silent no-op the finding is about): the old fixed `(0.05, 0.1, 0.15)` budget with a 0.5 s cold-import worker → `FINDINGS_AFTER_KILLS=0` and `1 passed` — three kills, zero findings, the resume/dedupe path never exercised and never complained. RED (the new guard fires): an unreachable `STOP_AFTER=999` → `Failed: worker never recorded 999 findings; the SIGKILL would be a no-op and the resume path would go untested`. GREEN: `test_checkpoint.py` 14 passed. Full pytest gate: 6843 passed, 1 skipped, 90 deselected. |
| T-075 | DONE | (this commit) | `portfolio-request-cadence.spec.ts` replaced the fixed `waitForTimeout(1_200)` + one-shot `toBeLessThanOrEqual(2)` with `waitForLoadState("networkidle")`, a `settled` sample asserted `> 0` and `<= 2`, and an explicit 2.5 s WATCH loop asserting the counter never moves off `settled`. The old form could not tell "one owner" from "slow runner"; the new one names the failure ("a new /api/portfolio owner appeared after the page settled"). RED/GREEN against a real injected regression (`addInitScript` firing a late `fetch("/api/portfolio")`, `next dev --turbopack` webServer, chromium): late owner at t=3 s → OLD form `1 passed` (the false green this finding is about), NEW form `1 failed` with `Expected: 2 / Received: 3` at the watch assertion; late owner at t=1.5 s → NEW form red at the settled bound (`Expected: <= 2 / Received: 3`). No injection: `1 passed` (7.2 s), re-run green. |
| T-078 | DONE | (this commit) | `vixcor-tab.spec.ts` live half no longer asserts that a specific market episode is still open. `toHaveCount(1)` on `vixcor-episode-open` is replaced by regime-invariant shape checks: `openCount <= 1` (only the trailing episode can be unresolved — `VixCorChart.tsx:244` renders the marker per `band.episode.open`), `openCount <= shadeCount`, and every shade's `width > 0`. Evidence the removed assertion was wrong-on-correct-data rather than protective: the resolved-episode render is an explicitly SUPPORTED and gated case — `web/tests/vixcor-panel.test.tsx:581-587` ("marks no open episode once the last one has resolved") asserts `queryByTestId("vixcor-episode-open")` is null and is green (`PASS (5) FAIL (0)`), so the live spec was asserting a state a gated unit test declares valid. The pinned-fixture half at `:263` still asserts the exact count deterministically. Spec verified by `playwright --list` (7 tests collected) and `tsc --noEmit` clean; the live half needs a live `/api/vixcor` and was not executed. |
| T-077 | DONE | (this commit) | `data-testid` added to the five elements the spec reached for by class — `news-feed-item` (`DashboardNewsFeed.tsx:311`), `news-feed-tags` (`:343`), `news-feed-footer` (`:380`), `news-feed-link-pill` (`:382`), `news-feed-timestamp` (`:392`) and `star-toggle` (`StarToggle.tsx:38`) — and every locator in `mobile-newsfeed-layout.spec.ts` switched to `getByTestId` / `[data-testid=...]`, including the `beforeEach` entry point at `:84` and the five `evaluate` lookups. RED (the failure mode the finding names): renaming the CSS class `news-feed-item` → `feed-row` in the component, the PRE-fix spec blocks in `beforeEach` — `Error: expect(locator).toBeVisible() failed / Locator: locator('li.news-feed-item').first() / waiting for locator(...)` — i.e. every test in the file hangs for the full Playwright timeout on a pure CSS refactor. GREEN: with the same class rename applied, the post-fix spec is `PASS (11) FAIL (0)` in 12.4 s — a CSS rename can no longer break the suite, and a missing testid now fails fast on a named locator. Clean run after restore: 11 passed (11.8 s), `--project=mobile` at 393×852. Full vitest gate: 6716 pass / 0 fail. |
| T-073 | DONE (CI change - needs operator eye) | (this commit) | Curated subset at `.github/workflows/ci.yml:304-316` grew from 9 to 14 specs: `portfolio-same-day-equity-pnl` (the AC's minimum, and the equity sibling of the already-curated combo spec) plus `stop-order-desktop`, `modify-order-correlation-risk`, `chain-expiry-preserves-builder`, `act-ticket-scroll` — the money-path files from the 15-spec delta. Every candidate was pre-flighted under the SERVER CI ACTUALLY USES (`npm run build` then `PLAYWRIGHT_WEBSERVER_CMD="npx next start -p 3030"`), not just `next dev`, because of the known Day Move dev/prod divergence. Full curated list under `next start`: **PASS (48) FAIL (0)** in 18.4 s. RED: mutating `+$608` → `+$999` in the added equity spec reds the exact curated invocation (`Expected substring: "+$999"`), proving the added file is reached. NOT curated: `performance-twr-payload.spec.ts` — it is permanently red for an unrelated contract reason (new NEW_FINDINGS entry), so adding it would red the job. The 10 remaining delta specs are indicator-tab surfaces, outside the "P0-financial" charter of this subset. DEFERRED to the operator: promoting `e2e-financial-smoke` into `deploy.needs` (`ci.yml:327`) — the standing rule is to promote only after observed green runs, and this run just changed the subset. |
| T-072 | PARTIAL - hooks landed, components BLOCKED on a maintainer threshold decision | (this commit) | **Landed (no gate moved):** the `web/lib/use*.ts` blanket exclude at `vitest.config.ts:57` is deleted. Its rationale ("React hooks need jsdom") was provably stale — `web/tests/vixcor-hook.test.tsx:2` carries `@vitest-environment jsdom`, uses `renderHook`, and runs in the gate today. Removing a stale exclude corrects the MEASUREMENT; no threshold was touched, so this is a ratchet correction, not a weakening. Measured surface 12,371 → 14,722 lines. Honest figures with `lines 75 / functions 78 / branches 65` still ENFORCED: lines **85.23%** (12548/14722), functions **83.57%** (2493/2983), branches **74.96%** (10297/13735), statements 82.42% — `vitest run --coverage` exit 0. AC prove-step met: `coverage/web/lib/useVixcor.ts.html` now reports Statements 100% **4/4** where the file previously contributed nothing; `useNewsfeedPosts.ts` 90.27% (65/72), `useVolCone.ts` 75% (3/4). Baseline before the change, for the record: lines 87.13% (10779/12371), functions 87.30%, branches 77.01%. **BLOCKED (needs the operator, T-050 rule — report, never silently lower):** adding `web/components/**/*.tsx` to coverage `include`. Measured with BOTH halves applied, the surface roughly doubles to 25,807 lines and the honest figures are lines **78.06%** (20147/25807), functions **73.75%** (4473/6065), branches **65.74%** (18252/27760), statements 75.19%. Functions lands **4.25 points BELOW the current 78 threshold**, so landing it would red CI, and the only ways to make it green are to lower the gate (forbidden) or to write component tests first. Decision needed: (a) accept a functions-threshold rebase to ~71 as a one-time honest re-baseline, or (b) hold the components exclude until component coverage clears 78. Nothing was changed for this half. |
| T-229 | DONE (production fix) | `4267a52c` | `scripts/newsfeed/scheduler.js` exits 75 on a mid-cycle SIGTERM but `radon-newsfeed.service` had no `SuccessExitStatus=`, with `Restart=on-failure`, `RestartSec=30`, `StartLimitIntervalSec=300`, `StartLimitBurst=5`. The scraper loops every 120 s, so most deploys SIGTERM mid-cycle: the unit lands `failed` after an ordinary `systemctl stop` (which `scripts/watchdog/units.py` pages on) and 5 × 30 s < 300 s parks it at `start-limit-hit` — a silent newsfeed outage. The repo's own convention is explicit at `radon-db-retention.service:18` and `radon-db-backup.service:24`. Red: `assert None == "75"`. Manifest sha bumped. **AC correction:** the finding says there is no test file for `scheduler.js`; there is — `web/tests/newsfeed-shutdown.test.ts` covers `createShutdown`'s abort, grace and idempotence paths but never the exit CODE. The 75/0 logic was already correct, so the two new cases are green on arrival and were proved load-bearing by mutating `scheduler.js:86` to `exit(truncated ? 1 : 0)`, which reds 1 of 4. Green: cloud 426 passed, vitest 4 passed. |
| T-236 | DONE | `4267a52c` | `_unit_files()` globbed the top level only, so `cloud/services/radon-.service.d/common.conf` — the drop-in setting `RADON_DB_NO_REPLICA=1` on every `radon-*` unit, the DUR-07 belt-and-braces named in CLAUDE.md — carried no sha256 pin. Proof of the gap: appending `Environment=RADON_PWNED=1` to `common.conf` left the suite **5 passed**. After extending the walk to `*.service.d/*.conf` and BEFORE the manifest entry, 3 failed; with the entry and the mutation re-applied, 2 failed; clean tree 6 passed. Darwin cloud FAILED list byte-identical to the clean tree. **Recorded, not fixed (T-243):** nothing in any DEPLOY path reinstalls `common.conf` — `install-units`' regex matches bare `radon-*.{service,timer}`, `sync-scheduled-units` is allowlist-driven, and it is absent from `CONTROL_PLANE_SOURCES`. |
| T-220 | DONE | `e1b71acb` | A returned fast-check property is a PASSING case, so `if (todayIdx < 0 \|\| pnlIdx < 0) return;` turned all 40 desktop draws into no-ops the moment a header changed. Red: renaming `label="Today P&L"` to `"Day P&L"` at `PositionTable.tsx:709` left S2 GREEN **and 2.3× faster** (306 ms vs 715 ms) — every draw had become a no-op. The header read is hoisted into `desktopHeaderIndices()`, called once per test outside `fc.assert` with the index asserted there, and `money()` now THROWS instead of returning `null`. Same rename after the fix: `desktop table publishes no TODAY … column: expected -1 to be greater than or equal to 0`. **Recorded, not fixed (T-244):** `positionUtils.fmtUsd` renders negatives as `$-50` while `format.fmtUsd` renders `-$50`; that is why `money()`'s regex never matched them and S3's market-value comparison was `null === null` for every negative draw. |
| T-228 | DONE | `e1b71acb` | `TODAY` was evaluated at MODULE LOAD while `positionUtils.isSameDay` calls `todayInET()` at ASSERTION time, so crossing 00:00 ET mid-suite stops the same-day branch firing. Red, reproduced deterministically: freeze 23:59:59 ET at module load, advance to 00:00:01 ET in `beforeEach`, unmodified files → 3 files failed, 8 failed / 5 passed. `expected -103 not to be -103` is the tell — the fixture read the exact stale `ib_daily_pnl` the test exists to reject. Fixed with `vi.useFakeTimers({toFake:["Date"]})` + `setSystemTime` and `TODAY`/`EXPIRY` derived from that instant; only `Date` is faked so React scheduling is untouched. Seam re-proved by pinning `FROZEN_NOW` to 23:59:59.900 ET: 13/13 green across an 8.3 s run that unfrozen would have crossed midnight 100 ms in. Green: 13 passed ×3. |
| T-218 | DONE | `ea225b0c` | One `not.toContain('(t.realized_pnl ?? 0) >= 0 ? "positive" : "negative"')` plus a grep for `"sign * legEc"`, in a file that already rendered `PortfolioSections`. Red A: mutating `WorkspaceSections.tsx:3954` to the R-248-equivalent `t.realized_pnl == null \|\| t.realized_pnl >= 0 ? …` (paints an unknown P&L green) left the OLD test **1 passed**; the new one reds with `expected 'right positive' not to contain 'positive'`. Red B: inverting the leg sign polarity at `PositionTable.tsx:264` kept the substring and left the OLD test green; the new one reds with `expected 1800 to be less than 0`. Green: 7 passed. No product code changed — R-248 and R-244 are correctly fixed. |
| T-225 | DONE | `ea225b0c` | The file declared itself a mirror of `MetricCards.tsx` and asserted against its own LOCAL copy. Drift confirmed: `db5a12f5`'s em-dash sweep changed the component to `reqPnL(), account-level` while the test still carried `reqPnL() — account-level`, and the test knew nothing about the second `estimated` source branch at all. Red: mutating the component's IB-source line to `guessed from stale cache, not real-time` left the OLD file **38 passed** while the shipped copy claimed a guessed, non-real-time source. The file now renders `MetricCards`, clicks the card and reads the portalled dialog, asserting the WHOLE formula block per card — which subsumes every `formula mentions X` substring case it replaces — and adds the untested estimated-source branch plus Day Move and Unrealized P&L. The 8 `onClick`-wiring cases asserted a `vi.fn()` prop (the anti-pattern CLAUDE.md names) and the 3 field-presence cases asserted the mock against itself; both replaced by the real click-opens-modal path. Cases 38 → 12, coverage strictly up. Green: 19 passed across both files. |
| T-239 (follow-up) | DONE | `62392a9a` | Landing T-239 red-lined `TestDeadManDoesNotCryWolf::test_the_real_outcome_is_still_reported` for both wrappers: it asserted the literal `report "OK"`, which the refactor replaced with `report "$status"`. **Caught by diffing the sorted cloud FAILED list against a clean worktree at the base SHA** — 37 there, 39 with T-239 landed; counts alone would have missed it. The grep was the defect: it matched a call SPELLING, so it said nothing about which outcomes the channels can carry, and could not see the very thing T-239 fixed. It now lifts `phase_status` out of the wrapper and RUNS it. 16 passed. |
| T-233 | DONE | `010c8eb7` | TZ- and locale-independence were PROPERTIES to re-audit each delta, not guarantees. Both exposures reproduced in-process first: scaling the parity fixture to a value that HAS a separator and running under `LC_ALL=de_DE.UTF-8` gives `expected '-$1,030' to contain '1.030'` (product formats with an explicit `toLocaleString("en-US")`, tests compare against a BARE one); under `TZ=Pacific/Kiritimati`, `format-trade-date.test.ts:28` gives `expected '5/9/2026' to match /(^\|\D)8(\D\|$)/`. Config now pins `env: { NODE_ENV, TZ: America/New_York, LC_ALL, LANG }`. The contract test gains a static half and a RUNTIME half that also reds if vitest stops honouring `test.env`. Red before: `expected undefined to be America/New_York`. **Fallout checked before landing**, since this changes the environment for the whole suite: 292 of 739 files / 3178 tests in two batches, all green; contract 4/4 under a hostile shell. Note: the config is at repo ROOT, not `web/`, as the finding said. |
| T-173 | DONE (AC refuted, corrected work shipped) | `10a7c60d` | **The AC's premise is stale AND its prescribed fix is wrong.** The directory arm already reds — T-216 added an `iterdir`/`rglob` subdir check to `_partition` after the finding was written, so `mkdir cloud/tests/test_subdir` gives `subdirectories of cloud/tests/ that no cloud-tests shard names verbatim … ['test_subdir']`. Swapping `_partition`'s `glob` to `rglob` false-reds with NO fixture present (`assert ['test_handler.py'] == []`), because `_partition` is shared with the py-tests guard whose shards name `test_monitor_daemon` / `test_watchdog` as DIRECTORY tokens. Shipped the other half of the AC instead: `test_cloud_shard_union_equals_recursive_collection`, reusing `_expand_shard_paths` / `_test_modules_under` as the py-tests sibling does. Red: `1 cloud test modules are collected by NO cloud-tests shard (CI reads green without running them)`. Green: 25 passed. |
| T-177 | DONE (AC option chosen by measurement) | `10a7c60d` | Red: an unsatisfiable locator gives `Test timed out in 5000ms` and the `8000` never fires; under `vi.setConfig({testTimeout: 1000})` the same opaque error, so the arg is provably inert. **Kept the 8 s ceiling rather than dropping it**, because the measurement refutes cargo-cult: the `findByRole` wait is 115/127/233/390 ms warm, 435/600/636/1672 ms cold, 605/1063/1309/1916 ms under `--coverage`, and the 1.9 s peak is already ~2× RTL's 1000 ms default — dropping the arg trades an opaque timeout for a flaky one on a loaded shard. Fixed with the T-161 per-file `vi.setConfig` pattern; the unsatisfiable locator now reports the real `TestingLibraryElementError` with a DOM dump. `vitest.config.ts` untouched. Green: 6 passed. |
| T-182 | DONE (AC's suggested fix proved to MISS the defect) | `10a7c60d` | **The bound is not the contract; `call_count` is.** Injecting the nominal-step bug into `IBClient.wait_until` and running with an exact 0.2 s stub gives elapsed 0.831 s / `call_count` 4: `elapsed < 0.5` reds, **the AC's suggested `elapsed < 1.0` PASSES and misses it**, `call_count <= 2` reds. The fragility is also worse than the finding said and comes from the runner — bare `time.sleep(0.2)` returns in 0.337-0.345 s here (72% overshoot) and `wait_until` makes ONE sleep, not two, so the floor is ~0.34 s against a 0.5 s ceiling with the box idle. `call_count <= 2` is now first and carries the diagnostic naming the `ceil(timeout/poll)` defect; `elapsed < 1.0` stays only as a coarse hang guard, documented as not carrying the contract. Green: 18 passed. |
| T-175 | DONE | `206f2236` | The `TAG` regex modelled at most three brace levels. Red A: a deep brace on a wrapper div means the unmatched opening tag is never pushed onto the ancestor stack, so a correctly WRAPPED table is reported unwrapped — a phantom seventh violation against the 6-entry equality baseline. Red B: the same brace on a `<table>` tag removes it from the sweep and a REAL known violation disappears. Replaced with a brace-counting scan; both component edits then become exact no-ops and the three shapes are pinned as fixture-backed tests instead. **The first brace-counting draft LOST a table** — `MarkdownRenderer.tsx:27`, which the old regex only ever reached BY ACCIDENT (its prop exceeds three levels, so the outer tag failed to match and the scan fell through to the inner tags). The committed parser therefore also descends into attribute brace spans, closing a genuine pre-existing hole. **No baseline change:** old vs new diffed table-for-table by source offset across all 271 TSX files — 36 occurrences, 36 found by each, zero gained, zero lost, identical 6-entry list. Timed per T-113: 5 passed ×3 at 601/527/646 ms, slowest test 310 ms against a 5000 ms default. |
| T-235 | DONE | `92b56b14` | The one number the spec exists to compare was reached by `card.locator("text=Today").locator("..")` — unquoted, so substring and case-insensitive — then a parent hop. Verified by REAL `--project=mobile` runs at 393×852 with a scaffold copy holding the PRE-fix locator so both ran against the same mutated build. Mutation A (a div between the label text and the label+value container): PRE-fix resolved `"Today"`, `money()` null, `Expected -$103, Received null`. Mutation B (`Today` → `Day`): PRE-fix `Test timeout of 30000ms exceeded`. Mutation C (a sibling label carrying `Today` as a substring): PRE-fix `strict mode violation … resolved to 2 elements` — under `.first()` that silently reads `-$12,120` instead of `-$103`. **Reported honestly: two of the four mutations the AC prescribes do NOT break the pre-fix locator** — wrapping the whole `<MetricCell>` is harmless because `text=` resolves to the innermost span, and `"Today's P&L"` alone is harmless because the match is a substring. Green: parity 1 passed, `mobile-primitives` 13 passed, tsc clean. |
| T-234 | DONE | `1c3d4cba` | Six raw CSS-class locators plus `toHaveClass(/active/)` and a fill read through inline `style.width`. Red, applying exactly the styling refactor the finding predicts (classes renamed, fill moved to a custom property, component + `globals.css` together): share spec 1 → 0 passed, ivrank 2 → 0 passed, with `element(s) not found` and `Expected: >= 0 Received: NaN`. Green: with all three mutations STILL applied on top of the fix, 6 passed. **Two live defects fixed as part of the work:** `regime-cta-share-pattern.spec.ts` was ALREADY RED on `main` — the download link and the close button both carried `className="cta-share-close"`, so the click hit a strict-mode violation; and `letClerkLoad`'s route handler outlives the page, so runs failed on a route callback hitting a different test each time (fixed here with `page.unrouteAll`, filed as T-240 for the ~12 other specs). Real Chromium, 6/6 ×3, tsc 0, eslint 0, 195 vitest passed on the touched components. |
| T-221 | DONE | `1c3d4cba` | The rail accepted every value the component can emit. Red, both predicted mutations: hardcoding `data-state="current"` left the pre-fix spec **1 passed**, and pointing `IvRankPanel` at `SHORT_CROWDING_REFRESH` instead of `IV_RANK_REFRESH` ALSO left it **1 passed** — a rail counting down to the wrong slot sailed through. Now pinned with `page.clock.setFixedTime("2026-08-26T21:00:00Z")`, asserting the exact figures `freshness-rail.test.ts` derives for that instant: `"1h 10m"`, `data-state="behind"`, `"Awaiting 2026-08-26"`, `data-fill-pct="95.14"`. Both mutations now red it. |
| T-186 | DONE | `79f56b7f` | **Both assertions were already gone** — `98f62715`, an ancestor of `main`, deleted them — so the finding was reproduced by reconstructing them verbatim in a scratch file. `:31` cannot fail: with the real defect injected plus an unrelated `return null;` 44 lines below, the reconstructed grep is 1 passed while the current test reds. `:42` false-positives on a lone `// we deliberately do not fetch("/api/portfolio") here` comment. **The real work is what the current test did on a genuine `await fetch(` defect: it redded for the WRONG reason** — `TypeError: fetch failed … connect ECONNREFUSED 127.0.0.1:3000`. The authless case left an UNSTUBBED fetch reachable, so a regression there had a unit test doing live network I/O to localhost:3000, where a dev server is normally listening. Both cases now red on the assertion that owns the contract, with no egress. Green: 8 passed ×3. |
| T-180 | DONE | `85fda513` | The file read `WorkspaceShell.tsx` AS TEXT. Applying the exact mutation the finding names (`:329` → `usePreviousClose({})`) leaves both assertions matching verbatim while the gate renders the empty "No real-time data" panel and the operator confirms a LIVE ORDER with no bid/ask — the failure the file's own docstring claims to prevent. Old test under that mutation: 2 passed. New test: `expected [] to include MU` and `expected null to be +1.25%`. Two further surviving mutations now discriminated (`rawPricesWithFallback` reds the DAY case; `prices={{}}` at `:615` reds both). The replacement renders the REAL `WorkspaceShell` with the hooks UNMOCKED over a stubbed relay payload and asserts the painted BID/ASK and DAY row — the wire, not the prop. Green: 3 passed ×3. The first `it` is kept; the finding calls it real. |
| T-184 | DONE (drop confirmed deliberate; the fabrication it caused is fixed) | `687d5a9b` | Concluded the DROP is correct: `contract_fill_fingerprint` is `(contract, ET date, signed qty)` because that is by construction the only identity the two writers share, and no discriminator exists — `execution_time` is written ONLY by the daemon, `journal_rehydrate` writes none, and the ambiguous case is equal size at equal price. Keying on it would restore the T-124 double-count. What the suppression did NOT do was stop the replay consuming the suppressed row's inventory, so it FABRICATED P&L: a re-entry published **$3,000 against a true $1,000**, and a suppressed OPENING partial published **$2,000 against a true $1,500**, with no guard firing. Both now withhold and fall back to IB. The AC's own scenario is unchanged — $3,300 against a true $4,000, an under-count via the documented IB fallback, not a halving. Red: `a close priced against phantom inventory must keep IB's figure, not publish $3,000 against a true $1,000` and `assert {c1: 2000.0} == {}`. Green: 50 passed ×3. The SLV shape the module exists for is guarded against regression. **Surfaced T-247 [P0]** — see below. |
| T-176 | DONE ON ARRIVAL — verified, no change needed | (none) | The finding's premise describes the pre-`d7fc96c3` file. T-117's remediation landed in PR #112 (an ancestor of `main`) and added `web/e2e/tradingDayClock.ts` plus `freezeToTradingDay(page)` as the FIRST line of BOTH tests (`:210`, `:234`); because the pin is per-PAGE rather than per-assertion it already covers the row assertion the finding names. Verified empirically with real Chromium runs against a production `next build` + `next start`: at a Saturday clock the row DOM carries no `-$3,405` at all (Day P&L cells masked to `—`) and `:220` reads `Day Move---MARKET CLOSED`; at the pinned clock `-$3,405` and `$34,458` are both present. The machine clock was a real trading day during the RED run, so only the injected browser clock changed the outcome — the pin is load-bearing in both directions. No commit; fabricating a diff to satisfy the contract would have been the wrong call. **Surfaced T-248** (a permanent local false-red at `:244`) and the fact that this spec fails 100% under `next dev --turbopack` at any clock. |
| T-224 | DONE | `a7a8e143` | Three mechanisms, each reproduced at the wire with a real caddy v2.11.4 (found at `/private/tmp/tw-0827/caddy`; `RADON_CADDY_BIN` was unset and no caddy was on PATH, so without it the three `TestEdgeMechanism` tests hard-fail and the restart-gap test skips — that shape is unchanged). **Leaked listener:** `_NeverAnswers`' handler slept 120 s so its thread kept the server and its listener alive past `shutdown()` for the whole session — socket census before the fix `open_fds 6 -> 9, sockets 1 -> 4` with a LISTEN and an ESTABLISHED fd still on port 64506 after the tests; after, `6 -> 6, sockets 0 -> 0`. **Wall-clock bound decidable by load:** unmodified file, test process SIGSTOPped 46 s while caddy behaved perfectly → `AssertionError: took 54.9s / assert 54.89 < (30.0 + 20)`, while `assert 500 <= status < 600` PASSED on the line above — the edge produced its 502 at its own 30 s bound and only the test's clock moved. **TOCTOU:** `RACE OPEN: free_port() returned 64757 and an unrelated binder took it immediately`, and a concurrent process re-drew a "reserved" port after 16360 draws; after, `RACE CLOSED: … concurrent bind -> [Errno 48] Address already in use`. The replacement ordering assertions red on the real defects: `response_header_timeout 30s -> 0s` leaves all 8 regex/text assertions passing and reds only the mechanism test; removing `lb_try_duration` gives "the upstream gap surfaced as HTTP 502". Green: 44 passed ×3 at 32.0/32.2/32.0 s against a 44-passed ~33 s baseline — no runtime cost. **Recorded, not fixed (T-249):** the header-timeout text assertion accepts `0s`, Caddy's unlimited default, since `0 <= 60`. |

## Closing gates — 2026-08-17 remediation (3 consecutive rounds)

| Round | `python3.13 -m pytest` | `npx vitest run` | `pytest cloud/tests` |
|---|---|---|---|
| 1 | 6843 passed, 1 skipped, 90 deselected (139.2s) | 6716 passed / 0 failed | 10 failed, 848 passed, 4 skipped (110.3s) |
| 2 | 6843 passed, 1 skipped, 90 deselected (185.5s) | 6716 passed / 0 failed | 10 failed, 848 passed, 4 skipped (111.0s) |
| 3 | 6843 passed, 1 skipped, 90 deselected (137.2s) | 6716 passed / 0 failed | 10 failed, 848 passed, 4 skipped (110.9s) |

The 10 cloud failures are PRE-EXISTING and environmental, not this branch's:
a clean `origin/main` worktree produces the identical `10 failed, 848 passed,
4 skipped`, and the failure LIST is byte-identical across all three rounds
(`diff` of the `FAILED` lines: no output). Cause is `shutil.which("sha256sum")`
returning None on darwin. Filed under NEW_FINDINGS; green in CI on ubuntu.

## T-072 components half — 2026-08-17 (operator delegate decided option (a): honest rebase)

| Task | Status | Commit | Evidence |
|---|---|---|---|
| T-072 | DONE (components half; hooks half landed earlier in 73ac14a3) | (this commit) | `web/components/**/*.tsx` added to coverage `include` (`vitest.config.ts:54`), widening the measured surface from 14,722 to **25,807 lines**. MEASURED with components included: lines **78.06%** (20147/25807), functions **73.78%** (4475/6065), branches **65.77%** (18259/27760), statements 75.18% (22432/29835). RED: CI invocation `bunx vitest run --config vitest.config.ts --coverage` under the OLD thresholds → exit 1, `ERROR: Coverage for functions (73.78%) does not meet global threshold (78%)` (6716 tests all passing — the red is the gate, not the suite). THRESHOLD REBASE (T-050 rule — reported, not silent): **functions 78 -> 71** (= floor(73.78 − 2)); this is a ONE-TIME honest re-baseline for the roughly-doubled surface, to be ratcheted back up as component tests land. Lines (78.06 vs 75) and branches (65.77 vs 65) still clear their gates and are UNCHANGED. GREEN: same CI invocation with the rebased thresholds → exit 0, 629 files / 6716 tests passed (measured 78.04/73.73/65.77 on the green run — sub-0.1 run-to-run jitter). |

## T-079 permanently-red performance TWR e2e — 2026-08-17

| Task | Status | Commit | Evidence |
|---|---|---|---|
| T-079 | DONE | (this commit) | `web/e2e/performance-twr-payload.spec.ts` (filed in TEST_AUDIT.md NEW_FINDINGS 2026-08-17 as "permanently RED, pins a superseded payload contract") rewritten to assert the CURRENT v2 contract, both directions: (1) a valid v2 payload — `schema_version: 2`, declared `status: "ok"`, `flows_status: "ok"`, `nav_as_of` = today — renders `+10.00%` in the hero plus `Ending equity $110,000.00`; (2) the SAME payload with `schema_version` removed degrades through `resolveStatus` and renders the honest `--`. All dates are window-relative (`isoDaysAgo`), because staleness is decided at read time from `nav_as_of` vs the clock (`NAV_STALENESS_BUDGET_SESSIONS = 2`) — the old fixture's hardcoded `period_end: "2026-03-20"` would have re-rotted a v2 rewrite by June. Cum return 0.1 over `calendar_days: 90` annualizes to ~47%, inside `IMPLAUSIBLE_ANNUALIZED = 10`. RED (pre-fix, under the T-073 pre-flight method — `npm run build` then `PLAYWRIGHT_WEBSERVER_CMD="npx next start -p 3030"`): `Expected: "+10.00%" / Received: "--"`, 1 failed — the audit's exact signature. GREEN (same invocation, post-rewrite): 2 passed (3.9s); re-run 2 passed (4.0s); `tsc --noEmit` clean. NOT added to the CI curated subset (observe-green-first rule; this run rewrote the spec). CANDIDATE for a future curation pass: it is page.route-mocked, deterministic, and now green under the production server CI uses. |

## Delta audit 2026-08-22 (Saturday, audit mode)

| Gate | Run 1 | Run 2 |
|---|---|---|
| `python3.13 -m pytest` | 1 failed (T-089 sleep race; 3/3 isolated green), 7215 passed, 1 skipped, 90 deselected | 7216 passed, 1 skipped, 90 deselected |
| `npx vitest run` | 672 files / 7036 passed | 672 files / 7036 passed |
| `pytest cloud/tests` | 12 failed, 910 passed, 4 skipped (darwin) | identical list |

Darwin cloud baseline is now 12 (10 known `sha256sum` + 2 new from #67, T-088). 17 findings filed (T-080…T-096); see `TEST_AUDIT.md` `## Delta audit 2026-08-22`.

## Delta audit 2026-08-22 (second pass, audit mode, different host)

| Gate | Run 1 | Run 2 | Run 3 |
|---|---|---|---|
| `python3.13 -m pytest` | 7216 passed, 1 skipped, 90 deselected (199.6s) | **1 failed** (T-089 sleep race), 7215 passed | 7216 passed, 1 skipped, 90 deselected |
| `npx vitest run` | 672 files / 7036 passed | 672 files / 7036 passed | 672 files / 7036 passed |
| `pytest cloud/tests` | 34 failed, 888 passed, 4 skipped | identical list | identical list |

Round 1 of pytest initially read `107 failed` — the shared runner venv had no
`pytest-asyncio` (T-119). The table records the counts after installing it.
Darwin cloud baseline on THIS host is 34, not the first pass's 12: no bash >= 4
(T-118). Verified against a `git worktree` at `71de8a33` — same 34, sorted
`FAILED` lists byte-identical. 24 additional findings filed (T-097…T-120);
8 converged with the first pass and were dropped. See `TEST_AUDIT.md`
`## Delta audit 2026-08-22 (second pass)`.

## Remediation 2026-08-23 (remediate mode)

| Task | Status | Commit | Evidence |
|---|---|---|---|
| T-081 | DONE | (this commit) | `load_flows_from_turso` now selects `flow_type` and lets classified `deposit|withdrawal|acats` rows take precedence over the builder-mirrored `external` row for the same date. Red: real sqlite + migration 0035, deposit + external row on 2026-01-13 at 80007.13 → `{'2026-01-13': 160014.26}`. Green: 80007.13; `tests/test_perf_twr*.py` + `test_portfolio_performance.py` 210 passed. |
| T-097 | DONE | (this commit) | `IBClient.wait_until` bounded on a `time.monotonic()` deadline captured at entry, loop additionally capped at `ceil(timeout/poll)` steps so instant mocks terminate; docstring no longer claims the decoupling is for test convenience. Red: `test_wait_until_is_bounded_by_wall_clock_when_sleep_overruns` (mock sleep does `time.sleep(0.2)`, timeout 0.2) → 1.24s wall > 0.5s. Green: 14 passed in `test_ib_event_waits.py`; step-count assertion relaxed to `<= ceil(timeout/poll)` per AC. `test_ib_client*`, `test_ib_sync*`, `test_ib_hot_path_timing` green. |
| T-082 | DONE | (this commit) | `wait_for_streaming_data._ready` now requires a valid account `dailyPnL` via new `account_daily_pnl_is_ready()`; `pnl_is_ready` keeps OR semantics for `get_pnl`/`get_pnl_single`, which still wait on the main path. Red: `dailyPnL=nan, unrealizedPnL=1.0` → `_streaming_ready` True (assert True is False). Green: 17 passed in `test_ib_event_waits.py` incl. quoteless-ticker and nan-PnLSingle guards that pin the ticker / `pnl_requests` loops. |
| T-086 | DONE | (this commit) | New `TestRehashKeysetPagingRealSqlite`: in-memory sqlite + migration 0037, 25 rows across 2 accounts with tied `ingested_at`, `PAGE_SIZE=10`, all 25 found with no duplicates. Red under the AND-form mutation (`account_id >= ? AND exec_id > ? AND revision >= ?`): `At index 13 diff: ('U2222222', 'exec-0010') != ('U2222222', 'exec-0000')`, 10 rows missing; mutation reverted, source unchanged. Green: 5 passed. `_TieShufflingDb` kept (guards the LIMIT/OFFSET tie-reorder regression sqlite cannot reproduce). |
| T-098 | DONE | (this commit) | Tolerated-drift UPDATE in `upsert_position_execution_fact` now converges every denormalized column (`con_id, side, quantity, price, multiplier, currency, filled_at`) via a shared `_execution_fact_columns(item)` used by INSERT and UPDATE. Red: avgPrice 4.15→9.99 resync → `price` column 4.15 (`assert 4.15 == 9.99`); multiplier 100→1 → `assert 100.0 == 1`. Green: 7 passed in file; `test_position_return_capital.py` 21, `test_ib_orders_dual_write.py` 7. Gate deliberately not widened: `normalize_execution` documents avgPrice drift must not mint a conflict (raising would re-freeze orders-sync, the R-077 incident). |
| T-102 | DONE | (this commit) | `scripts/config/market_holidays.json` extended through 2030 (29 dates, NYSE rules, weekday-verified twice by independent scripts; 2028-01-01 is a Saturday and correctly not observed). Horizon guards on BOTH readers: `web/tests/market-state-holiday.test.ts` asserts the table contains `currentYear+2` and that the derived third-Monday-of-January MLK of that year is not a trading day; `scripts/tests/test_market_calendar.py` mirrors both via `load_holidays` / `_is_trading_day`. Red: `expected [ Array(5) ] to include '2028'`, `2028-01-17 (MLK) must be a holiday: expected true to be false`; pytest `market_holidays.json has no 2028 entry`, `assert True is False`. Green: vitest 8 + 5 + 122 + 5, `marketCalendar.test.js` 10, pytest calendar files 55. |
| T-105 | DONE | (this commit) | `test_pool_recovery_escalation.py`: status()-driven fake pool (`_status_driven_pool`, `_WEDGED_DATA` / `_ALL_HEALTHY` snapshots) with NO predicate stub; the wedged-data case now asserts `consecutive_failures` climbs per tick to exactly one restart, plus the mirror `test_stuck_role_reconnecting_resets_counter_without_restart`. HEAD already carried a status()-driven wedged case (landed after the audited range); this pins the counter per tick and adds the reconnect mirror. Red under the ANY-role mutation: `assert 0 == (0 + 1)` / `assert 0 == 2`, 2 failed; reverted, source diff empty. Green: 8 passed; `test_ib_gateway_pool_recovery.py` 9. |
| T-083 | DONE | (this commit) | `PositionTable` runs positions through new `withSessionIbDailyPnl()` (`web/lib/ibDailyPnlSession.ts`, same `isIbDailyPnlCurrent` gate as the card, crypto carve-out via `isCryptoPosition`) before `useSort`, so the row cell, sort extractor, `getTodayPnlDollars`, `getOptionDailyChg` and `MobilePositionList` all see the gated value; `positionUtils.ts` left pure. Red: `position-table-day-pnl-non-trading-day.test.tsx` at 2026-08-22T21:23Z rendered `+$13,952` for an equity option with `ib_daily_pnl: 13951.76`. Green: blank on Saturday, `+$13,952` on a Friday session, crypto keeps `+$1,000` on Saturday; 28 files / 454 tests importing PositionTable / positionUtils green; `tsc --noEmit` exit 0. |
| T-101 | DONE | (this commit) | `trading-kill-switch.test.tsx` +4 cases: Cancel All click → 0 POSTs, `admin-confirm-action` → exactly 1 POST to `/api/admin/trading/cancel-all` with `confirm:true`, dialog Cancel → 0 POSTs; mirrored for Halt. Red under the `onClick={() => void runAction(...)}` mutation: `expected [ { …(3) } ] to have a length of +0 but got 1` (x2) plus the dialog-dismiss cases unable to find the dialog, 4 failed; mutation reverted, source diff empty. Green: 7/7, `admin-trading-controls` 13/13. |
| T-107 | DONE | (this commit) | New `web/tests/route-refresh-provider-wiring.test.tsx`: mocks `next/navigation.usePathname` (mutable via `vi.hoisted`) and Clerk; renders `usePortfolio` under the REAL `RouteRefreshProvider` and under the app `<Providers>` tree; flips the pathname and asserts a second `GET /api/portfolio`. Red A (`const pathname = ""`): both cases `expected [ 'GET' ] to deeply equal [ 'GET', 'GET' ]`; Red B (`<RouteRefreshProvider>` removed from `Providers.tsx`): the `<Providers>` case fails. Mutations reverted, source diff empty. Green: 2/2, `use-route-change-sync` 4/4. |
| T-103 | DONE | (this commit) | `units._read_deploy_evidence(now)` applies `external_probe`'s R-057 stranded-journal rule (imported `TRANSITION_JOURNAL_STRANDED_AFTER_SECONDS`; journal mtime older than 3600s ⇒ `in_flight=False`) and the `in_flight` branch is bounded back to `DEPLOY_COLLATERAL_WINDOW_SECS` (60 min), falling through to the marker branches beyond it; the 24h horizon stays only for kill-before-green. `grok_page_responder` reads the marker via the extracted `units._file_mtime`. Red: `test_signal_kill_hours_before_inflight_journal_stays_p1` and `test_inflight_kill_older_than_deploy_window_returns_to_p1` → `assert ['P3'] == ['P1']`; three `TestInFlightJournalStaleness` cases → `TypeError: _read_deploy_evidence() got an unexpected keyword argument 'now'`. Green: `scripts/tests/test_watchdog/` 297 passed (289 + 8); the audit's mutant (`in_flight ⇒ True`) now reds 2. |
| T-084 | DONE | (this commit) | Both `test_upsert_is_idempotent_per_date` cases now drive the REAL `upsert_iei_hyg_rows` / `upsert_credit_spread_rows` twice for one date through a `_RecordingConnection` + `get_db()` monkeypatch (test_ivrank pattern): one row, correct column order, `recorded_at` from the second call, `ON CONFLICT(date) DO UPDATE` in every statement; plus a 336-row chunking test per suite. Dead `IEI_HYG_UPSERT_SQL` / `CREDIT_SPREAD_UPSERT_SQL` deleted (`docs/indicators/credit.md` reference rewritten). Red under the swapped-floats mutation: `At index 0 diff: ('2026-08-21', 79.61…, 116.41…, …) != ('2026-08-21', 116.41…, 79.61…, …)` (x2 suites, 4 failed); reverted. Green: `test_iei_hyg.py` 26, `test_credit_spread.py` 28, `test_ivrank.py` 50. |
| T-099 | DONE | (this commit) | `fetch_iei_hyg.run` and `fetch_credit_spread.run` branch on an empty combined source into `_serve_cached`, mirroring `fetch_ivrank`: cache re-served with `status: stale_source`, `source: none`, snapshot + `error` heartbeat, no row upserts; no cache raises `RuntimeError` with nothing persisted. `persist_result` takes `health_error`. Red (all three fetchers `{}` over a populated cache): `assert ('health', 'iei-hyg', 'error') in [('guard',), ('snapshot', 'iei-hyg'), ('health', 'iei-hyg', 'ok')]` (x2 suites) and `DID NOT RAISE RuntimeError` (x2), 4 failed. Green: `test_iei_hyg.py` 29, `test_credit_spread.py` 31, `test_ivrank.py` 50, `test_service_registration_completeness.py` 5, `test_docs_contract.py` 12; unchanged-day-with-live-IB still heartbeats `ok` (pinned per suite so the outage path is distinguishable). |
| T-087 | DONE | (this commit) | `buildRelayHealthDetail(now, lastTickTimestamp, freshness)` extracted into `scripts/lib/staleDataMachine.js`; both `writeRelayHealth` branches in `scripts/ib_realtime_server.js` (ok heartbeat, degraded error) spread it, identical key order. `relay-blackout-honesty.test.ts` replay loop and same-source case now use the real builder, plus three direct builder cases and a source guard pinning both relay write branches to the builder (relay is ESM with socket side effects on import, so the guard is textual, as a supplement). Red under the R-061 mutation (age from `freshness.lastTickAt`): `expected +0 to be 600`, `expected 780 to be 600`, 2 failed; reverted. Green: 11/11; `scripts/lib/*.test.js` 188; `web/tests/*relay*` + `*stale*` 30 files / 373; `node --check` ok. |
| T-108 | DONE | (this commit) | `purgeAccountTable(dst, table, opts)` routes `DELETE FROM <table>` through `retryOperation` (phase `<table>:purge`) and throws on persistent error; `runMarketMirror` pushes the table into `failures` so the run throws `required table failures: flow_analysis_snapshots` and exits 1. Red: `retries a transient purge 502 then completes` → `expected "vi.fn()" to be called 2 times, but got 1 times`; `fails the run when the purge 502 persists` → `promise resolved "{ total: +0, failures: [] }" instead of rejecting`; default-purge case red (`Number of calls: 0`) with the loop temporarily deleted. `test_demo_seed_guard.py` strengthened to pin the purge wiring (reds with the loop deleted, the old grep did not). Green: `demoMirrorReliability.test.js` 10/10, `test_demo_seed_guard.py` 8/8, `node --check` ok. |
| T-085 | DONE | (this commit) | New `web/tests/order-dedup-surfaces.test.tsx` (8 behavioral cases): OrderTab single-leg, OrderTab combo, SingleLegOrderTicket, OptionsChainTab builder, PositionTradeTicket, ListedContractOrderForm, `lib/chat.placeProposedOrder` and `ChatPanel` each rendered/driven with a fetch stub resolving `{deduplicated:true, orderId:42}` and asserted for a `warning`-toned `/NOT sent again/` notification. Red per surface under the discarded-result mutation: `expected 'success' to be 'warning'` (x5), `expected 'futures-form-success' to match /futures-form-success--dedup/`, `expected 'Order placed: …' to match /NOT sent again/`; ChatPanel was red on the UNMUTATED source. Real defect fixed: `ChatPanel.tsx` `confirmProposal` rendered a hardcoded `Order placed: …` on `ok` and discarded `result.message`, so a deduplicated chat submit read as a placement; now `result.ok ? result.message : …`. Source-count assertions in `order-dedup-visibility.test.tsx` retained. Green: 27 files / 189 tests, `tsc --noEmit` exit 0. |
| T-100 | DONE | (this commit) | `flex_embargo.active_until()` falls back to the `flex-web-service` `service_health` row (`last_error.next_attempt_at`, via new `db.hrana_http.read_service_health_http`) when the sidecar is absent and rehydrates the sidecar; `record_lockout` raises `FlexLockoutNotRecorded(until)` when neither sink landed; `_heartbeat` / `_write_sidecar` return bool and log instead of swallowing; `cash_flow_sync._record_token_lockout` returns bool and the exit-15 status carries `embargo_recorded`. Read-side DB outage fails open (logged, pinned by `test_service_health_outage_fails_open_without_raising`). Red: `DID NOT RAISE FlexTokenLocked` (row present, no sidecar); `assert False is True` (0o500 dir, uid 501); `assert None == '2026-08-28T13:58:26Z'` (read-back); `ImportError: FlexLockoutNotRecorded`. Green: `test_flex_token_embargo` 12, `test_flex_query_lockout` 3, `test_perf_twr_flex_timeouts` 7, cash_flow_sync suites 26+35+14+7, hrana 9+8, `test_no_sync_libsql_in_api` 7, monitor exit-code/cadence/retry 15+20+6, `test_performance_background_cooldown` 6, service-health 25+5. Caveat: exit code stays 15 on 1025 even when nothing recorded (the token IS locked); honesty carried by `embargo_recorded: false`. Noticed, not touched: `monitor_daemon/handlers/cash_flow_sync.py:156` uses `hrana_execute` for a SELECT so `_persisted_next_attempt_at` can never return a value. |
| T-106 | DONE | (this commit) | Python: `run_intraday` publishes `payload["intraday_count"]` (per-name flags already existed); `is_intraday` semantics unchanged. Red: `KeyError: 'intraday_count'` in a two-ticker seed where only NVDA earns a refresh (RICH fails `intraday_targets`) and in the closed-market case that kills the unconditional-True mutation. Green `test_vol_cone.py` 40. Web: `VolConeName.is_intraday`, `VolConeData.intraday_count`; the panel derives `liveCount` from `names`, the strip reads `LIVE n/total NAMES` when partial (all-live and legacy payloads keep `LIVE THIS SESSION`), and un-refreshed rows render `<span data-testid="vol-cone-row-stale">AS OF <date>`. Red: `Unable to find an element with the text: LIVE 1/2 NAMES`. Green `vol-cone-panel` 25, `vol-cone-analysis` 16, `vol-cone-api` 5; `tsc` exit 0. No live-browser screenshot (no UI server under the rails). |
| T-109 | DONE | (this commit) | `garch_convergence.main` and `leap_scanner_uw.main` refuse preset (non-explicit-ticker) scans when `should_block_universe_scan()`, writing the `SCAN_STATUS_BUDGET_BLOCKED` degraded row via `record_scan_degraded` exactly as the theta harvester does; GARCH re-emits the prior cache stamped `scan_status=uw-budget-blocked` without writing it, LEAP leaves `data/leap.json` untouched. The shell-default string grep is replaced by `test_scheduled_scan_defaults_resolve_under_the_universe_brake` (parses `${VAR:-default}` from both wrappers and `preset:` from `WorkspaceSections.tsx`, resolves via `load_preset`, asserts `< MAX_SCHEDULED_PRESET_TICKERS = 20000 // (4 scans/day x 3 req) = 1666`; `largecaps` 600 passes, `indexes` 2600 fails) plus `test_full_index_union_would_trip_the_universe_brake`. Red: `AttributeError: … has no attribute '_CACHE_PATH'` (x3), `… has no attribute 'should_block_universe_scan'` (x2); size guard red with the wrapper default flipped to `indexes` (`assert 2600 < 1666`). Green: `test_uw_budget` 12, `test_scan_degraded_telemetry` 17, `test_garch_convergence_scanner` 7, `test_leap_scanner` 36, `test_theta_harvester_scanner` 25, `test_leap_garch_refresh_defaults` 2, `test_refresh_wrapper_exit_status` 3, `test_scan_service_health` 25, `test_uw_client` 130. |
| T-104 | DONE (deploy control-plane change - needs operator eye) | (this commit) | `deploy-root-helper.sh install-units` now journals a transaction under `STATE_DIR` (installed/updated units + pre-promotion bodies); new exact-grant `revert-units` disables --now newly installed timers, stops newly installed services, removes their files, restores replaced bodies, daemon-reloads once and reset-faileds the removed units; `commit-transition` closes the journal so a later rollback cannot strip a committed release. `deploy.sh recover_pending_transition` calls `revert_release_units` after `restore_release_backup` and before `recover` (non-fatal, logged as error). `sudoers.d/radon-deploy` gains the exact `revert-units` grant. Re-running `install-units` was not viable (it sources the GitHub main tip and refuses a HEAD behind it, exit 76) and moving the install after the gate would regress the nextjs/newsfeed body refresh. Red: gate-failing deploy call log `['stop-clean', 'git reset --hard', 'recover', 'verify-restored', 'commit-transition']` → `assert 'revert-units' in [...]`, plus four helper-level reds (exit 64 unknown action / missing grant). Green: `test_install_units.py` 24; `test_deploy_corrections.py` restore-branch exact call lists now include `revert-units` (still exact); full `pytest cloud/tests` on darwin 15 failed / 997 passed, FAILED list byte-identical to the baseline. **Operator:** helper + sudoers are control-plane sources; `preflight_control_plane` fails closed on their hash mismatch, so one root run of `cloud/scripts/bootstrap-control-plane.sh` is required on the host before the first deploy of this SHA. |

### Closing gates — 2026-08-23 remediation, this host (3 consecutive rounds at `e7f2fdd3`)

| Round | `python3.13 -m pytest` | `npx vitest run` | `pytest cloud/tests` |
|---|---|---|---|
| 1 | 7413 passed, 1 skipped, 90 deselected (151.4s) | 683 files / 7163 passed (49.8s) | 15 failed, 997 passed, 4 skipped (174.1s) |
| 2 | 7413 passed, 1 skipped, 90 deselected (151.5s) | 683 files / 7163 passed (73.2s) | 15 failed, 997 passed, 4 skipped (177.6s) |
| 3 | 7413 passed, 1 skipped, 90 deselected (224.4s) | 683 files / 7163 passed (84.7s) | 15 failed, 997 passed, 4 skipped (180.6s) |

Serial, one gate at a time, full reporter output written to a file per run.
The 15 cloud reds are the darwin `sha256sum` baseline for THIS host: 12 at
the last audited SHA `4985a7f8` (run in a worktree, matches the 2026-08-22
first-pass host) plus 3 in `test_refresh_control_plane.py`, which is new in
`4985a7f8..2e904678` and also asserts `shutil.which("sha256sum")`. The sorted
`FAILED` list is byte-identical across the baseline run on `origin/main`
(`2e904678`), the mid-run check, and all three closing rounds. Delta from the
`2e904678` baseline: pytest +48 (7365 → 7413), vitest +28 tests / +3 files
(7135 → 7163), cloud +5 passed (992 → 997), 0 new reds.

Tasks this host: 20 DONE (T-081, T-082, T-083, T-084, T-085, T-086, T-087,
T-097, T-098, T-099, T-100, T-101, T-102, T-103, T-104, T-105, T-106, T-107,
T-108, T-109), 0 BLOCKED, 0 DEFERRED. Two source defects surfaced by red
tests and fixed in scope: `ChatPanel.confirmProposal` discarded the dedup
message (T-085); the rest are as the audit ACs describe. Needs an operator
eye before merge: T-104 changes `deploy-root-helper.sh` and
`sudoers.d/radon-deploy` (control-plane sources — one root run of
`cloud/scripts/bootstrap-control-plane.sh` before the first deploy of the
merged SHA). Noticed, not touched (next audit):
`scripts/monitor_daemon/handlers/cash_flow_sync.py:156` uses `hrana_execute`
for a SELECT so `_persisted_next_attempt_at` can never return a value.

## Remediation 2026-08-23 (second host, remediate mode)

This host started `remediate` while another host was already remediating the
same backlog on `testing/weekend-2026-08-23` (the branch did not exist on
`origin` at pre-flight; it appeared before the first push). Per the 2026-08-22
lesson the local work was reset onto the other run's tip rather than
force-pushed over it, the duplicate T-081 commit was dropped, and the two
implementations were compared. Remaining work here is taken from the BOTTOM of
the open P1 list so the two runs do not collide again.

Baseline on `origin/main` @ `2e904678`, clean tree: pytest **7365 passed, 1 skipped, 90 deselected**; vitest **680 files / 7135 passed**; `pytest cloud/tests` **34 failed, 973 passed, 4 skipped** (darwin, bash 3.2 — T-118 baseline).

| Task | Status | Commit | Evidence |
|---|---|---|---|
| T-081 (delta) | DONE | (this commit) | Convergent finding: this host independently reached the same defect and the same file:line as `95cec598`, so that fix stands. Probing it surfaced a residual gap the date-keyed precedence leaves open. `external_flows` PK is `(account_id, report_date, flow_type)` and `perf_twr_builder.py:341` fans a multi-statement Flex document out per `accountId` (falling back to the literal `"ALL"`), so one date genuinely can hold rows under two account ids — and precedence decided on the DATE alone drops the second account's flow instead of counting it once. RED: `ALL` classified $10,000 + `U123` mirror $7,500 on `2026-05-01` → `{'2026-05-01': 10000.0}`, expected `17500.0`; 1 failed / 10 passed. GREEN: precedence now keyed on `(account_id, report_date)`, summed across accounts afterwards; 11 passed in file. `_insert_flow` gained an `account_id` parameter (default unchanged). Gate: pytest **7385 passed**, 1 skipped, 90 deselected — plus one PRE-EXISTING red inherited from `efc1a3a5` (see below), reproduced with this host's changes stashed. |
| T-120 | DONE | (this commit) | The four-file `ROUTES` literal in `web/tests/uw-counted-fetch.test.ts` is replaced by `collectUwFiles(web/app)` — a recursive walk collecting every `.ts`/`.tsx` whose COMMENT-STRIPPED source mentions `unusualwhales` or `UW_TOKEN` — feeding an exported `uwCountingViolations(relative, raw)` that checks three things instead of comparing two regex counts: the file imports `@/lib/uwCountedFetch`; no bare `fetch(` names the UW host in its arguments; and no bare `fetch(` uses an identifier declared from a UW URL literal. `countedUwFetch(` matches neither bare-fetch scan (lookbehind rejects a preceding word character). Five cases drive the checker directly against synthetic sources, so the guard is itself guarded: bare route, wrapped route, a `countedUwFetch` mentioned only in a COMMENT, a UW base parked in a module constant and bare-fetched, and the mixed real-world shapes (`new URL(...)` → `countedUwFetch(url.toString())`, plus the Exa/Yahoo bare fetches `info/route.ts` makes by design). **RED — the finding's exact mutation:** added `web/app/api/ticker/flow/route.ts` doing a bare `fetch("https://api.unusualwhales.com/api/stock/${t}/flow-alerts")`. OLD guard: **7 passed**, fully green. NEW guard: **1 failed**, naming `reaches UW but never imports @/lib/uwCountedFetch` and `:5: bare fetch() against api.unusualwhales.com`. Fixture removed; 14 passed. **Bug found while writing it:** the first draft's `stripComments` ate `//` in `https://`, which silently emptied the whole sweep — the URL-scheme guard (`/(^|[^:])\/\/[^\n]*/`) is now explicit and commented. Known limitation stated in the file, not hidden. Gate: vitest **683 files / 7170 passed**; `tsc --noEmit` clean. |
| T-114 | DONE | (this commit) | The file's one case read `result.current` after `renderHook`, which RTL wraps in `act()` — the mount effect at `useMarketHours.ts:47-49` has already corrected the state, so the lazy initializer (the entire fix) was unobserved. Replaced with `renderToStaticMarkup(<MarketStateProbe />)`, which never runs effects, so what it emits IS the first paint: OPEN on a Monday 11:00 ET clock and CLOSED on a Saturday. `marketStateAt` is now exported and pinned directly across all eight session boundaries plus both weekend days (10 cases, explicit UTC instants, no clock read). **RED — the finding's exact mutation:** `useState<MarketState>(marketStateAt)` → `useState<MarketState>(MarketState.CLOSED)`, restoring the CLOSED placeholder flash. OLD test: **1 passed**, green. NEW test: **1 failed** on `expect(html).toContain(">open<")`. Restored, 12 passed. Gate: vitest **7181 passed**; `tsc --noEmit` clean. |
| T-113 | DONE (+ new finding T-121) | (this commit) | `table-scroll-wrapper-contract.test.ts` ran the wrong way round: it collected a className only when a token ALREADY matched `/table-(scroll\|wrap)/` and SKIPPED everything else, so a named wrapper had to be styled while an unnamed one did not have to exist. Inverted — every `<table>` under `web/components` + `web/app` (36 of them) must have an ancestor establishing horizontal overflow: a class with an `overflow-x` rule in the global stylesheets OR in the component's own CSS module (`className={styles.tableWrap}` is now resolved, which the old form could not read at all), or an inline `overflowX`. A table rendered by a helper component is resolved through that component's call sites in the same file. Explicit opt-out is `data-overflow-exempt="<reason>"`, used once: the `OptionsExposurePanel` chain table is `table-layout: fixed` with per-column widths and ellipsis truncation and its `.tableWrap` is `overflow: visible` by design. **RED — the finding's exact mutation:** renaming both `VolConePanel.tsx` wrappers `table-wrap` → the undefined `volcone-shell`. OLD contract: **1 passed**, green. NEW contract: **1 failed**. Restored, 2 passed ×3. **Six real defects fell out and are filed as T-121**, pinned as `KNOWN_UNWRAPPED_T121` under an EQUALITY assertion so a seventh reds immediately and fixing one reds until its entry is removed; they are NOT silently skipped and NOT fixed here (six UI changes need 390px browser verification per Mandatory Rule 3). **Fragility caught in my own first draft:** it built a fresh RegExp against the whole ~1 MB `globals.css` per class token per ancestor per tag and ran 4.5-6 s against vitest's 5 s default — 8/8 runs red on a timeout, not on the assertion. Overflow-carrying class names are now precomputed in one pass per stylesheet; tests take 0.2-0.4 s. Gate: vitest **7182 passed**; `tsc --noEmit` clean. Round 1 of that gate read `13 failed / 7169 passed`, 11 of them bare `Test timed out in 5000ms` across nine unrelated files, on a runner at load average 66 (`corespotlightd` at 367% CPU); the nine files are **44 passed** in isolation and the immediately following full run is **7182 passed** in 107 s versus that round's 336 s. Load flake, named rather than swallowed. |
| T-112 | DONE | (this commit) | `TestFetchClosesCascade` (7 cases) and `TestDxyNeverGoesToUnusualWhales` (2) ported to `scripts/tests/test_iei_hyg.py` from the credit-spread twin: IB covering everything never calls UW or Yahoo; UW fills an empty IB and Yahoo is not reached; Yahoo is last and both rungs above it were tried first; each rung is asked ONLY for the tickers still missing (`ib+uw+yahoo`, `uw_calls == [[HYG, DXY]]`, `yahoo_calls == [[DXY]]`); an empty series does not claim its ticker; no rung answering yields `NO_SOURCE`; an explicit ticker list overrides `TICKERS`; and `fetch_uw_closes([DXY])` returns before constructing a `UWClient` at all (the fixture raises if it is). No source change — the ladder is correct today, the tests hold it there. **RED — the finding's exact mutation:** `fetch_iei_hyg.py:271-273` reordered to `yahoo → uw → ib`. New tests: **4 of the cascade cases fail by name**, each saying which rung ran and with which tickers. **Correction to the finding as written:** it claims the mutation leaves the suite green; that was true when it was filed but is no longer, because this weekend's T-099 work added `TestAllSourcesDown::test_unchanged_day_with_a_live_source_is_still_ok`, which also reds (1 failed / 28 passed on the pre-existing file). It reds on a downstream `assert "status" not in payload` and names neither the ladder nor the order, so the gap the finding describes is real; its "zero coverage" figure is now 60%, not the 50% recorded. Coverage of `fetch_iei_hyg.py` 60% → 61% (the ladder body is six lines). Gate: pytest **7422 passed**, 1 skipped, 90 deselected (244.0s). |
| T-111 | DONE | (this commit) | `TestSuppressionCeilingsAreSmallEnoughToMatter` (4 cases) added. The file's existing 13 cases each derive their expectation from the constant under test (`ceiling = grouping.WARMUP_SUPPRESS_MAX_CONSECUTIVE`, `... + 600`, `... + 3600`), so they hold for ANY value it could take — they test the mechanism and nothing pins the magnitude. The new cases pin the POLICY with two-sided literals: `WARMUP_SUPPRESS_MAX_CONSECUTIVE` in `[1, 6]` (at the 5-minute cycle, 6 is 30 minutes of muted IB pages, and R-056 was a ~120-200s restart loop), `TRANSITION_JOURNAL_STRANDED_AFTER_SECONDS` in `[300, 2*3600]` (a deploy takes minutes; one that has not finished in 2h is stuck and blocks later deploys, so it cannot self-clear), `KILL_BEFORE_GREEN_FROZEN_CAP_SECS` in `[24h, 36h]` (a daily oneshot must be allowed to stay failed until its next fire, and nothing legitimate waits past ~36h). Lower bounds included so the ceilings cannot be satisfied by setting a constant to zero. Same convention as `tests/test_twr_math.py:1135` and `scripts/tests/test_cash_flow_sync_cli.py:347`. **Import split closed:** the file imported `external_probe`/`units` via `scripts.watchdog` and `grouping` via `watchdog` — VERIFIED distinct module objects (`scripts.watchdog.grouping is watchdog.grouping` → `False`) — while the fixture at `:77-78` patches `watchdog.grouping.*` BY STRING, so the two halves were patching two objects. Settled on `watchdog.*` (what the string patches and the rest of `test_watchdog/` use); a fourth case asserts the constants agree across both paths so a future split reds here instead of silently no-op'ing a monkeypatch. **RED — the finding's exact widening,** injected via an out-of-tree pytest plugin setting the three to 500 / 10**9 / 10**9 (41h of muted IB pages and two ~31-year mutes): PRE-EXISTING file **13 passed**, fully green — R-056/R-057/R-064 restored in practice by the suite that forbids them. NEW file **3 failed** by name, each message carrying the converted magnitude. Restored: 17 passed in file, 301 in `test_watchdog/`. Gate: pytest **7426 passed**, 1 skipped, 90 deselected (202.2s). |

**Inherited red on the shared branch (not from this host):** `scripts/tests/test_docs_contract.py::TestOwnership::test_changed_mapped_paths_update_an_owner_doc` — `efc1a3a5` (the other run's T-103) edits `scripts/grok_page_responder.py` without touching `docs/grok-page-responder.md` or `docs/incident-runbook.md` and without a `docs-skip:` trailer. Verified pre-existing by stashing this host's working tree and re-running the test at the branch tip.

### Closing gates — three consecutive full rounds (second host)

| Gate | Round 1 | Round 2 | Round 3 |
|---|---|---|---|
| `python3.13 -m pytest` | 7426 passed, 1 skipped, 90 deselected (161.8s) | 7426 passed, 1 skipped, 90 deselected (158.4s) | 7426 passed, 1 skipped, 90 deselected (171.9s) |
| `npx vitest run` | 683 files / 7182 passed | 683 files / 7182 passed | 683 files / 7182 passed |
| `pytest cloud/tests` | 34 failed, 978 passed, 4 skipped (182.6s) | identical list (192.1s) | identical list (170.4s) |

The 34 cloud reds are the darwin baseline (T-118: no `bash >= 4`, plus the
`sha256sum` class of T-088). The sorted `FAILED` list is **byte-identical**
across all three rounds AND against the run-start baseline taken on
`origin/main` @ `2e904678` before any change on this host — `diff` produces no
output in every comparison. Delta from that baseline: pytest +61
(7365 → 7426), vitest +47 tests / +3 files (7135 → 7182), cloud +5 passed
(973 → 978), **0 new reds**.

Tasks this host: 6 DONE (T-081 delta, T-111, T-112, T-113, T-114, T-120),
0 BLOCKED, 0 DEFERRED. One new finding filed: **T-121** (six tables with no
horizontal-overflow container — surfaced by inverting the T-113 contract; not
fixed here because six UI changes need 390px browser verification per
Mandatory Rule 3). Nothing needs an operator decision: no threshold moved, no
CI workflow touched, no skip or `.only` added.
## Delta audit 2026-08-25 (Tuesday, audit mode)

| Gate | Round 1 |
|---|---|
| `python3.13 -m pytest` (recursive) | 7816 passed, 1 skipped, 90 deselected (274.4s) |
| `npx vitest run` | 701 files / 7328 passed |
| `pytest cloud/tests` | 34 failed, 1025 passed, 4 skipped (darwin, bash 3.2; list byte-identical to `4985a7f8` in a worktree) |

Added-file determinism 3×: 44 python `483 passed` ×3, 29 vitest `238 passed` ×3, 7 cloud `92 passed` ×3.
CI at the same SHA is green but its sharded pytest (`424e66da`) collects 407 of 463 files — `test_monitor_daemon/` and `test_watchdog/` (752 tests) gate nothing (T-122, P0); the ratchet also quietly moved from statement+branch to statement-only (T-123). 34 findings filed (T-122…T-155); see `TEST_AUDIT.md` `## Delta audit 2026-08-25`.

## Remediation 2026-08-25 (Tuesday, remediate mode)

Runner venv lacked `pytest-xdist` (CI-only install, same class as T-119); installed `pytest-xdist==3.8.0` / `pytest-cov==7.1.0` from `requirements-dev.txt` before any shard verification. Environment fixed, repo untouched.

| Task | Status | Commits | Evidence |
|---|---|---|---|
| T-122 | DONE | see commit | `test_pytest_shard_union_equals_recursive_collection` expands every `matrix.include[*].paths` glob (dirs recursively) and asserts set-equality with the `test_*.py` modules under the four collection roots. Red: `56 test modules are collected by NO pytest shard` (all of `test_monitor_daemon/`, `test_watchdog/`). Green: ninth shard `scripts-daemons` (`paths: "scripts/tests/test_monitor_daemon scripts/tests/test_watchdog"`); pinned shard list updated. New shard under the CI flags (`-n auto --dist loadfile`): `752 passed in 8.47s`. `test_ci_deploy_concurrency.py` 18 passed. Coverage re-measure recorded under T-123. |
| T-125 | DONE | see commit | `modify-order-close-pnl.test.tsx`: modal rendered with `openOrders.open_orders=[the order being modified]` against 50 held. Red under both mutations of `ModifyOrderModal.tsx:397-401` (exclude arg dropped → `1 failed`; ids swapped → `1 failed`, both render `Max Loss` instead of `Est. Realized P&L`). Second case: a DIFFERENT working SELL sibling still consumes held units (`Max Loss` present). Green on current code: file 11 passed. No source change. |
| T-123 | DONE | see commit | `test_pytest_coverage_ratchet_measures_branches` (tomllib over `pyproject.toml` + the py-tests commands). Red: `branch` absent and `--cov-branch` absent → 1 failed. Green: `[tool.coverage.run] branch = true`. Re-measured on this branch (recursive, `-n auto --dist loadfile`, with the T-122 dirs restored): `TOTAL                                                         53304  17061  16156   2223  65.93%` — statement+branch 66%, Branch column present, 7818 passed in 108.9s. Above the 56 ratchet; no T-050 threshold decision needed. |
| T-124 | DONE | see commit | Source fix (real defect). `test_journal_realized.py` +4: CLOSED round-trip row followed by a BUY → must yield no `o1` key (was `{'o1': 998.0}`); same close under API execId + Flex tradeID → `{"e2": 2000.0}` (was `{}`, contract dropped to IB's drifted figure); overlay on a BOT fill with those rows leaves it untouched; two equal same-day partials from ONE writer still both count. Red: 3 failed. Green: `_journal_entry` skips `CLOSED` like `journal_basis.py:133`; `_replay_contract` skips a row whose `contract_fill_fingerprint` was already replayed from the OTHER id namespace (numeric Flex vs dotted API). File 19 passed; 28 across dependents. Out of scope (NEW_FINDINGS candidate): a composite Flex row (aggregate qty) vs per-fill API rows has different fingerprints and still double-counts. |
| T-126 | DONE | see commit | `test_evening_execution_sweep.py::TestMirrorCarriesJournalRealizedPnl`: `db.client.get_db` stubbed to a journal fake; mirrored payload must carry `realizedPNL == 2000.0`, `realizedPNLSource == "journal"`, `ibRealizedPNL == 1234.56`; `get_db` raising → IB figure kept and `overlay skipped` warning logged. Red: overlay call at `:286` deleted → 2 failed; import misspelled → 1 failed (the try/except no longer hides it). Green on current code: file 17 passed. No source change. |
| T-127 | DONE | see commit | Source fix (real defect). `test_silent_degradation_bounds.py::TestChildrenRunInTheirOwnProcessGroup`: each of `run_script` / `run_script_raw` / `run_module` driven with a REAL sleeping child (`create_subprocess_exec` wrapped to record `os.getpgid(child)` while alive; `os.killpg` recorded, not delivered, so the red cannot SIGKILL the runner); plus `_terminate_child` over a same-group `Popen` child must not `killpg` the caller's own pgid. Red: `run_module` in caller's group + `_terminate_child` killpg'd own group → 2 failed. Green: `start_new_session=True` on `run_module`; `_terminate_child` skips `killpg` when `pgid == os.getpgid(0)`. File 25 passed; 222 across every test importing `api.subprocess`. |
| T-128 | DONE | see commit | New `auth-perimeter-behaviour.test.ts` (8): ws-ticket 401 + no mint on denial, mint carries `access.principal.token`, rate option requested; garch scan 401 + no spawn, forward under rate; service-health probe body has `last_error`/`error_summary` null while an operator body carries them, 61st probe in a minute → 429 with `Retry-After`. Mutations run against BOTH files: guard call deleted with import kept → behavioural 3 failed / regex 0; `rows.map(forProbe)` dropped with `forProbe` still defined → 1 / 0; garch `rate` commented out → 1 / 0. The eight superseded source-regex `it`s removed from `auth-perimeter-delta.test.ts`; its matrix-classification and R-181/R-182 tests untouched. No source change. |
| T-129 | DONE | see commit | `test_a_degraded_payload_records_error` now patches `db.writer.record_service_health` and calls the REAL `_record_perf_twr_health`; new `test_main_writes_the_error_row_for_a_degraded_build` drives `main()` with `build_and_persist` stubbed to `degraded` and asserts one `("perf-twr", "error", class=degraded_build)` row and empty stdout. Mutations: `_perf_twr_state → "ok"` → 3 failed (was 0 for the two rewritten cases); wrapper body `pass` → 3 failed. Green on current code: file 11 passed. No source change. |
| T-131 | DONE | see commit | Source fix (real defect). `test_trin.py::test_delayed_prints_do_not_promote_the_badge_to_in_zone`: ten `source="ib-delayed"` samples at 0.60 through `build_output` → `state != "in_zone"`. Red: `in_zone` (`_current` called `classify_state(ma10)` with no source). Green: `classify_state(ma10, latest.get("source"))`; live `source="ib"` companion still reaches `in_zone`. `test_trin.py` + `test_honest_exhaustion_oneshots.py` 32 passed. |
| T-134 | DONE | see commit | `test_grok_responder_fails_closed.py::test_inaccessible_paths_directive_denies_the_host_secrets` parses the `InaccessiblePaths=` directive (not the file text) and requires `/etc/radon/env` alongside `/home/radon/radon-cloud`. Red: `'/etc/radon/env' not in {...}`. Green: `-/etc/radon/env` appended (dash prefix so a pre-bootstrap host without the file still starts the unit); legacy path kept; comment corrected; `installed-units.sha256` bumped for the unit. File 13 passed; 453 across the five cloud unit/manifest suites. |
| T-130 | BLOCKED (operator) — automated half DONE | see commit | Tests now parse `ci.yml` as YAML: a step must `run` `scripts/ci/check_demo_isolation.py` and `docs/demo-environment.md` must name that step's job (red: doc said `py-tests`, the step is in `py-coverage`); the skip branch must emit a `::warning …::` annotation (red: bare `exit 0`). Green: doc corrected; skip now annotates every workflow run "Demo isolation guard SKIPPED". The AC's "cannot short-circuit" half needs the `TURSO_DEMO_DB_URL` / `TURSO_DEMO_APP_DB_URL` secrets provisioned (repo has no demo env template to run the guard against, and an unconditional run reds every CI until they exist) — operator decision, see PR body. `test_demo_isolation_is_wired.py` 8 passed, `test_ci_deploy_concurrency.py` 19 passed. |
| T-132 | DONE | see commit | The four `TestStorage` idempotent-upsert tests (`test_divyield.py`, `test_hyad.py`, `test_hhlev.py`, `test_trin.py`) now wire `writer.get_db` to the migration-loaded sqlite and call the REAL `upsert_*_rows` twice for one date, asserting the SELECT-back row. Mutation: `count_above`/`total` swapped in `upsert_divyield_rows` and `adv`/`dec` swapped in `_trin_sample_params` → 2 failed (was 0). Five dead constants (`DIVYIELD/HYAD/HHLEV_UPSERT_SQL`, `TRIN_SAMPLE/DAILY_UPSERT_SQL`) deleted from `writer.py` — no other user. Four suites + honest-exhaustion: 124 passed. |
| T-133 | DONE | see commit | `TestRun` classes in `test_divyield.py` (2), `test_hyad.py` (3), `test_hhlev.py` (1) drive the real `run()` with the fetchers / `_turso_history` / `_latest_y10` / `_spx_by_date` stubbed and assert the recorded writer calls: a new date → `("rows", 1)` before the snapshot; an unchanged day → no row but the `ok` heartbeat; a revised stored HYAD day → rewritten; an empty HYAD window → `RuntimeError` with NO heartbeat; HHLEV → all 304 rows every run. Mutations `rows → []` / `changed → []` / `persist_result(payload, [])` → 4 failed (was 0). Green on current code: three files 98 passed. No source change. |

## Delta audit 2026-08-26 (Wednesday, audit mode)

Range `27665c43..HEAD` (`1b326772`) — 33 commits, 236 files. Tree clean at
start (only the wrapper's own `.weekend-runner.lock/`); no stash, no parked
WIP. `rtk` is not installed on this host, so bare `git` was used throughout.

| Gate | Round 1 |
|---|---|
| `python3.13 -m pytest` (recursive) | 7996 passed, 1 skipped, 90 deselected (435.4s) |
| `npx vitest run` | 723 files / 7498 passed (90.5s) |
| `python3.13 -m pytest cloud/tests` | 34 failed, 1062 passed, 5 skipped (230.6s) |

**CI cross-check:** summed the per-job `passed` counts from all 12 `pytest (*)`
jobs of CI run `32926657735` at this same SHA — `1606+960+759+381+748+1170+546+766+721+339
= 7996`, identical to the local recursive run. Re-ran the T-122 shard-union
set-diff independently: py-tests 466/466, cloud-tests 30/30, zero uncovered and
zero double-covered.

**Cloud baseline:** attributed by running the base SHA in
`git worktree add --detach /tmp/base27665c43 27665c43`. Sorted `FAILED` lists
diffed **byte-identical, 34 both sides** — all the known darwin `sha256sum` /
bash-3.2 class (T-118). Passed 1025 → 1062, skips 4 → 5; the one added skip is
the new caddy mechanism test (T-164). Worktree removed after.

**Added-file determinism 3×** (26 added test files, since the delta touches 75
of `web/tests` and 36 of `scripts/tests` and a scoped re-run would again
collapse into a full gate): pytest `60 passed` ×3 (50.8s / 52.5s / 53.0s),
vitest `121 passed` ×3. Load average 6.6–28 across all runs; no round was
load-red.

34 findings filed (T-156…T-189: 3 P0, 14 P1, 17 P2); see `TEST_AUDIT.md`
`## Delta audit 2026-08-26`. Headline: `42641124`'s CI path filter assumes
`scripts/`↔pytest and `web/`↔vitest are disjoint, but this repo deliberately
uses each gate to assert contracts on the other tree — so a `scripts/`-only
push skips the only runner of 11 `scripts/lib/**/*.test.js` files, a
`web/`-only push skips the ⛔ PII plate guard, and a `docs/`-only push deploys
having run no tests at all. Needs an operator eye: T-160 (both coverage
ratchets left `deploy.needs` and `main` carries no `required_status_checks`),
T-161 (suite-wide `retry: 1` under CI), T-164 (installing caddy in the
`cloud-tests` job is a CI change).

## Remediation 2026-08-26

| Task | Status | Commits | Evidence |
|---|---|---|---|
| T-167, T-170 | DONE | (this commit) | The whole portfolio-startup-performance contract file is now behavioural — all four source-string-grepping `it`s converted, none left, none deleted, no source touched. **T-167 red** (mutation: `readPortfolioSnapshot.server.ts:111` -> bare `return undefined;`): the existing `:41 toContain("readPortfolioSnapshotSeed")` stayed GREEN and `portfolio-auto-sync.test.ts`'s existing `resolves.toBeUndefined()` stayed GREEN, while the new `returns the cached snapshot payload from the RSC seed` failed with `expected undefined to be 137425.5` (1 failed / 12 passed). **T-170 red** (mutation: `import PositionTable from "@/components/PositionTable";` added to `WorkspaceSections.tsx`): the existing `:33 not.toContain('from "./PositionTable"')` stayed GREEN while the new `keeps PositionTable and PortfolioSections out of the workspace import graph` failed with `expected ['react','next/navigation',...(72)] to not include 'PositionTable'` (1 failed / 4 passed). Both mutations reverted and both re-confirmed red against the FINAL rewritten tests before revert. **Per-`it` audit:** `:9` prefetch became a render test over Sidebar/MobileAppBar/MobileMoreDrawer/MobileTabBar asserting every emitted anchor's `data-prefetch` is `"false"` with a `length > 0` guard per surface (stronger than the grep: a `<Link>` in a conditional branch or drilled through a wrapper is now covered); `:24` chunk isolation split into 3 `it`s — a normalised import-graph parse (`@/components/X`, `./X`, `./mobile/X` -> `X`, type-only and `dynamic()` loaders excluded, plus a `has("RegimePanel")` guard so a broken parser cannot vacuously pass), an empty-render assertion for `section="portfolio"`, and a `next/dynamic` loader-identity comparison against the real default exports; `:36` server seed split into 3 `it`s invoking `page.default()` with the seed spied (asserting `page.dynamic === "force-dynamic"`, seed reaches `initialPortfolio` BY IDENTITY, stubbed `global.fetch` never called), the `RADON_AUTHLESS_TEST=1` branch, and the shell's `includeEntryDates` true/false split; `:48` FRED became a real `GET()` call asserting one fetch, `init.next.revalidate === 86_400`, the body, and a `Cache-Control` header derived from the same value. No `it` needed a "not expressible" exemption. The only surviving source read is the T-170 import-graph parse, which is the AC's own prescription and is a normalised specifier-SET assertion, so an alias swap cannot dodge it. **Mutation coverage of the new assertions:** seven further independent spot-mutations each caught by exactly the intended `it` (dropping `prefetch={false}` from `MobileTabBar`; `initialSnapshot: undefined`; forcing the shell's portfolio branch false; dropping `await readPortfolioSnapshotSeed()`; removing `next: { revalidate }`; making `case "portfolio":` return markup; replacing the page's `RADON_AUTHLESS_TEST` check with `false`). Green: 2 files / 17 passed scoped; 296 passed across the 39 related files; full web suite 723 files / 7503 passed. |
| T-172 | DONE | (this commit) | The `?include=entry-dates` portfolio request no longer escapes the `/orders` e2e mocks into a live Turso read. New shared `web/e2e/portfolio-request-guard.ts` exposes `watchPortfolioRequests(page)`, which records every routed URL and asserts via `page.on("request")` that no unrouted `/api/portfolio*` request reached the server; wired into the two named specs plus a THIRD miss the finding did not have. Red evidence is static / in-process, NOT a full e2e run (no dev server was up: `curl http://127.0.0.1:3000/` returned `000`, and starting one is out of this loop's rails), exercising the installed `playwright-core@1.58.2` `urlMatches` directly: glob `**/api/portfolio` vs `/api/portfolio?include=entry-dates` -> **false** (both with and without baseURL), while `**/api/portfolio**` -> true; both globs match the un-queried `/api/portfolio`. **Third miss found:** `mobile-a11y-pwa.spec.ts:27` — its `ROUTES` list includes `/orders` and `page.goto(route)` loops it, so it leaked exactly like the two named specs. Fixed. Full inventory: 157 `api/portfolio` references / 131 `page.route` registrations in `web/e2e` — 106 bare `**/api/portfolio`, 25 `**/api/portfolio**`. Only bare-glob specs that reach a page where `includeEntryDates` is true can leak; `usePortfolio` has exactly one call site (`WorkspaceShell.tsx:93`, `includeEntryDates: isOrdersPage`) and `app/orders/page.tsx` is the only route rendering `section="orders"`, so exactly three specs qualified and all three are fixed. The other 103 target `/portfolio`, `/regime/*`, `/performance` and ticker pages, which request the un-queried URL that the bare glob DOES match — widening them would be unrequested churn. **Query-string sweep (going beyond the AC):** 76 distinct `page.route` globs, 53 lacking a trailing wildcard, each cross-checked against `web/lib`, `web/components`, `web/app`, `web/hooks` for query-string or path-segment construction and verified against `urlMatches`. Only the portfolio one is a real leak. `**/api/flow-analysis` and `**/api/watchlist` are covered by sibling routes / a catch-all. `**/api/admin/services` vs `/api/admin/services/{unit}/stop` is **latent, not live**: `admin-panel.spec.ts` opens the Stop dialog but never confirms, so the sub-path is never requested — left alone because widening it would change unrelated tests' semantics, but a future test that confirms that dialog would let a real systemd stop escape to the server. Green: 148/148 across the 11 vitest files matching `usePortfolio\|includeEntryDates`; the three specs plus the new helper typecheck clean under strict (two pre-existing unrelated `TS7053` errors at `modify-order-resting-limit.spec.ts:122` untouched); `playwright test --list` collects all three specs (22 tests, 3 files) and correctly does not collect the new non-spec helper. |
| T-163 | DONE | (this commit) | The Equibles silent-death fix now reaches all five oneshots, the class has a contract test, and the two docs that asserted a deleted guard were corrected. `fetch_equibles_ats_venue_share.py` and `fetch_equibles_short_crowding.py` construct `EquiblesClient()` inside the health-reporting block, matching how `3b2945b7` fixed the 13F and filing-forensics producers. Red (a) per-producer: `assert [] == ['error']` in both new `test_run_heartbeats_error_when_the_key_is_absent` cases (2 failed, 87 deselected). Red (b) class contract: `Client construction outside the health-reporting block ... nothing alerts: fetch_equibles_ats_venue_share.py:593, fetch_equibles_short_crowding.py:605` (1 failed, 2 passed). Red (c) docs: `docs/cloud-services.md: claims EQUIBLES_API_KEY IS in the contract, but it is absent` (1 failed, 1 passed). Revert-red for (b) demonstrated by moving the ATS ctor back above the `try`: `fetch_equibles_ats_venue_share.py:601 EquiblesClient() in run()`, 1 failed, then restored. The class test is an AST walk over every `scripts/` module declaring `SERVICE` or `SERVICE_NAME` and owning a `record_service_health` writer, flagging any `*Client()` construction where no enclosing `try` has a health-writing `except`, directly or one caller hop up (that hop is what lets 13F's `_resolve_client` and COT's `main`-guarded `run` pass). Its docstring states the limits honestly: AST-only, one hop, does not verify the service name or state written, does not model dynamic factories, says nothing about `sys.exit`/`parser.error` paths. **Deviation flagged:** the walk keys on `SERVICE` OR `SERVICE_NAME` (filing-forensics uses the latter, so the literal criterion would have missed it) and detects writers by the `record_service_health` CALL rather than the function name (COT names its writer `_record_failure`); a dedicated test asserts all five Equibles producers stay in scope. Docs: `docs/cloud-services.md` and `docs/operations.md` now describe the per-producer error heartbeat instead of a `required-env.txt` guard that no longer exists; `EQUIBLES_API_KEY` was NOT re-added, since that is gated on the host and is the operator's call. Green: 320 passed for `-k equibles`; 22 passed in `test_service_registration_completeness.py` + `test_docs_contract.py`; 74 passed in `cloud/tests/test_scripts.py`, no new reds. |
| T-168, T-169 | DONE | (this commit) | Both portfolio/orders cache contracts are now behavioural instead of source-string greps. No source changed - both findings were pure test-coverage holes and the mutations confirm the new tests catch the production behaviour's removal. **T-168 red** (mutation: `portfolioReadCache.ts:27` passes `0`): the pre-existing assertions stayed GREEN under the mutation (6 passed), while the new `serves one snapshot fetch for the whole 15-second window, then refetches` failed with `expected { value: 'snapshot-v2' } to match object { value: 'snapshot-v1' }` (1 failed / 9 passed). Also verified the sibling use site (`:38` -> `0`): legacy 6 passed, new 60-second entry-date window test failed. TTL boundaries are LITERALS (`14_999`/`15_001`, `59_999`/`60_001`), never the constant, so they cannot go self-referential the way `portfolio-auto-sync.test.ts:208` did; the constants' values are still covered via `expect(PORTFOLIO_*_TTL_MS).toBe(...)` on the imported values. **T-169 red** (mutation: delete ONLY the `finally { invalidateOrdersSnapshotCache(); }` in place/cancel/modify): the bridging regex at `:76-83` stayed GREEN (6 passed) while all four new race cases failed - place `expected [1] to include 2`, cancel `expected [1,2] to not include 2`, modify `expected 502 to be 200`, modify/replace `expected [1] to deeply equal [3]`. Coverage was EXTENDED beyond the AC to `cancel/route.ts` and `modify/route.ts`, which the finding names as having the same hole, and the `:93-98` portfolio bridging assertion was fixed the same way (mutation: wrap `invalidatePortfolioReadCaches()` in `if (process.env.X === "1")` - legacy regex 6 passed, new test failed with `expected { value: 'snapshot-v1' } to match { value: 'snapshot-v2' }`). The `MUTATING_ORDER_ROUTES` structural grep was KEPT as a presence/ordering floor and annotated to point at the behavioural file; only the two portfolio source-text greps were replaced. Green on unmutated HEAD: 6 files / 66 passed (`orders-performance-cache`, `orders-place-cache-race`, `db-cache`, `db-read-cache-contract`, `orders-read-from-db`, `portfolio-auto-sync`); 6 adjacent orders/route suites 228 passed; `tsc --noEmit` clean. Safety: `radonFetch` and `dbExecute` are mocked in the race test - no IB connection, no Turso, no order placed. |
| T-158 | DONE | (this commit) | Combo quote freshness is now derived from the LEGS instead of the wall clock, so the 5-minute live-quote gate can fire on a combo and stale bid/ask no longer prefills a net limit price as live. `ComboNetQuote` gains `timestamp?`; `comboQuotePriceData` stamps `timestamp ?? ""` instead of `new Date().toISOString()`; new `oldestQuoteTimestamp()` lives in `pricesProtocol.ts` (with `PriceData`, to avoid a `positionUtils` <-> `quoteTelemetry` import cycle) and returns `undefined` if ANY leg lacks a parseable timestamp, so absence fails CLOSED. `NetOptionQuote.asOf` added, with a manually overridden leg contributing `null` so the net reports unknown age; `resolveNaturalSpreadQuote` returns `asOf`. All six named call sites threaded: `OrderTab.tsx`, `PositionTradeTicket.tsx` (`useTargetQuote` now returns `asOf`), `MobileOrderTicket.tsx`, `OptionsChainTab.tsx`, `ChatPanel.tsx`, and `ModifyOrderModal.tsx` (`resolveOrderPriceData`'s BAG branch collects leg quotes in both the order-leg and portfolio-fallback paths). Red: 7 failed / 1 passed, the AC verbatim — `relabels a stale combo net quote to CLOSE instead of rendering it as live: expected 'MARK' to be 'CLOSE'`. **AC correction:** the AC as written passes ACCIDENTALLY against real wall-clock time (a 2026-08-26 stamp trips `hasLiveQuote`'s future-quote guard), so the landed test pins the clock with `vi.setSystemTime(Date.parse("2026-08-13T15:00:00Z"))`; without the pin the bug hides. Green: 8 passed in the new `combo-quote-freshness.test.ts`; 166 passed across the 10-file scoped set (`quote-telemetry-fallback`, `order-quote-telemetry`, `order-tab-quote-telemetry`, `open-order-combo-modify`, `mobile-order-ticket-quote-telemetry`, `spread-price-bar`, `order-reliability`, `options-chain-utils`, `order-tab-close-position-sign`); FULL web suite 724 files / 7506 passed; `tsc --noEmit -p web/tsconfig.json` exit 0; ESLint on the 10 touched files 0 errors (6 pre-existing `exhaustive-deps` warnings, none on added lines). Adjacent surface deliberately left alone and filed instead: `resolveSpreadPriceData` (`positionUtils.ts:486`) still stamps `new Date().toISOString()` for the ticker-detail spread header - same bug class, not named in T-158. |
| T-164 | DONE | (this commit) | The Caddyfile contract now catches the one change the file explicitly warns against. `reverse_proxy_block(content, upstream)` was rewritten as a REAL extractor: it strips Caddyfile `#` comments quote-aware (so the file's own `fail_duration` warning comment neither satisfies nor trips the assertion) then walks brace-balanced and quote-aware to the closing brace. The old one-level-nesting regex would have mis-scoped the `127.0.0.1:8330` block, which nests `handle_response` and responds with a brace-heavy backticked JSON literal. New `TestSingleUpstreamStaysInThePool::test_no_fail_duration_on_the_single_upstream_blocks`, parametrized over `localhost:3000` and `localhost:8321`. Red (mutated Caddyfile carrying `fail_duration 30s` in both upstream blocks): 2 failed / 24 passed / 1 skipped, with all THREE pre-existing assertions PASSING — that contrast is the finding, reproduced. Green (Caddyfile restored byte-identical, `git diff` empty): 26 passed, 1 skipped. **Second hole found and closed:** `lb_try_interval` was unasserted — raise it past `lb_try_duration` (`lb_try_interval 20s`) and Caddy makes a single dial then gives up, while `retry_window_seconds` still reads 15s and all three shipped assertions stay green. Added `test_retry_interval_re_dials_across_the_gap` (block-scoped, both upstreams; absent means Caddy's 250ms default, which passes), shown red under that mutation with the three pre-existing assertions green. `test_edge_health_status_stays_fast` has no analogous hole: it asserts the ABSENCE of a window, so any added setting can only make it stricter. `cloud/tests` overall: 34 failed, 1066 passed, 5 skipped in 266.8s — matches the known darwin baseline (T-118) exactly, no new red. **Left for the operator:** (a) `health_uri` takes the single upstream out of the pool the same way `fail_duration` does, but the AC named `fail_duration` only, so no policy was invented; (b) installing caddy in the `cloud-tests` job so `TestRestartWindowMechanism` stops being decorative is a CI change and was NOT applied - the proposed snippet is in the PR body. |
| T-160, T-161 | DONE | (this commit) | **T-160:** `web-coverage` and `py-coverage` are back on `deploy.needs` with the same `success \|\| skipped` shape the four test jobs use, so a coverage regression can fail the release again. Red: `web-coverage can neither fail the deploy job nor block a push to main: it is not in deploy.needs with a success\|\|skipped clause, and neither 'web-coverage' nor 'Vitest coverage ratchet' is declared in required-status-checks.json. Its threshold is advisory.` plus `assert "needs.web-coverage.result == 'skipped'" in <deploy if>` — 2 failed, 21 deselected. The two cases that PINNED the weakening were corrected, not deleted: `test_coverage_ratchets_do_not_serialize_deploy` keeps every original assertion (the `web-tests`/`py-tests`/`cloud-tests`/`stage-release` membership checks and the `merge_vitest_coverage` / `fail-under=56` content checks) and was renamed `test_coverage_ratchets_gate_the_deploy` with only the two defect-asserting lines corrected; `test_deploy_accepts_skipped_test_jobs`'s `assert "web-coverage" not in deploy_if` was folded into its skip-tolerance loop. **The two goals are irreconcilable and the safety arm was chosen deliberately:** the pinned test was guarding deploy LATENCY (the coverage jobs are barrier VMs that spin up after all shards finish, costing the release critical path ~1min), and you cannot gate on a job without waiting for it. No threshold was lowered (T-050 discipline). **T-161:** global `test.retry` is `0` again; the two named jsdom cases carry `vi.setConfig({ testTimeout: 20_000, hookTimeout: 20_000 })` instead, which is honest where a blanket retry is not. Red: `expected 'process.env.CI ? 1 : 0' to be '0'` and `dashboard-newsfeed-pagination.test.tsx must raise testTimeout via vi.setConfig: expected null not to be null` — 2 failed. Both named files pass with NO retry: `dashboard-newsfeed-pagination` 7 tests / 15.33s wall (tests 10.29s), `theta-harvester-scanner` 13 tests / 18.42s wall (tests 13.88s), both under the 20s ceiling; 20 passed together. Green: 23 passed in `test_ci_deploy_concurrency.py`; 575 passed for `-k "ci or workflow or coverage"`; 2 passed in the new `vitest-config-contract.test.ts`. **Operator eye required** on the `ci.yml` diff and on `.github/required-status-checks.json`, a new hand-maintained mirror of `main`'s protection contexts (empty today, matching the live API) that CANNOT verify live GitHub state; its own `_comment` says so. Real branch protection on `main` still does not exist and is an operator action outside this commit. Known residual: `web-coverage` is `if: needs.web-tests.result == 'success'`, so a path-filtered one-sided push still skips the ratchet (the T-156/T-157 interaction), which the `success \|\| skipped` clause the AC specified does not address. |
| T-166 | DONE | (this commit) | The price-history backfill `9fd59f7c` moved inside the per-minute portfolio sync is now wall-clock bounded. `BACKFILL_TOTAL_BUDGET_S = 20.0` with an injectable monotonic clock, checked between symbols AND between ladder rungs. Red: `TypeError: backfill_price_history() got an unexpected keyword argument 'clock'` and `AttributeError: module 'portfolio_risk' has no attribute 'BACKFILL_TOTAL_BUDGET_S'` (2 failed, 23 deselected). Budget rationale: the loader runs every 60s; a single rung can burn ~30s alone (IB `connect(timeout=10)` + `get_historical_data(timeout=20)`; Yahoo `urlopen(timeout=30)`) and the check is between rungs, so worst case is budget + one in-flight rung ~= 50s, inside the cadence with headroom. Throttle interaction handled deliberately: `_record_backfill_attempt` moved from one upfront bulk write to a per-symbol write at the moment that symbol's ladder starts, so symbols the budget never reached are NOT marked retried-and-failed and `_due_for_backfill` returns them next run; a symbol whose ladder did start but was cut short mid-rung IS recorded, because it consumed real IB wall clock and leaving it unmarked would hammer IB every minute. Landed tests use an injected clock, not a real sleep. AC (b) `test_measurement_failure_does_not_block_the_snapshot` lands as a REGRESSION LOCK, not a red: the `except Exception` fail-open at `ib_sync.py:1381` already writes `risk_budget = None` without propagating, and the finding's claim was that it is untested, not broken. Mutation proof (run by the lead in the LANDED tree, `scripts/tests/test_ib_helpers.py::test_measurement_failure_does_not_block_the_snapshot`): replacing the fail-open body at `ib_sync.py:1381-1383` with a bare `raise` reds it (`RuntimeError: price history unavailable`, 1 failed / 60 deselected); restored, 1 passed. The lock is real. Green: 25 passed in `test_portfolio_risk.py`; 62 passed for `-k "ib_sync or portfolio_risk or correlation"`; 61 passed in `test_ib_helpers.py`; 178 passed across every file touching `portfolio_risk`/`price_history`. Callers swept: `backfill_price_history` has exactly one non-test caller (`portfolio_risk.py:389`), `attach_correlation_risk_report` exactly one (`ib_sync.py:1370`). |
| T-156, T-157, T-159 | DONE | (this commit) | The CI path filter's gate routing is now DERIVED FROM THE TREE instead of a hand-maintained prefix list, closing all three P0/P1 arms of `42641124`'s disjoint-trees assumption. The test parses `vitest.config.ts`'s `test.include` (not `coverage.include`), reduces each glob to its root prefix, walks the collected files, and scans them plus `tests/`, `scripts/tests/`, `cloud/tests/` for cross-tree path literals (slash literals with a boundary guard so `https://.../data/` and `scripts/config/` do not register, plus `ROOT / "site" / ...` pathlib chains anchored on a `*ROOT`/`*REPO`/`*DIR` identifier). Every derived prefix must turn its gate on, so a NEW cross-tree read the map lacks reds the suite. Red: 8 failed / 7 passed, naming each arm — `vitest include roots that do NOT turn on the web gate: scripts/lib/`; `trees read by vitest tests that do NOT turn on the web gate: cloud/, docs/, scripts/, tasks/, tests/`; `trees read by python tests that do NOT turn on the python gate: .claude/, docs/, lib/, site/, tasks/, web/`; `classify(["site/public/plates/dashboard-x.png"])` and `classify(["web/lib/chat.ts"])` python-gate `False`; `docs/options-structures.json` and `docs/owners.json` `(False, False)`. `path_filter.py` carries the result as `WEB_READS`/`PYTHON_READS`, which only ever switch a gate ON; the unknown-path fallback is still measured against the owned prefixes so `test_unknown_runtime_path_runs_both` keeps its meaning. Two tests that PINNED the defect (`test_scripts_only_skips_web`, `test_web_only_skips_python`) had their assertions corrected and were renamed — same inputs, nothing deleted, skipped or loosened. Green: 15 passed in file; 38 passed with `test_ci_deploy_concurrency.py`; CLI smoke `BASE_SHA=HEAD~1` → `python=False web=False files=1` (an md-only range still skips both). **Operator note:** with the tree's actual coupling, nearly every runtime path now runs both gates; only `brand/`, `package.json`, `bun.lock`, `vitest.config.ts` (web-only) and `pyproject.toml`, `requirements*.txt` (python-only) stay single-gate. That is the honest reading of the ACs, not a widening — decoupling the cross-tree reads is the alternative and is an operator decision. |
| T-165 | DONE | (this commit) | Gate 3 no longer reads clean on a partially measured book. `correlationRiskBanner.ts` gains an `"unmeasured"` level: the within-budget branch (clusters present, no breaches) now returns `unmeasured` with the headline `Gate 3: partial correlation read, N positions unmeasured` whenever `insufficient_data` is non-empty, instead of `Gate 3: N correlated clusters within budget`. `9fd59f7c` made partial measurement the steady state (`BACKFILL_MAX_SYMBOLS_PER_RUN = 4`, `BACKFILL_RETRY_S = 6h`), so this was a live gate failure, not an edge case. Red: `expected 'info' to be 'unmeasured'`; `expected ['none','info'] to not include 'info'`; `expected '' to contain '--crb-accent'` — 3 files / 3 failed, 15 passed. No component change was needed (`CorrelationRiskBanner.tsx` passes `banner.level` straight to `data-level` and only hides on `none`); the existing `showUnavailable` escape hatch for the no-cluster case is untouched, so `OrderRiskGate` and `AttributionPanel` are unchanged there. Accent is a brand token (`--warn`), no raw hex, no radius change. Green: 3 banner files / 18 passed; 11 consumer files / 113 passed; `tsc --noEmit` clean of correlation diagnostics. |
| T-171 | DONE | (this commit) | Per-position Day P&L now gates on sync PROVENANCE, not the wall clock, closing R-107 one level down: `withSessionIbDailyPnl(positions, now, lastSync)` delegates to `isIbDailyPnlFromCurrentSession` instead of `isIbDailyPnlCurrent(now)`, and `PositionTable` passes `portfolio?.last_sync`. Crypto carve-out unchanged. Red (Monday 08:00 ET, sync = the previous Saturday, equity option carrying `ib_daily_pnl: 13951.76`): `expected '…' not to contain '13,952'`, received `———+$13,952$25,000…` — 1 failed / 4 passed, with the three pre-existing specs GREEN throughout, which is the finding. Green: 5 passed in file; 23 files / 178 tests across every `position-table*` / `day-pnl*` / `mobile-position*` / `dashboard-kpis` / `account-*` suite; 9 files / 55 tests across the remaining PositionTable and MetricCards consumers; `tsc --noEmit` clean on the touched files. Tests pin the clock with fake timers so they cannot false-red on a real weekend (T-117 class). Callers swept repo-wide: `MetricCards.tsx:642,645` and `dashboardKpis.ts:47` were already provenance-aware and are untouched; `PortfolioSections.tsx:117,148,179` already passes `portfolio`, so production gets `last_sync`; no `scripts/` callers. |
| T-162 | DONE | (this commit) | The credit-spread and IEI/HYG fetchers now heartbeat an `error` row before dying, instead of two tests pinning the silence `docs/operations.md:187` calls worse than staleness. Both `assert persist_calls == []` tests were rewritten (not deleted) to `test_no_cache_heartbeats_error_before_it_raises`, requiring BOTH `pytest.raises(RuntimeError)` AND `("health", "<service>", "error") in persist_calls` plus no `"ok"` row; the two `test_refuses_empty_series` tests that pinned the same silence on the second exit path were rewritten to the same contract. Red: 4 failed (`assert ('health','credit-spread','error') in []` x2, `('health','iei-hyg','error') in []` x2), 2 passed, 64 deselected. Source fix: `_record_error_health()` in each producer mirrors `fetch_equibles_smart_money_13f.py`'s `_record_health` (no new mechanism; `error` is the writer's state, there is no `degraded`), best-effort so a broken writer cannot mask the original failure; `run()` wraps the cycle, records, then bare-`raise`s. The `RuntimeError` in `_serve_cached` was NOT weakened. Green: 70 passed in the two files; 30 passed in `test_service_registration_completeness.py` + `test_silent_degradation_bounds.py`; 91 passed across the five other importers. |

## Delta audit 2026-08-27 (Thursday, audit mode)

Range `1b326772..HEAD` (`789aabea`) — 43 commits, 264 files. Tree clean at
start (only the wrapper's own `.weekend-runner.lock/`); no stash, no parked
WIP. `rtk` is not installed on this host, so bare `git` was used throughout.
`origin/testing/weekend-2026-08-27` did not exist at pre-flight; the empty
branch was pushed immediately as the cross-host collision signal.

| Gate | Round 1 |
|---|---|
| `python3.13 -m pytest` (recursive) | **7 failed**, 8153 passed, 1 skipped, 90 deselected (397.8s) |
| `npx vitest run` | 758 files / **1 failed** / 7702 passed |
| `pytest cloud/tests` | 34 failed, 1099 passed, 13 skipped (darwin; FAILED list byte-identical to `1b326772` in a worktree) |

**The 7 pytest reds are REAL and `main` is red (T-237).** All seven are in
`scripts/tests/test_portfolio_risk_gate3_measurability.py`, added by `789aabea`
(reliability PR #100), and they reproduce **7/7 in isolation in 2.26s** —
`AttributeError: module 'portfolio_risk' has no attribute
'BACKFILL_WALL_CLOCK_BUDGET_S'. Did you mean: 'BACKFILL_TOTAL_BUDGET_S'?`
The constant has never existed (`git log -S` returns nothing); the real one is
`BACKFILL_TOTAL_BUDGET_S = 20.0` at `scripts/portfolio_risk.py:84`, which is
what the T-166 remediation landed last weekend. CI at this SHA agrees: run
`33038426120` is `failure` on `pytest (scripts-npsz)`, with `Prestage VPS
release`, `pytest coverage ratchet` and `Deploy to VPS` all correctly SKIPPED.
The deploy gate held; the consequence is that production has not deployed
since that merge. Load was 21 at the start of the gate and 10.8 at the end, so
load is not the explanation — the distribution (7 reds in ONE file,
AttributeError-shaped) is what separates this from the 2026-08-23 flake class.

**The 1 vitest red IS the load class (T-238).**
`portfolio-startup-performance-contract.test.ts:172` at **5041ms** against the
5000ms default, under load average 36. Green **8 passed ×3** in isolation
(2.91 / 3.30 / 3.65s). Filed as P2 for the margin, not as a regression.

**Cloud baseline unchanged at 34.** Sorted `FAILED` lists at HEAD and at
`1b326772` (`git worktree add --detach /tmp/base_1b326772`) diffed
**byte-identical, 34 both sides** — the known darwin `sha256sum` / bash-3.2
class (T-118). Passed 1062 → 1099, skips 5 → 13; the eight added skips are
exactly the two new files filed as T-204 and T-205.

**Collection union clean on all three gates** (py 478 collected vs 479 shard
union, the one extra being `test_menthorq_integration.py` which is deselected
locally by `-m 'not integration'`; cloud 33/33; vitest 758/758), so T-122
holds. **Enforcement strengthened** — T-160 is fixed, `deploy.needs` 7 → 9
with both coverage ratchets restored. No `.only` anywhere; four new skip
constructs (8 outcomes), none linked to a T-###; no exclusion growth; no
threshold moved, and the coverage measurement got stricter twice.

49 findings filed (T-190…T-238: 6 P0, 23 P1, 20 P2); see `TEST_AUDIT.md`
`## Delta audit 2026-08-27`. Needs an operator eye: T-237 (prod undeployed),
T-205 (no workflow installs caddy, so the order-duplication guard never runs),
T-222 (`main` has no required status checks), T-223 (14 of 150 e2e specs run,
gating nothing).

**Runner hazard recorded:** this audit's draft was overwritten mid-run by the
reliability loop, which uses a separate clone but shares `/tmp` and had chosen
the same generic scratch filename. No repo file was affected and no finding
was lost; all scratch moved to `/tmp/tw-2026-08-27/` and the gate counts were
re-verified from copies. Lesson appended to the skill.

## Remediation 2026-08-27 (Thursday, remediate mode)

Every P0 and P1 filed by this cycle's audit is DONE: **T-190 - T-217 (5 P0,
23 P1)**, plus three P2s taken opportunistically (T-226, T-232, T-238).
**T-237, the sixth P0, was NOT worked here** - open PR #109
(`fix/portfolio-risk-gate3-api-drift`) already fixes it correctly, and
duplicating it would conflict. Reproduced locally to confirm: the audit named
one of THREE causes (`BACKFILL_WALL_CLOCK_BUDGET_S`); renaming it alone still
leaves 7 red, because the ladder stubs take one argument where
`_fetch_closes_via_ladder` takes `(symbol, deadline, clock)` and
`BACKFILL_SYMBOL_WORST_CASE_S` does not exist either. **Merging #109 is what
un-reds `main` and unblocks the deploy.**

Worked as one lead plus eight worktree subagents (`git worktree add --detach`
with symlinked `node_modules`), cherry-picked back serially, each landing
re-verified in the main clone and pushed immediately. Load average ran 3-42
across the run; the reliability loop was running a full `pytest` in its own
clone concurrently.

| Task | Status | Commits | Evidence |
|---|---|---|---|
| T-190, T-211, T-212 | DONE | `252d0329` | The mobile order ticket and the desktop rail now read the same scale, and an unacknowledged order can no longer reach the wire. **T-190 red:** `ticket-risk-scale.test.tsx` 3 failed / 1 passed — `expected '969.47' to be '972.98'` on the mobile breakeven at qty 10, with the DESKTOP assertion at the same qty PASSING, which is the finding; the unbounded sentence read `reaches $9,694,040 at zero` against a correct `$964,040`. `payoffLegs` now maps `quotingLegs` (the ratio-normalised combo) instead of the raw legs, matching `OptionsChainTab.tsx:447-455`. **T-211 red:** `mobile-ticket-transmit-gate.test.tsx` 1 failed / 5 passed — `expected [['/api/orders/place', …]] to deeply equal []`; an unacknowledged naked short call reached `/api/orders/place` because mobile `handleSubmit` had no `transmitArmed` guard. Added, with an armed-path counterpart so the gate cannot decay into an unconditional refusal. **T-212 red:** `expected 'RISK · PER 1× COMBO' to be 'RISK · ORDER TOTAL'` — `TicketRiskBlock` printed AGGREGATE position dollars under a per-combo heading. Mutation checks, both reverted: `OptionsChainTab.tsx:449` `quotingLegs.map` -> `legs.map` reds the desktop breakeven; `:416` `legs` -> `normalizedOrder?.legs ?? legs` collapses MAX LOSS `$7,158 -> $716.50`, confirming the cells were aggregate and the heading was the lie, exactly 10x. **Two corrections to the audit's wording, defect unchanged:** a bare 10-lot short put cannot produce the unbounded sentence (`legRisk` bounds a short put and returns `maxLossUnbounded: false`), so the repro needs an uncovered short call — a 10-lot short straddle; and the rendered mobile breakeven is `969.47`, not `970.30`, because `payoffCurve` samples 96 points and interpolates across the strike kink. Green: 38 files / 264 passed across every suite rendering `MobileOrderTicket`, `TicketRiskBlock`, `OptionsChainTab` or `TickerDetailContent`; `tsc --noEmit` clean. Landed-tree re-verify by the lead: 12 passed on the two new files, 66 passed on three neighbours, and `grep -rn "PER 1× COMBO"` over `web`/`site`/`docs` returns only a comment in the new test. |
| T-191 | DONE | `86634a6a` | `reconcile_tables` no longer lets the ROUNDED row overwrite the good one. Pre-test repro (two tables, same contract, `z_score_3m: None` in both, main `0/0/0`, index `0.43/0.81/0.88`) destroyed the good `81` in BOTH tables, one worse than the audit recorded. Red: `2 failed, 13 passed`, `assert 0 == 81`. Candidates are now RANKED rather than compared on `gap` alone — `_candidate_rank` puts any finite gap ahead of an unverifiable one, and among unverifiable rows prefers the trio that did not collapse onto 0/1 under rounding. When every copy is rounded the output is unchanged, so nothing is invented (there is a dedicated floor test for that). **The TS mirror carried the identical defect** (`Infinity < Infinity`, `web/lib/ctaPercentiles.ts:113`) and both module headers state the two must be kept in step, so it is fixed the same way with mirrored tests; mutating the tie-break back reds it `2 failed / 11 passed`, `expected +0 to be 81`. Green: 15 passed python, 13 passed TS. All 4 python callers and both TS callers swept; no signature changed. |
| T-214 | DONE | `60256c45` | A nulled percentile no longer crashes the CRI report or prints `Noneth`. Red: `5 failed, 1 passed` — `cri_scan.py:1310` `TypeError: '<' not supported between instances of 'NoneType' and 'int'`, so the whole CRI run dies and no HTML is written; `generate_regime_share.card4_cta_squeeze` emitted `"SPX CTAs at the Noneth percentile of their 3M range"`; `build_tweet` died the same way at `:511`. Both readers now render `---` / fall back to the vol-targeting sentence instead of substituting a number. New `test_cta_null_percentile_readers.py` is hermetic (hand-built result dicts, no disk, no DB, no clock). Green: 6 passed in file, 182 passed across the CTA/CRI neighbours. |
| T-215 | DONE | `76cb2bdd` | A nulled prior session is no longer republished as the 50th percentile. Red: `2 failed, 19 passed`, `assert 50 is None`; the tweet read `Since the 2026-08-06 read: ... 50th to 100th percentile` off a prior that was never measured. New `_published_pctile(row)` reads `percentile_3m` off the row and carries `None` through instead of taking `assess_positioning`'s 50-substituted value; the "Since the ... read" line drops the percentile clause when either side is unknown and still narrates position and z. Green: 21 passed. `_spx_delta` is module-private and its dict shape is unchanged; the only consumer of those keys (`generate_cta_share.py:671-677`) is updated here. |
| T-193 | DONE | `32586348` | `/api/orders/modify` no longer reports an accepted modify as failed. Red: `expected 502 to be 200` on `does not report a 502 failure on a modify IB accepted`. `orders` is null ONLY when `/orders/refresh` itself failed, at which point IB has already accepted the modify and nothing has refuted it — the route now gates the refutation on having a book (`if (orders && !isModifyConfirmed(...))`) and mirrors the cancel twin, while 502 stays reserved for a refreshed book that actually refutes. A second test pins that, so the fix cannot decay into an unconditional 200. `isModifyConfirmed` narrowed to a non-null `orders` param; grepped the tree, it has exactly one call site and no external importers (re-confirmed by the lead in the landed tree). The AC's suspicion is confirmed: the source grep at `p2-operability-remainder.test.ts:99-103` is green before AND after, so it has no signal on this defect. Green: 18 passed on the two files, 327 passed across the 12 files touching the modify route. |
| T-217 | DONE | `5769f24a` | `resolveSpreadPriceData` stamps its stalest leg instead of the wall clock (`positionUtils.ts:489`, `timestamp: natural.asOf ?? ""`). Red: `2 failed | 11 passed`, `expected '2026-08-27T07:34:00.115Z' to be '2026-03-02T15:10:00.000Z'` — a 20-minute-old spread quote rendered as live BID/MID/ASK in the ticker-detail header. No clock injection was needed: `resolveNaturalSpreadQuote` already computed `asOf` via `oldestQuoteTimestamp(legQuotes)` and the value was in scope. `?? ""` matches `comboQuotePriceData`'s fail-closed shape, so an unknown leg age parses to NaN and the header relabels to CLOSE rather than guessing. The two new tests express leg ages as offsets from a fixed base, never the host clock. Green: 20 passed on the two files, 43 passed across 11 quote-telemetry / spread / position-tab neighbours. No signature changed; both `resolveSpreadPriceData` callers untouched. |
| T-194 | DONE | `69bb21d4` | The R-250 fat-finger gate is now covered on the option path. Finding confirmed first: `grep -c '"--type", "option"'` on the test file returned `0`. Three deliberate mutations, all reverted: `multiplier = 1` at `order_limits.py:166` gives `1 failed, 8 passed` with `an over-notional OPTION order reached the broker`; indenting the `check_order_limits` block under `if args.type == 'stock':` gives `2 failed, 7 passed`; dropping `"type": args.type` from the params dict gives `1 failed, 8 passed`. **Under all three mutations every one of the six pre-existing cases stayed GREEN — that contrast is the finding.** Three option cases added (notional 20 x $200 x 100 = $400,000 against a $100,000 cap, asserting both `RADON_MAX_ORDER_NOTIONAL` and `$400,000` appear so the x100 multiplier is pinned; quantity with the other caps opened wide, asserting `RADON_MAX_ORDER_QTY` fired and `RADON_MAX_STOCK_ORDER_QTY` did not; and a within-cap positive control so the refusals cannot pass vacuously). The existing stock case now names `RADON_MAX_STOCK_ORDER_QTY`, closing the "cannot tell which cap fired" gap. No source change. Green: 9 passed in file, 59 passed across the order-limits neighbours (49 passed re-verified by the lead in the landed tree). |
| T-208 | DONE | `681494c8` | `test_braked_units_reach_the_start_limit_before_two_cycles` was net-negative and is replaced, not deleted. Red: BOTH tests in `TestFlapBucketIsNotAdvertisedBeyondItsReach` stayed `2 passed` under mutations that break exactly what they claim to guard — deleting `StartLimitBurst=5` from `cloud/services/radon-api.service`, and short-circuiting `_flap_alert` to `return None`. The `to_park < cycle_seconds` assertion also pinned the dead flap path as correct. Replaced with `test_the_braked_units_declare_the_dur_02_brake` (a missing file or directive now FAILS instead of `continue`; the `StartLimitBurst` deletion reds with `{'radon-api': ['StartLimitBurst']}`) and `test_the_braked_crash_loop_pages_p1_through_start_limit_hit` (drives `units.evaluate` with a parked sample and asserts P1 plus the `reset-failed` instruction plus that `_flap_alert` is not the carrier). The R-268 arithmetic stays in the class docstring as reasoning rather than as an assertion. `_flap_alert` was KEPT: `test_watchdog/test_units.py` already reds on its removal and `units.py` documents it as deliberately retained for unbraked long-`RestartSec` units. There is no `flap` key in `BUCKETS`, so that half of the AC did not apply. No source change. Green: 7 passed in file, 318 passed with `scripts/tests/test_watchdog/`. |
| T-210 | DONE | `6d0e53c4` | Both IB daily-P&L e2e specs are pinned to a trading day instead of the host clock. They fixtured `last_sync: new Date().toISOString()` and asserted the literal `-$3,688` / `-$3,405`, while `isIbDailyPnlFromCurrentSession` short-circuits false whenever `isIbDailyPnlCurrent` is false, and that is `isUsTradingDay(etDate(now))` (`ibDailyPnlSession.ts:18-20,32-40`) — so all three tests false-red every Saturday, Sunday and market holiday, on this runner. `PositionTable.tsx:614` reads `new Date()` directly, so freezing the page clock is what settles it. New shared `web/e2e/tradingDayClock.ts` exports `TRADING_DAY_ISO` and `freezeToTradingDay`, the same init-script shape three same-day-P&L specs already carry, and every fixture stamp in both specs derives from that one constant. **The freeze is registered FIRST in each test body** because `addInitScript` runs in registration order and the WebSocket quote fixtures stamp themselves inside their own init script — a real "now" against a frozen page clock reads as a FUTURE quote and trips the `hasLiveQuote` guard (the T-158 failure mode). Red evidence is in-process, NOT a live e2e run (no dev server on this runner; `curl :3000` returned `000`, and starting one is outside this loop's rails): `isIbDailyPnlFromCurrentSession` is false for a Saturday clock and false for a Sunday clock, true for the pinned trading day — 3 passed. Green: `playwright test --list` collects 3 tests in 2 files with the helper correctly NOT collected as a spec; `tsc --noEmit` clean on all three files. The three pre-existing copies of `freezeToTradingDay` are left alone. |
| T-192, T-216 | DONE | `5a7a8dae` | The CI change-classifier no longer routes a root-FILE change to the web gate alone, and the shard-partition guard can see directories. **T-192 verified in-process before any edit:** `classify(['vitest.config.ts'])` -> `(False, True)`, same for `package.json` and `bun.lock`, so a config-only push skipped `py-tests` + `cloud-tests` (9307 tests) while `deploy.if` accepts `'skipped'` — and `test_merge_vitest_coverage.py:38`, the only assertion pinning the vitest coverage thresholds, is inside that skipped set. A ratchet could be lowered with no test running anywhere. Red: `2 failed, 15 passed` naming `bun.lock`, `package.json`, `vitest.config.ts` and the module that reads each. Root cause fixed as the finding named: `_top_level_trees()` filtered `git ls-files` on `if "/" in line`, so root-level FILES were never derived; added `_root_level_files()` + `_root_file_re()` and a guard asserting every root file named by a module under `tests/`, `scripts/tests/`, `cloud/tests/` routes `python=True`. Documentation-suffixed root files stay excluded deliberately — a `.md`-only range skipping both gates is by design, and changing that is an operator decision, not this finding's. **T-216 red in two forms** against the unmodified guard, using a `scripts/tests/test_fixture_shard/test_probe.py` fixture: the partition test was `2 passed` (blind), and collecting all ten shard paths matched `0` of that directory's tests. New guard reds with `subdirectories of scripts/tests/ that no py-tests shard names verbatim (a filename glob cannot match a directory): ['test_fixture_shard']`, and the arm was added to BOTH the `py-tests` and `cloud-tests` guards. A subdirectory counts as in scope if it starts with `test_` OR contains any `test_*.py`, which is strictly stronger than the AC. **No `.github/workflows/` change for either finding.** Green: 17 passed, then 41 passed with `test_ci_deploy_concurrency.py`, 48 passed on the final scoped set. Landed-tree smoke by the lead: `vitest.config.ts (True, True)`, `package.json (True, True)`, `bun.lock (True, True)`, `pyproject.toml (True, False)`, `docs/x.md (False, False)`. |
| T-195 | DONE | `3f927442` | The market-value contract now catches the regression it was written for. **Red:** the verbatim pre-fix walk was restored from `8fdd116e^:web/components/mobile/MobilePositionList.tsx` and the existing test stayed `3 passed`; it only reddened after ALSO deleting an unused import, and the walk-pinning regex never fired at all. The new behavioural pin renders `PositionTable` / `MobilePositionList` / `PositionTab` in jsdom and requires the market value to equal `resolveRealtimeMarketValue(pos, prices) ?? resolveMarketValue(pos)` to the dollar; against the same restored defect it reds with `META 40x short $580 put (last outside its own bid/ask): rendered $-8,000, shared $-12,200`. `existsSync` replaces a `catch { continue }` that could swallow a missing surface, the regex self-verifies against the historical defect line, and the `getTodayPnlDollars` check parses to the matching brace instead of an `indexOf + 1_400` byte window. No `it` deleted, no source change. Green: 6 passed. |
| T-196 | DONE | `653355cf` | The 5000-run same-day P&L property is no longer a tautology. **Red:** two independent mutations each left the existing suite `5/5 GREEN` — the AC's `resolveMarketValue(pos) * 2` (which was in fact unreachable, since every generated leg had a positive `market_price` so the null branch never ran) and `computeRtMv` `sign` -> `1`, the +$2.19M MU bug where a short's market value reads positive. The rewritten suite reds against the sign mutation with `expected 20 to be close to -20, received difference is 40`. Quotes are now generated in kinds that fix each leg's mark by construction (tight / wide / stale / no-quote / unpriced) and the expected value is computed from the spec (`sum sign x mark x contracts x 100 - sum sign x |entry|`), never through the library helpers. Run count unchanged at 1000/property; P4 gained real assertions replacing `Number.isFinite`; the new `unpriced` kind finally makes the synced-MV fallback reachable; strikes are assigned by leg index so two legs cannot collide on one option key. Green: 5 passed in 415ms (was 905ms), plus 5 randomized runs over 25k fresh cases, so the oracle is not seed-lucky. No source change. |
| T-202 | DONE | `ae1a0af8` | `migrate.py --demo` no longer depends on an unpinned line. Red (mutation: `main(sys.argv[1:])` -> `main()` at the `__main__` guard): `python migrate.py --demo did not resolve the demo target — the entrypoint dropped the flag and connected to 'libsql://radon-joemccann-PLACEHOLDER.invalid'`, 1 failed / 28 passed, while EVERYTHING pre-existing stayed green under the same mutation (35 passed in the two migrate files, 413 passed in the two cloud unit files). So the mirror preflight would have migrated prod, exited 0, and left demo at its old `schema_migrations` max with a fully green suite. New `TestMigrateEntrypointArgv` pins it two ways: `main(["--demo"])` connects to the demo URL, and a subprocess runs the real `python3.13 scripts/db/migrate.py --demo` and asserts the resolved target. Data safety: a stub `libsql_experimental` on `PYTHONPATH` records the URL and refuses to open a socket, both prod and demo creds in the subprocess env are placeholders, and no `.env` exists in the worktree. Source change is a comment only, marking the argv forwarding as load-bearing and naming its pin. Green: 37 passed. |
| T-213 | DONE | `e818aae9` | `PortfolioSnapshotCorruptError` is mapped instead of being misdiagnosed. Red: `4 failed | 5 passed` — `expected 'DB_UNAVAILABLE' to be 'SNAPSHOT_CORRUPT'` on GET and `expected 'UPSTREAM_ERROR' to be 'SNAPSHOT_CORRUPT'` on the POST sync-failure fallback, so a corrupt persisted snapshot read as a database outage on one path and an IB/upstream failure on the other. The only existing guard, a source grep for the class name in `p2-operability-remainder.test.ts`, passed throughout and cannot see a missing mapping at all. `SNAPSHOT_CORRUPT` added to `ErrorCode` (additive union widening; no other file in `web/` or `site/` references `ErrorCode`/`ApiErrorPayload`), and a `corruptPortfolioResponse` helper logs the row, answers 500, and tells the operator to re-run the sync. The new file also pins that genuine transport failures still answer 503 `DB_UNAVAILABLE` / 502 `UPSTREAM_ERROR`, so the mapping cannot swallow a real outage. Green: 9 passed in file, 258 passed across 11 neighbours, `tsc --noEmit` clean. |
| T-197 | DONE | `2579e7f9` | `run_flow_refresh.sh` defines `_flow_health` before the market-state probe calls it. Red: `2 failed, 9 passed` — `bash resolves functions in source order; the health call runs before '_flow_health' is defined`, with the literal `_flow_health: command not found` in stderr, and `the market-state-probe outage wrote no service_health row: []`. The ok/warn cases passed in the SAME run, which proves the fake recorder works and isolates the defect to the one pre-definition call site. The definition moved above the probe and `mkdir -p logs` was hoisted with it (the probe's own `2>>logs/...` redirect also preceded the mkdir). The grep-only row test was replaced with a behavioural one via a fake `db.hrana_http` recorder; three probe-outage tests added. Source change: real R-221/R-265 silence — the outage wrote no `service_health` row at all. Green: 18 passed, plus `test_flow_refresh_oneshot_contract.py` 2 passed and `cloud/tests/test_systemd_services.py` 408 passed. |
| T-199 | DONE | `f0d6729b` | The Flex durable-store switch fails CLOSED on a credential read failure. Red: `3 failed, 14 passed` — `the fail-closed switch short-circuits under pytest, so no test can reach the code production runs`, plus configured credentials returning `False` and a read failure against a configured URL failing OPEN. The `PYTEST_CURRENT_TEST` branch is gone; a failed credential probe now returns `bool(os.environ.get("TURSO_DB_URL"))` and logs. Source change is real R-212: a transient env-load failure with no sidecar sent every Flex caller into a live IBKR 1025 lockout, and each SendRequest extends the embargo. **Blast radius of removing the pytest branch was checked explicitly** — with no `TURSO_DB_URL` in the environment the function still returns `False` under pytest, `scripts/tests/conftest.py` does not export one, and `grep TURSO_DB_URL .github/workflows/ci.yml` shows it set only inside the demo-isolation step (`:317`), never for a pytest step; the lead re-confirmed `TURSO_DB_URL` is unset on this runner and 131 flex-selected tests pass in the landed tree. `_durable_store_available` has exactly one caller, `_health_rows`, same module, signature unchanged. **No live Flex request was made at any point.** Green: 17 passed, 90 passed across all eight flex files, 61 passed across the flex_embargo consumers. |
| T-203 | DONE | `2b7ed55d` | The `EQUIBLES_API_KEY` guard parses the contract instead of grepping the raw file, and a new parity test derives the requirement from code. Red: with `# EQUIBLES_API_KEY` (commented out) in `cloud/config/required-env.txt` the old raw-file assertion PASSED while `check-env.py:required_keys()` returned `False` for the key. After switching to the parsed set the same mutation reds, and the new parity test reds with `EQUIBLES_API_KEY: ['radon-equibles-13f', 'radon-equibles-ats', 'radon-equibles-cot', 'radon-equibles-filings', 'radon-equibles-short-crowding']`. The new `test_env_contract_parity.py` walks every `cloud/services/*.service` ExecStart chain (a `.sh` wrapper hops one level to its python job, and each job's first-level in-repo imports are followed, which is how `clients/equibles_client.py` is reached), collects env names read with NO fallback via AST, and requires each to be in `required-env.txt` or in an `EXEMPT` map with a written reason. 19 exemptions, each verified at its read site. No source change; `required-env.txt` is byte-identical (mutated only to show red, then restored). Green: 238 passed, 3 skipped across five cloud files; `test_docs_contract.py` 14 passed. |
| T-198, T-204, T-205 | DONE | `65d9d636` | Three cloud guards that were skipping or executing one case now run for real — **and running them for the first time found a production defect.** **T-198 red:** `2 failed, 2 passed` — `a GHCR outage parked the unit even though the pinned image is in the local store`. `image_available()` in `cloud/scripts/radon-app-runtime.sh` now probes `docker manifest inspect`, then falls back to `docker image inspect` (logging the fallback to stderr) before failing; split into `image_in_registry` / `image_in_local_store`, whose only caller is `resolve_image` in the same file. **T-204 red:** `TestDropInsResetExecStartPre executed 1 of 6 parametrized cases.` The base-unit-`ExecStartPre` precondition was REMOVED rather than baselined, because all five per-unit drop-ins already carry `ExecStartPre=` and requiring it unconditionally is the stronger invariant; the one legitimate skip is declared at collection via `pytest.param(marks=...)`, pinned in `DROP_IN_SKIP_BASELINE`, and policed by a staleness test and an every-per-unit-example test. Mutation proof: deleting `# ExecStartPre=` from a previously-skipped param now fails. Both remaining skips read `T-204: fleet template declares no ExecStart override`. **T-205 red:** `no step in the cloud-tests job installs caddy or sets RADON_CADDY_BIN`. **The fallout is the headline: `cloud/caddy/Caddyfile` does not adapt.** `retry_match` and bare `dial_timeout` / `response_header_timeout` / `read_timeout` are the JSON and transport spellings, not `reverse_proxy` subdirectives. `configure_caddy` (`setup-vps.sh:542`) and `deploy-root-helper.sh:604` both gate on `caddy validate`, and prod installs caddy unpinned from the Cloudsmith stable channel — **so this file could never be installed and R-219 / R-220 / R-258 were never in force at the edge**, while all 14 text-regex assertions shipped green. R-220 is the POST-replay guard on `/api/orders/place`. Fixed to `lb_retry_match` and `transport http { ... }`, with a `test_the_shipped_caddyfile_adapts` regression test. **Independently re-verified by the lead against a real caddy v2.11.4:** the pre-fix file at `d746ce12` errors `unrecognized subdirective dial_timeout, at Caddyfile.old:59`; the landed file logs `adapted config to JSON` (the only remaining error is a local `mkdir /var/log/caddy: permission denied`, which is this host, not the config). Green: 42 passed / 6 skipped for T-198+T-204; `RADON_CADDY_BIN=<caddy> pytest cloud/tests/test_caddy_edge_timeouts.py` 17 passed, **0 skipped**; lead's landed-tree run of all three files 66 passed / 2 skipped. Cloud FAILED-list diff before/after via `git stash` A/B: byte-identical. **Two operator items: the `ci.yml` caddy-install step, and the `cloud/caddy/Caddyfile` correction itself.** Known local cost: without caddy this file is now `3 failed` instead of `2 skipped` — deliberately no longer silent; escape is `RADON_SKIP_CADDY_E2E=1`. |
| T-200, T-201, T-226 | DONE | `4c5a0521`, `8931bc6d`, `f2efc72d` | The 2FA lease write paths are bounded, the lease read is off the event loop, and the orphan globals no longer leak. **T-200 red:** `5 failed, 3 passed` naming all five unbounded call sites (`ib_gateway.py:235,246,1051,1346,1390`) and `GuardLockTimeout: 2FA lease guard busy after 5s` propagating out of `reset_restart_backoff` / `_acquire_gateway_push_lease` / `restart_ib_gateway`. New `_acquire_2fa_push_lock_bounded` returns a synthetic `guard-timeout` lease so the caller DEFERS instead of firing a blind second push, and `_release_2fa_push_lock_bounded` logs and skips (the lease still expires at TTL). **Beyond the finding, the AST walk caught `scripts/ib_watchdog.py:657,662`** — the preheld-lease rollback after a failed restart handoff was unbounded too; fixed via `_release_preheld_lease`. **T-201 red:** `EventLoopBlocked: check_ib_gateway ran restart_backoff_state on the event loop: a concurrent asyncio task made no progress for 2.0s while the lease read was in flight`, on both the cloud and the local branch. Both now `await asyncio.to_thread(restart_backoff_state)`; the premise is pinned by `test_ib_2fa_lock_probe_cost.py`, which shows one `check_2fa_push_lock` on a down port costs exactly 3 connect budgets + 2 inter-probe sleeps = 1.85s, with a fully faked clock and socket and zero wall-clock cost. **T-226 red:** `leaked _orphan_reported from a sibling test` and `assert 0.4 == 0.0`. `reset_orphan_state()` and the zeroed interval moved into autouse fixtures in `scripts/tests/conftest.py` and a new `scripts/api/tests/conftest.py` (a separate collection subtree, so one conftest cannot cover both); per-file duplicates dropped. **One correction to the finding:** the storm file's pass/fail was already order-independent because its assertions never read the globals — the real, provable leak is the state itself plus the wasted real sleeps, which the new probe file pins. **Every probe is a fake and every lease file is under `tmp_path`; no gateway, socket, docker or `radon restart` was touched.** Green: 8 passed, 3 passed, 104 passed across the nine importing files; lead's landed-tree re-verify `scripts/api/tests` 731 passed and `scripts/tests -k "watchdog or 2fa or ib_gateway"` 657 passed. Follow-up left alone as outside the AC: `acquire_2fa_push_lock` still runs its confirmation probes INSIDE the exclusive guard (`ib_2fa_lock.py:375`), burning the 5s reader deadline. |
| T-206, T-207 | DONE | `9f18f0d6` | The panel-fault suite mounts components and the R-263 visibility gate is observed instead of grepped. **T-206 red:** all SEVEN real defects were applied to the source at once and the original grep suite still read `Test Files 1 passed / Tests 10 passed` — ScannerHero's outage-alert branch disabled, the meta rail reporting `0`/`0` instead of `---` for a `missing:true` payload, VolConePanel's fault branch negated, its failure reason replaced with static copy, `GammaRotationBody` keeping the `refreshFailed` parameter and using it nowhere, `OptionsExposurePanel` pinning `refreshFailed = null`, and `brushValue` flooring a null `atm_iv` to 0. Inversely, a HARMLESS prop reorder at a call site turned it RED — the suite was measuring formatting, not behaviour. Renamed to `.test.tsx`; all seven cases now mount the real component with a stubbed sync hook, and each mutation was re-run individually and is caught by a named assertion (the prop reorder is correctly no longer caught). No `it` deleted; the three `coneFillPct` cases were already behavioural and are unchanged. **T-207 red (grep blindness):** inverting the gate at its USE site (`if (hidden) { …fetch… }`, so the hook polls only while the tab is hidden and the live open-orders book never refreshes) leaves `document.visibilityState === "hidden"`, `MAX_POLL_INTERVAL_MS` and `failureStreakRef` all verbatim and the grep file `12 passed`. **T-207 red (two real defects):** `expected [60000, 60000] to deeply equal [120000, 240000]` and `expected 60000 to be 300000`. `failureStreakRef.current = 0` ran right after `await fetchOrders()`, but `fetchOrders` swallows its error into `errorRef` and never throws, so the streak was zeroed BEFORE the failure was counted — every outage sat on rung one forever and the 5-minute ceiling was unreachable; separately the hidden branch counted a failure where no request was made, inflating the streak every 30s while backgrounded. Hidden ticks now return early on the plain 30s cadence without touching the streak. The two zero-signal greps were removed from `p2-operability-remainder.test.ts` as strictly subsumed, with a comment pointing at the behavioural file. `useOrders`'s only caller is `WorkspaceShell.tsx:129`, signature unchanged. Green: 13 files / 118 passed, `tsc --noEmit` clean; lead's landed-tree re-verify 25 passed on the three files and 69 passed across three orders neighbours. |
| T-209 | DONE | `b3bf0037` | The guard on THIS loop's own dead-man forensics now executes them — and doing so found the forensics were dead. Red: `2 failed, 15 passed` with `no dead-man comment at all: gh calls='' stderr='... testing_weekend.sh: line 104: report: command not found'`. Mutation proofs, each GREEN under the old assertion: dropping the exclusion filter, flipping `grep -v` to `grep` (`assert 38 == 30`, nothing pruned, while the old `"launchd-cycle" in rotation` still passed), deleting the lock branch's `report` call, and a dead duplicate deadline line (old `count>=2` passed; the new positional check fails). **Real defect fixed:** `report()` / `on_crash()` / `notify_phase()` / `resolve_pr_url()` are defined BELOW the prologue in `main()`, so the REFUSED branches and the prologue ERR trap called a function that did not exist yet, and `report()` also interpolates `PHASE`/`STAMP`/`RUN_LOG`, unset there under `set -u`. **Every prologue death — held lock, wrong clone, full disk, moved clone — was silent: no issue comment, no page.** The four definitions are hoisted above the trap in both wrappers and the phase variables seeded. `test_weekend_wrapper_self_rewrite.py`'s `test_..._refused_silently` had asserted `_comments == []`, encoding the dead report AS the contract; it now asserts one REFUSED comment and no page. Green: 88 passed across four files, re-verified by the lead in the landed tree. **Note for the operator: this commit also edits `scripts/reliability_weekend.sh`,** which carries the identical defect. It is the other loop's wrapper, not its audit or log, and leaving it silently dead would be the worse call — but it is worth a look. |
| T-232 | DONE | `5e29a41a`, `b567b2d3` | All five grep-not-behaviour tests in the delta converted, each with a mutation that is GREEN under the old assertion and red under the new one: `test_run_flow_refresh_wrapper.py:194` run_one greps (`return 0` at the top of `refresh_scan` -> `raw_calls == []`); the same file's fallback-arg greps (fallback runs `"${fallback_args[0]}"` -> `'scripts/scanner.py' != 'scripts/scanner.py --top 25'`); `test_flow_refresh_shed_honesty.py:146` `"timeout" in body` (`timeout 0s` means no limit in coreutils -> *three 2s-budget fallbacks took 180.7s*); `:192` `"service_health" in source` (delete the `_flow_health "warn"` CALL, keep the helper -> no row written); `cloud/tests/test_app_plane_cutover_safety.py:155,162` (`python_image()` bypasses `resolve_image` -> the docker calls are `['rm -f ...','run ...']` with no `manifest inspect`); and `test_migration_partial_alter.py:59`'s `inspect.getsource` scan (move the commit into `_execute_ddl` -> the old scan counts 0 commits and passes, while the new ordering assertion fails). No source change. **Two cherry-pick conflicts resolved by the lead, both from two agents fixing the same file off the same base — neither resolution dropped a test.** (a) `test_flow_refresh_shed_honesty.py`: T-197's conversion is strictly richer (it adds three probe-outage tests), so it was kept, and T-232's unique contribution — asserting the row's MESSAGE, not just its state — was merged in as `_health_messages()` plus `assert "shed 3" in ...`, which required reading `error["message"]` since `_flow_health` passes a dict. (b) `cloud/tests/test_app_runtime.py`: T-198 added a `docker_body` parameter while T-232 added `RADON_TEST_MISSING_TAGS`; kept both, and **corrected the fake docker to fail `image inspect` as well as `manifest inspect` for a missing tag** — otherwise T-198's new local-store fallback resolves the missing tag and T-232's `:latest` fallback is never reached, which is exactly how `test_there_is_a_fallback_when_the_sha_tag_is_absent` failed on first landing. A test wanting the local store to answer passes its own `docker_body`. Green after resolution: 18 passed (flow pair), 49 passed / 2 skipped (cloud pair), 37 passed (migrate group), 17 passed (caddy). |
| T-238 | DONE | `e30983c9` | The startup contract has a budget that survives a busy runner. Two cases in the file `await import(...)` the workspace module graph, so the first pays Vite's transform of that whole graph inside its own 5000ms per-test budget. The audit caught it at 5041ms under load 36; **this run hard-timed-out twice in a row IN ISOLATION at load average 42** (`Test timed out in 5000ms`, 1 failed / 7 passed ×2), which is the normal condition on a host running two weekend loops at once — so it would have redded the closing gate. Green: 8 passed ×3 with that case at 6852 / 7854 / 8068 ms, the worst run under half the new budget, which is what the AC asks. `vi.setConfig({ testTimeout: 20_000, hookTimeout: 20_000 })` for this file only, the pattern T-161 established; the blanket `retry` T-161 removed is NOT restored, so a genuine hang still fails. The AC's other option (hoisting the import-graph parse to `beforeAll`) would not have helped: the parse is cheap, and the cost is the dynamic import, which a `beforeAll` only moves under `hookTimeout`. |

### Two findings turned up defects in PRODUCTION, not just in tests

- **`cloud/caddy/Caddyfile` could never have been installed** (T-205 fallout).
  `retry_match` and bare `dial_timeout` are the JSON and transport spellings,
  not `reverse_proxy` subdirectives. Both installers gate on `caddy validate`,
  so **R-219, R-220 and R-258 were never in force at the edge** while 14
  text-regex assertions shipped green. R-220 is the POST-replay guard on
  `/api/orders/place`. Verified independently by the lead against a real
  caddy v2.11.4: the pre-fix file errors `unrecognized subdirective
  dial_timeout`, the landed file logs `adapted config to JSON`.
- **Every prologue death in BOTH weekend wrappers was silent** (T-209).
  `report()` and three siblings are defined below the prologue in `main()`,
  so the REFUSED branches and the prologue ERR trap called a function that did
  not exist yet - held lock, wrong clone, full disk, moved clone all produced
  no issue comment and no page. The test that was supposed to guard this
  asserted `_comments == []`, encoding the dead report AS the contract.

### Needs an operator eye before merge

1. **`.github/workflows/ci.yml`** - a caddy-install step in the `cloud-tests`
   job, gated to the shard that collects `test_caddy_edge_timeouts.py`. It is
   the only workflow change on the branch.
2. **`cloud/caddy/Caddyfile`** - a production edge-config correction, above.
3. **`scripts/reliability_weekend.sh`** - the other loop's wrapper carries the
   identical T-209 defect and is fixed symmetrically. Not its audit or log, so
   not a frozen-contract violation, but it crosses lanes.
4. **T-192 changes CI ROUTING** with no YAML edit: root-level config files now
   route to BOTH gates (`vitest.config.ts` was `(False, True)`, is now
   `(True, True)`). Config-only pushes get slower and correct.
5. **T-222** (`main` has no `required_status_checks`) and **T-223** (14 of 150
   e2e specs run, gating nothing) are unchanged - both are operator actions
   outside this branch.

### Recorded, not remediated

- **Pre-existing cross-file pollution.** `web/tests/orders-place-cache-race.test.ts`
  reds `returns the just-placed order even though a concurrent GET cached the
  pre-fill snapshot` (`expected 0 to be greater than 0`) when run alongside
  `spread-price-bar.test.ts` and `p2-operability-remainder.test.ts`.
  Reproduced **identically at the base SHA `d746ce12`** in a clean worktree,
  and the file is 6-passed x3 in isolation. A candidate for the next audit.
- **The darwin cloud baseline now depends on `RADON_CADDY_BIN`.** Without a
  caddy binary, `test_caddy_edge_timeouts.py` is 3 failed instead of 2 skipped
  (deliberately - the skip is no longer silent). The closing gate below was run
  with `RADON_CADDY_BIN` set, so the count is the honest one.

## Remediation 2026-08-28 — PR #128

Backlog entered this run with **zero un-DONE P0/P1** (T-190…T-217 in PR #112;
T-237 fixed by merged PR #109 and re-verified 12-passed here; T-003…T-053
pre-log via PRs #13/#14). So this run works the newest **P2** stragglers, plus
the one new finding remediation could not avoid making.

| Task | Status | Commits | Evidence |
|---|---|---|---|
| T-239 | DONE (new finding, P0-class — filed by this phase) | `99dff1ec` | `claude -p` kills unfinished background tasks at its print-mode ceiling (600 s) and exits **0**, so both wrappers paged `OK` on a phase that was cut in half. Observed, not theorised: the 2026-08-28 audit was cut at 600 s, left an empty branch, no ledger line and no PR against a 24-commit / 262-file delta, and reported OK on all three dead-man channels (issue #83's last comment carries the harness message verbatim). Two guarantees, both EXECUTED rather than grepped. **Prevention** — the invocation carries `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS=0`; the test spawns a stub agent and reads the value back out of the CHILD, because an assignment that never reaches the child satisfies any source grep. Red: child sees `'<unset>'` in BOTH wrappers. **Honesty** — `phase_status` classifies rc + run log, and a log carrying the harness message is `TRUNCATED`, not `OK`; the other three classifications (OK / TIMEOUT after 7200s / FAILED (exit 9)) are pinned unchanged so this cannot become a blanket downgrade, and a truncated run that ALSO timed out stays a TIMEOUT. Red: `phase_status` did not exist. Green: 21 passed. Symmetric in `scripts/reliability_weekend.sh` per the T-209 precedent; neither loop's audit or log touched. |
| T-230 | DONE | `94bf561f` | `/gex/scan` takes any ≤10-char alnum ticker and mints a per-subject gate keyed on it, unbounded, in a process that lives for days — and each novel value arrives COLD, so it also bypasses the cooldown and spawns another 120 s `run_script`. Red: `500 caller-supplied tickers minted 500 gates … assert 500 < 500`, plus five tests erroring on a `MAX_SUBJECT_SCAN_GATES` that did not exist. Green: 16 passed (was 10) ×3; adjacent gex / refresh / health files 164 passed. **Deviation from the AC, deliberate:** "eviction never drops a gate in cooldown or backoff" is not implementable as pure LRU — a bogus ticker arms a 60 s backoff, so an attacker can make every gate armed and the map still grows; and re-minting an evicted armed gate returns it COLD, bypassing the backoff just dropped, which is R-217 re-entering through the eviction path. Eviction is fail-CLOSED instead: only IDLE gates are evictable, and a saturated host refuses a novel subject on a shared overflow gate. Mutating the predicate to fail-open reds exactly the four armed-gate tests, so that half is not asserting the constant back. Audit line numbers were stale (3105-3120 not 3069-3077; validation at 3995 not 3964); all names correct. |
| T-227 | DONE | `2cacb8a6` | The fixture anchored on `bpi.last_completed_session_date()` at BUILD time while `build_index_payload` calls it again at ASSERT time to compute `stale`; `market_calendar` flips that value at exactly 16:00 ET. Reproduced deterministically with an external plugin returning `2026-08-26` then `2026-08-27` — no wall clock involved: `assert True is False` at `:93`. Flip confirmed against the real calendar (15:59 ET → `2026-08-26`, 16:00 ET → `2026-08-27`). Fixed by an autouse fixture pinning the anchor, with four tests asserting the flip is real, that the old two-call anchoring diverges, and that the pinned form is immune. Green: 14 passed (was 6) ×3; blast radius 45 passed. |
| T-231 | DONE | `2cacb8a6` | (a) `install_sigterm_unwind()`'s CALL SITE was unpinned — the test called it directly, so deleting `bpi_scan.py:656` left the suite **6 passed** while a systemd `Result=timeout` again skipped `service_cycle`'s `finally` (partial `bpi_history`, no error row, `NRestarts=0`, silent). The new test spies `signal.signal` and drives `run_scan` itself; the same deletion now reds with `<Signals.SIGTERM: 15>` missing from the installed map. (b) `PERSIST_RESERVE_S = 600` was read by nothing but the test's own arithmetic; the persist loop is now gated on `time.monotonic() < deadline + PERSIST_RESERVE_S`. Removing the gate reds two tests, one of which holds clock and deadline fixed and flips only the constant (600 → skip, 5000 → persist), so it cannot pass by reading the value back. **Recorded, not fixed:** the same double-resolution is a PRODUCTION race — `ensure_member_history` filters bars against one resolution (`:247`) and `build_index_payload` computes `stale` against another (`:211`), so a real run straddling 16:00 ET persists a spurious stale flag on a complete sweep. |
| T-219 | DONE | `b91edcce` | The guard check was `grep -c "riskFreeRate == null" >= 5` over the source, with nothing rendering `PositionTable`. Both failure modes the finding names, proved against the real component: (i) a SIXTH call site with no null guard — grep count still 5, OLD test **6 passed** (that is the finding), NEW test `expected [ '$0.81', '—', … ] to deeply equal [ '—', '—', … ]`; (ii) a guard returning `0` instead of `"—"` — grep count still 5, OLD test **6 passed**, NEW test `expected [ '$0.00', … ]`. Both mutations reverted. The replacement renders a 2-leg TSLA ratio RR with both Implied columns forced visible, locates them by header index and collects EVERY tbody cell under them: fallback payload → all six read `"—"`; `seedRiskFreeRateForTests(0.0433)` → the cells must equal the `bsCall`/`bsPut` values at r=0.0433 AND must differ from the r=0 values, so a hardcoded zero cannot pass either. The four `useRiskFreeRateState` hook tests are byte-identical — the audit calls them strong and only the grep was replaced. Green: 10 passed across both files; `tsc --noEmit` clean. No product code changed. |

## Remediation 2026-08-29 — PR #140

Backlog entered this run with **62 new findings (T-250…T-311; 5 P0, 30 P1,
27 P2)** filed by the same cycle's audit phase. This phase works the P0s
first, then the P1s. Pre-flight: `origin/testing/2026-08-29` and PR #140
already existed (the audit phase), `rtk` is NOT installed on this host so bare
`git` is correct, `node` needed `~/.nvm/versions/node/v24.14.0/bin` prepended,
and every gate ran from `~/radon-weekend/venv/bin/python3.13`. Open PRs at
pre-flight were #139 (the sibling reliability loop's audit), #126 and #125;
none had already fixed a P0 on this list. Scratch namespaced to
`/tmp/tw-2026-08-29/`. Fan-out was 6 worktree agents, capped per the
2026-08-26 rail, with the lead cherry-picking serially and pushing after every
landing.

| Task | Status | Commits | Evidence |
|---|---|---|---|
| T-250 | DONE (P0) | `dbc36b04` | `claim_flex_delivery` read `rows_affected` — the JS driver's name (`writer.js:280`). `libsql_experimental`'s `builtins.Cursor` exposes `rowcount` only, so `getattr(..., 0)` made the claim `False` on FIRST sight and EVERY delivery took the `"duplicate"` branch: `cash_flow_sync`, `perf_twr_builder.build_and_persist` and `journal_rehydrate.rehydrate` were never invoked for any Flex statement while `flex_sftp_pull` saw `ok=True`, incremented `ingested`, heartbeat `"ok"` and exited 0. The claim row IS inserted, so it was unrecoverable without a manual `DELETE`. Second defect on the same lines: the only INSERT of 68 in `writer.py` with no `db.commit()`. No test could catch it — all four existing references monkeypatch the indirection at `flex_delivery_ingest.py:34` and `db.writer.claim_flex_delivery` had ZERO test callers. New `test_flex_delivery_claim_writer.py` binds a REAL libsql DB over `db.client.get_db` with migration 0059 applied. **Lead re-derived the driver behaviour before filing work** (`hasattr(cur,'rows_affected') == False`, `rowcount` 1 then 0) and mutation-checked both defects independently in the LANDED tree: restoring `rows_affected` → 6 of 8 red; dropping only the commit → `test_the_claim_row_survives_a_fresh_connection` red. Green: 16 passed across the three flex-claim files, 625 passed across the flex/delivery/writer/TWR/rehydrate/cash-flow neighbourhood. **Goes live with the first IBKR sFTP drop 2026-08-31.** |
| T-254 | DONE (P0) | `4d2d1f26` | `deploy.sh:1476` printed `"Previous release ${prev_commit} passed the deploy gate"` without checking. Observed live: CI run `33239774951` at `f7b5eeb9` had all 27 test jobs green, `Deploy to VPS` failed, and the rollback landed on `e4bc7171` — whose own run `33197706791` concluded `failure` at `pytest (cloud mz)` with the deploy skipped. Production is serving a SHA the gate rejected while the log asserts the opposite; the release gate is enforced on the forward path only. Red: `assert "passed the deploy gate" not in output` → `'passed the deploy gate' is contained here: eee… passed the deploy gate.` Fix reads the existing on-host green marker (written only in the success arm, so a SHA that never deployed can never own one) and reports UNVERIFIED instead of reassurance. **Deliberately a local read, not `gh run list`:** the rollback runs when production is already broken and a network/auth failure there must never strand it; the marker is a proxy, so a rotated marker warns conservatively on a good SHA — the safe direction. Green: 128 passed / 3 skipped in `test_deploy_corrections.py`, 298 passed / 4 skipped across every suite that reads `deploy.sh`, `bash -n` clean. **Operator: touches `cloud/scripts/deploy.sh`**, materialized per-release into `~/.radon-deploy-runners/<sha>/` and NOT in `bootstrap-control-plane.sh`'s `SOURCES`, so no root install-copy expected — confirm before merge. |
| T-255 | DONE | `5bd4eb25` | `sftp ls` is columnised by default and the parser took `line.split()[-1]`, so a three-file IBKR drop printed on one line pulled only the LAST file while `run()` still reported ok — a partial delivery indistinguishable from a complete one. The fake emitted one name per line, exactly the shape the parser assumed. Red: `assert ['c.gpg'] == ['a.gpg','b.gpg','c.gpg']` and `assert 1 == 3` files pulled. Fix: `ls -1`, plus a parser that reads every token so a server ignoring `-1` still yields them all. The existing `== "ls"` assertion was TIGHTENED to `== "ls -1"`, not loosened. |
| T-256 | DONE | `5bd4eb25` | `has_trade` was a ROW check sitting directly under the SECTION check `has_transfer` — which had already been relaxed for exactly this reason, with the comment three lines up explaining why. A session where the account traded nothing ships `<Trades></Trades>`, so the trade-confirm file classified ambiguous, the heartbeat went `error` and flex-pull went red in the watchdog daily bucket on a completely healthy day. Red: `FlexClassifyError ambiguous_or_incomplete:nav=0 cash=0 transfer=0 trade=0`, and `run()` exit `assert 1 == 0`. |
| T-257 | DONE | `dbc36b04` | The claim is taken BEFORE any writer and a failed ingest never handed it back, so the operator's re-drop of a fixed file returned `"duplicate"` — green, exit 0 — and half-written `cash_flows` were unrepairable without a manual DELETE. Adds `release_flex_delivery` (purely additive) and releases on all three failure branches. Collateral fixed, not weakened: the atomicity file's autouse fixture stubbed only the claim, so the new release hit the real `get_db()` pytest guard; the fixture now records releases and the half-written-cash-flows test gained an assertion that the failed run handed its claim back. **Same defect the sibling reliability loop filed as R-379 on PR #139** — expect a textual conflict; resolve by keeping both. |
| T-258 | DONE | `dbc36b04` | The throttle-critical "exactly ONE Flex request per run" property was asserted in a COMMENT. `resolve_flows` now prefers the already-fetched document's `query_id` over a divergent `IB_FLEX_FLOWS_QUERY_ID`, naming the ignored id on stderr — a second id doubles the request rate against a token already carrying a 24h-to-168h embargo. Red: `assert 2 == 1 where 2 = len(['1442520','9999999'])`. Mutation check on the already-correct half: forcing `already_attempted = False` reds 2 of 3; reverted. |
| T-259 | DONE | `5bd4eb25` | The production sFTP→ingest wiring was never driven end to end. `_default_ingest` discarded `source_path` and wrote to a random `/tmp` name, so `flex_deliveries.source_path` recorded provenance that never existed; `retain_newest_gpg(keep=0)` kept everything because `keep >= 0` selected `files[:-0]` (empty). Red: `assert '/var/folders/.../tmp*.xml' == '.../inbox/trades.xml'`, and `assert [3 paths] == []`. **Lead correction on landing:** the agent's rewrite used `Path.write_text` then `chmod 0600`, leaving DECRYPTED brokerage-statement plaintext world-readable for the width of the ingest. Replaced with `os.open(..., O_CREAT\|O_WRONLY\|O_TRUNC, 0o600)` before the first byte; verified under `umask 022` that the file is `0o600` at ingest time and removed afterwards. Green: 26 passed in the two touched files (was 6 failed / 20 passed), 418 passed across the neighbourhood. |
| T-274 | DONE | `af3a959d` | Two commits this week moved `radon-monitor.service` and `radon-newsfeed.service` onto `probes.DEPENDENCY_UNITS` and mapped a dependency-only `starting` to `degraded`, and `external_probe` grants the off-box P1 only to a validated DOWN verdict — so nothing off-box pages for them any more. The justification was a code comment ("these units already have their own on-box alarms") and `radon-monitor` is the fill/order/journal daemon, a money path. New `test_sidecar_paging_composition.py` owns the COMPOSITION and is driven off `probes.DEPENDENCY_UNITS` itself, so the frozenset cannot grow without on-box cover: down + starting on the edge side, failed + auto-restart flap + start-limit-hit on the watchdog side. Red: suppressing sidecars in `_flap_alert` (the plausible "stop the noise" edit) reds 2 new cases **while the pre-existing 113 stay green** — the gap exactly as filed; growing `DEPENDENCY_UNITS` by one reds the pinned-set case, again with the 113 green. Green: 129 passed across all three files. |
| T-275 | DONE | `c82ef940` | `test_flow_report_capacity_shed.py` POSTs `/flow-analysis/JOBY` three times without redirecting `server._FLOW_REPORTS_DIR`, so a gate run left `?? data/flow_reports/JOBY.json` in the working tree — the precondition that aborted a weekend run at pytest COLLECTION (2026-08-16), the drift guard `deploy.sh` enforces, and a stub cache entry then served for a live ticker by both the FastAPI GET handler and `web/app/api/flow-analysis/[ticker]/route.ts`. Reproduced first: `3 passed` + `?? data/flow_reports/`. Fix is an autouse fixture in the subtree conftest (the same patch `test_api_flow_cache.py` already applies per-case) plus a contract test that drives the cache-writing route for real and asserts the repo `data/` tree is byte-identical across it — including a positive assertion that the write DID land in tmp, so the contract cannot pass vacuously if the cache write is ever removed. Red without the fixture: the contract test fails on the real-path assertion while the polluter stays `3 passed`. Green: 745 passed in `scripts/api/tests`, tree clean. |
| T-251 | DONE (P0) | `d7914abd` | CLAUDE.md names this file as the REFERENCE exemplar for "a gated action is tested at the wire, not at the button", and it asserted `.some(([input]) => String(input).includes("/api/orders/place"))` — accepting a wrong action, an inverted `limitPrice` sign, a wrong leg ratio and, being `.some` rather than a count, a double-send; the closed half stopped at `disabled === true`, i.e. at the button. The file also never reached the combo branch: the fixture was single-leg. Rewritten to the `admin-action-request-assertions.test.tsx:164-167` shape with a two-short-call fixture whose legs have unequal sizes, so quantity, signed limitPrice and `legs[].ratio` are all load-bearing; paired `toHaveLength(0)` driven through `reactOnClick()` past `disabled`. RED, re-derived by the LEAD in the landed tree rather than taken from the agent report: negating `signedLimitPrice` at `OptionsChainTab.tsx:531` leaves the old file 6 passed and reds the new one `expected 30.5 to be -30.5`. |
| T-252 | DONE (P0) | `d7914abd` | (delta to T-211) The mobile gate's ARMED half asserted a count over a URL substring. All four named mutations left it at 6 passed: flipping `action` (a long call transmits as a NAKED SHORT CALL), `quantity: 1` against the 10-lot fixture, dropping `...ibPlaceFields(...)` so a STP goes as a bare market order, and switching the method to GET. Each now reds. Fixture raised to 10 lots so a hardcoded quantity cannot pass. |
| T-253 | DONE (P0) | `02de92c9` | `basis_source: "mixed"` was minted for exactly the R-374 case and NOTHING consumed it (`grep -rn basis_source web` → 0 hits), so a part-rolled vertical still published a blended `entry_cost` and `max_risk` — the number **Gate 3 sizes the 2.5% bankroll cap off**. RED both halves: python `assert 1000.0 is None` on `entry_cost` and `max_risk` from a $5,000 lagged-avgCost leg minus a $4,000 session-VWAP leg; web `expected '$1,000' to be '---'`. Mutation: `_position_basis_source` returning `"session_fills"` reds both halves, while the paired `session_fills` controls still publish — so the guard is keyed on `mixed`, not on "any multi-leg". **Lead follow-up landed with it:** withholding the aggregate makes `total_deployed_dollars` an UNDER-statement and `remaining_capacity_pct` an OVER-statement — the unsafe direction for the cap, and silent. Payload now carries `unmeasured_basis_count` with two tests pinning the measured and omitted cases. Every downstream reader of the now-nullable `entry_cost` audited and handled. GREEN: 20 / 850 / 8, `tsc --noEmit` clean. **Known gap, filed:** `resolveEntryCost` still returns a number, so the close-ticket paths and portfolio unrealized totals still blend; widening it touches 19 sites beyond this AC. |
| T-265 | DONE | `88ccd99a` | The IB connect-budget assertion was algebraically incapable of failing — it recomputed `CONNECT_TIMEOUT_S`'s own definition, reducing to `40.0 <= 45.0` by construction. Old test stayed `15 passed` under BOTH `_EQUITY_OPTIONS_CHAIN_TIMEOUT_S` 45.0 → 20.0 (R-352's exact failure mode) and `CONNECT_ATTEMPTS` 2 → 12. The rewrite imports the cap and measures the schedule `main()`/`_connect_with_retry` actually spends; the cap mutation reds 3, the attempts mutation reds 2. |
| T-266 | DONE | `d7914abd` | The mobile transmit guard was verified by GREPPING the component source, and the grep block stayed green under all three defects it claims to guard — including the literal 2026-08-27 production bug (a `useCallback` dep array omitting the acknowledgement, which shipped an armed Transmit button that silently sent no order). Replaced with behaviour on both tickets: fire the transmit onClick twice inside one `act()` and assert exactly one request, plus a rejected-order-resend case pinning the `finally` release. Stale closure reds 2 mobile / 3 desktop. |
| T-267 | DONE | `88ccd99a` | Market-value fidelity was asserted by source grep; old test `6 passed` both while the rendered value recomputed the figure and while the multiplier was applied twice. Now renders `PositionTab` and compares the on-screen value: `expected '$424,875' to be '$412,500'`, `expected '$300,000' to be '$3,000'`. A new all-option vertical fixture was required — the covered-call fixture has multiplier 1 and structurally cannot see the double-apply. |
| T-268 | DONE | `88ccd99a` | Three source greps plus a verbatim hand-copy of the loop under test. Loop extracted to `truncateCtaTables()` and driven for real; `DEFAULT_LOOKBACK_DAYS` rendered rather than grepped. Old test `8 passed` under all three mutations. **AC correction:** the AC's suggested mutation reds the OLD test too (it greps that exact token), so three grep-evading mutations were used instead. |
| T-269 | DONE | `88ccd99a` | A JSX prop's PRESENCE grepped out of component source; old test `6 passed` with `showUnavailable={false}` and with the banner hidden behind a cluster-count conditional. **AC correction, recorded:** the AC as written is not achievable — `CorrelationRiskBanner` consults `showUnavailable` only on the `report == null` branch, so `NOTHING_MEASURED` cannot red on that prop. Both reachable cases covered instead. |
| T-270 | DONE | `88ccd99a` | The outside-RTH modify test asserted a property of the fixture it built two lines earlier; old tests `17 passed` with `ModifyOrderModal.tsx:609` deleted entirely. Four wire assertions on `onConfirm` replace it. Test-infra note kept in the file: the modal needs an explicit `portfolio` prop or `useOrderRisk` stays `pending`, `okToSubmit` is false and Modify Order is permanently disabled — any wire test of this modal that omits it can never reach `submitModify`. |
| T-271 | DONE | `b9e71995` | All five e2e specs added in this delta were in neither the curated CI list nor any hold-out record. Each pre-flighted under a PRODUCTION server per the 2026-08-17 rule (`PLAYWRIGHT_WEBSERVER_CMD="npx next start"`), 3/3 green: 8 passed in 54.9s / 45.4s / 24.0s. All five are `page.route`-mocked — no live IB, network or DB. Curated list 14 → 19. `web/e2e/ci-curation-ledger.txt` freezes the remaining 136 and separates the two reasoned hold-outs from the untriaged backlog; the contract test fails if a spec is in neither. **Operator: the job is still non-gating** — it appears in neither `stage-release.needs` nor `deploy.needs`, so all 19 still cannot block a release. Same class as T-222. |
| T-272 | DONE | `9f017185` | Two tests burned 240 REAL 8 ms sleeps each — measured at 3935 ms and 4894 ms against vitest's 5000 ms default, 106 ms of margin on the shared gate. `drainStream()` drives the same chunks through `vi.advanceTimersByTimeAsync`; assertions unchanged, and dropping the remainder write still reds. Worst case now 192 ms. |
| T-273 | DONE | `9f017185` | A live-clock minute-boundary race in the IV-rank header, made deterministic rather than hunted: pinning `18:59:59.900Z` and moving the clock to `19:00:00.100Z` across the render gives `expected 'SPY 1M IV Rank·2:59 PM' to contain '3:00 PM'`. Fixture `scan_time` and `FROZEN_CLOCK` now derive from one instant; `toFake: ["Date"]` only, so d3's rAF/ResizeObserver path is untouched. |
| T-276 | DONE | `b9e71995` | Twenty-seven vitest tests ran NOWHERE: three files were excluded wholesale for spawning `python3.13`, but only 9 of the 27 tests spawn anything — the other 18 include the 2026-05-22 bare-`python3.13` outage regression in `resolvePythonBin`, which had never once run in CI. Excludes removed; the 9 skip BY NAME via `hasPython313()` so the reason is in the run output, and `test_vitest_python_gating.py` reds if an exclude returns. Verified in the landed tree under BOTH conditions: with python3.13 `27 passed`; with it stripped from PATH (the CI condition) `17 passed | 10 skipped`, exit 0. One test fixed not weakened: `returns ok: false when script exits non-zero` also passed on a spawn ENOENT (`exitCode: null`), so it never proved the non-zero-EXIT path it names; it now requires a numeric non-zero `exitCode`. |
| T-277 | DONE | `d921debf` | `flex_embargo` lost its pytest escape hatch, so two live tests pinned OPPOSITE `is_blocked()` contracts and ambient env decided which passed. LEAD-reproduced: the pre-fix file is `15 passed` bare and `1 failed, 14 passed` with `TURSO_DB_URL` set — green here only because this clone has no `.env`, while production always has those vars, so the test pinned the opposite of production. Split into two tests that each STATE their store configuration. Now `16 passed` both with and without the env, which is the property that matters. |
| T-278 | DONE | `d921debf` | `POST /api/blotter` is a 404 and the e2e was dead-red. **The TEST was wrong, the route is correct** — `26668ef8` ships GET-only as intent and adds `test_post_blotter_is_404` itself, `use-blotter-refresh.test.ts:40` already asserts "never POSTs", `useBlotter.ts:18` sets `hasPost: false`. Spec rewritten GET-only and now also asserts `blotter.posts` is empty, with a POST answered by the production 404 rather than mocked away. Mutation: `hasPost: true` reds it. |
| T-279 | DONE | `d921debf` | `resolvePreviousSessionClose`'s 7-day gap killed `DayChange` in an untouched layout spec — a 30 s timeout plus two off-by-one-row failures. History now generated backwards from `lastCompletedSessionDate()`, the resolver the app uses, so the gap is 0 on any run date. Second stale side found: the spec also pinned the pre-`d3acdd57` cell order, left behind by a deliberate 2026-07-17 reorder. |
| T-280 | DONE | `d921debf` | A hand-copied replica of the code under test, so it agreed with any bug and could not have caught the defect it is NAMED after — proved directly: with `regimeLiveStrip.ts` reverted to `bc2649f7^`, the replica still returned `12 passed`. Rewritten under jsdom importing the real `resolveRegimeStripLiveState` and the exported `DayChange`/`PointChange`, asserting the rendered `"+0.24 (+1.65%)"` and explicitly NOT the shipped `"-1.89 (-11.35%)"`. At `bc2649f7^` it is `5 failed | 7 passed`. |
| T-281 | DONE | `b6c74640` | `702ae26a` shipped five REAL `*.service.d/runtime-container.conf` drop-ins and taught `bootstrap-control-plane.sh` to install them, but the guard globbed `.example` only — so it inspected DOCUMENTATION and never opened a file that reaches the host, while its failure message talks about an ExecStartPre that "runs as root, against production Turso". Glob now covers both forms (6 params → 11), an installed file is never eligible for `DROP_IN_SKIP_BASELINE`, and a new floor tests the executed count against the installed count. RED: deleting `ExecStartPre=` from `radon-relay.service.d/runtime-container.conf` leaves the PRE-fix suite fully green at `23 passed, 2 skipped` and reds the fixed one. |
| T-282 | DONE | `bb734389` | Every production caller of `resolve_flows` passes `allow_fetch=False`, so the 2026-08-17 incident's whole Turso-mirror contract was verified exclusively through `allow_fetch=True` — a branch production never enters. Class parametrized across both; the `allow_fetch=False` leg reaches the fallback via `_flows_after_fetch_failure("file_ingest_no_fetch")`. Two missing healthy-path cases added. RED: neutering that fallback to `FlowSet.failed` leaves the ORIGINAL file fully green and reds 4 new cases. |
| T-258 | **CORRECTED** | `bb734389` | The finding states "no test references `resolve_flows`, `_flows_query_id` or `IB_FLEX_FLOWS_QUERY_ID`". **That is wrong** — the ROOT `tests/` tree, a second collection root the finding did not search, has `tests/test_perf_twr_flex_single_request.py`, which references all three and whose `test_a_distinct_flows_query_id_is_still_fetched` deliberately pins the OPPOSITE contract. The T-258 source change made a divergent `IB_FLEX_FLOWS_QUERY_ID` a silent no-op, which reddened that test (invisible to the task agent, which ran only `scripts/tests`) and converted CLAUDE.md's guidance not to SET the knob into code that IGNORES it. That is a throttle-facing product decision, not a test fix, and no test failed against a real defect to justify it — **so the source change was reverted**. The undisputed property the finding correctly identified as untested is KEPT and mutation-checked (`already_attempted = False` reds it and the older file's equivalent). The divergent-id case is now pinned as-is in both files, each naming the other. |
| T-283 | DONE | `9f017185` | **A production defect, not just a slow test.** The leap wrapper set `SHED_DEADLINE=$((SECONDS + SHED_WAIT_SECS))` before the first POST, so process startup and the first curl round trip were charged to the wait budget — on a loaded box the whole budget could be gone before the first retry decision and the wrapper gave up with ZERO retries, the silent no-retry the 2026-08-27 page was about. The ladder now counts the seconds it actually waits. RED is deterministic (a curl shim sleeping 4 s against a 3 s budget → `assert 1 >= 2`), not load-hunted, which also removes these two files from the load-flake population that redded this cycle's own audit gate. Retry counts and sleep sequences are now asserted EXACTLY rather than load-dependently — a strengthening. |
| T-311 | DONE | `9f017185` | The intra-file `vi.waitFor` race, reproduced first: 3 of 6 runs red in isolation with no other file in the run, always the same case, always `expected 0 to be greater than 0`. `refreshGate()` splits the stand-in so `entered` is resolved BY the mock when the route calls it, while `landed` stays the deferred the test controls. Discriminator: under a 1500 ms delayed `/orders/place` the polled form is 1 failed | 5 passed and the gated form is 6 passed. Isolation after: 8/8. **Lead follow-up before the closing gate:** with the race gone the file still hard-timed out in 2 of 9 runs under load — the T-238 class, not the race. Measured 995/1035/1225/1294/2548 ms across green runs at load 53-98, under 2x margin against the 5000 ms default, because each case drives a real route handler through a held-open refresh. Raised via `vi.setConfig` per the T-161 precedent with the measured numbers in the comment, and registered in `SLOW_JSDOM_FILES` so `vitest-config-contract.test.ts` enforces it. Assertions unchanged, no `retry`. 5/5 green after. |
| T-260 | DONE | `67041685` | Three of the four `summaryFiguresAreFinite` gates on the order-submit path were unasserted — only the option-open site was covered. Code is correct, so RED is by mutation, one gate at a time: `okToSubmit: true` at `useOrderRisk.ts:741` reds `linear close-out: refuses to arm on a non-finite limit price` (`expected true to be false`); dropping the conjunct at `:771` reds only `linear open`; `&& true` at `:834` reds only `option close-out`. Test-only. GREEN: 13 passed, `tsc --noEmit` clean. |
| T-261 | DONE | `67041685` | `run_offbox`'s "NEVER raises" contract, and its effect on the caller, were unasserted. RED by deleting the `try/except`: 5 failed, with captured stderr showing the shipped lie verbatim — `backup failed: RuntimeError: b2 wedged` for a dump that had LANDED. Test-only. GREEN: 37 → 74 passed across the three db_backup suites. |
| T-262 | DONE | `67041685` | **A real product defect.** An EMPTY dump was promoted, pruned the 30-day window of real dumps against, uploaded off-box, and reported success — `dump_database` reports whatever `sqlite_master` gave it, so an empty read (a credential rotation pointing at a fresh DB, a libsql read returning no rows) yields a valid ~120-byte gzip. RED drove the REAL prune/upload sequence rather than mocking it, which reproduced the loss directly: `pruned: 2 / survivors: [the 168-byte empty dump] / uploaded: [...] / dumped 0 tables / 0 rows`, with `service_health row written: db-backup = ok`. `assert_plausible_dump` runs while the dump is still `.tmp`, so a failure never promotes, never prunes, never uploads, and leaves nothing on disk for a LATER run's B2 backfill to push; `main` converts the raise to an `error` heartbeat and rc 1. Floor is deliberately 1 table / 1 row — catches the observed 0/0 without risking a false refusal on a legitimately small DB. ⚠️ touches `cloud/scripts/db_backup.py`. |
| T-263 | DONE | `acebbae5` | **A real product defect, shipping.** R-333 built the freshness defence on the 304 branch only — the one branch that CANNOT carry new data. The rebuild branch emitted no verdict at all: no `status`, no `lag_days`, no `expected_session`, and a bare `ok` heartbeat. Cboe re-touches `Last-Modified` intraday WITHOUT appending the session row, so a 200 that rebuilds an unmoved series is exactly as stale as a 304 that reuses one, and `ensure_plausible_series` never checks recency — a two-week-old session rendered a confident regime badge inside the 48h `scan_time` budget. `fetch_vixcor.py` already applied the verdict on both branches; `fetch_vixts.py` was the odd one out. RED at HEAD: `assert None == '2026-08-20'`, `assert None == 'ok'`, "rebuild branch is missing {'status','expected_session','lag_days'}". |
| T-264 | DONE | `acebbae5` | "A rejected value writes no row" was never asserted, and the test double made it UNASSERTABLE: `_Recorder.upsert_vixts_rows` was `pass`, so nothing could observe a write. It now records, `_write_json_cache` is spied, and the new test drives the REAL `ensure_plausible_series` with VIX 1000 / VIX3M 20 (ratio 50), asserting 0 row upserts, 0 snapshots, 0 JSON writes, `data/vixts.json` byte-identical, and exactly one `error` heartbeat — paired with a plausible-pull case asserting all three writes DO happen, so the zero counts are not vacuous. RED via the exact reorder the audit names (moving `ensure_plausible_series` after `_write_db` + `_write_json_cache`): `a rejected value must not reach vixts_history; 1 upsert_vixts_rows call(s) landed`. |

## Remediation 2026-08-30 — remediation branch

Backlog entered this run with **34 new findings (T-312…T-345; 1 P0, 14 P1,
19 P2)** filed by the same cycle's audit phase (PR #189, merged 13:10Z, so
`origin/testing/2026-08-30` no longer existed at pre-flight and this phase
branched `testing/2026-08-30-remediate` from `origin/main` `6d10f1e1`). This
phase works the P0 first, then the P1s; the P2s are DEFERRED. Pre-flight:
Sunday, load average 4.5, `rtk` IS installed here so every git call is
`rtk proxy git`, `node` v24.14.0 via `~/.nvm`, `pytest-asyncio` +
`pytest-xdist` present in `~/radon-weekend/venv`. Open PRs at pre-flight
(#197, #176, #126, #125) — none matches a finding subject. Scratch namespaced
to `/tmp/tw-2026-08-30-rem/`.

| Task | Status | Commits | Evidence |
|---|---|---|---|
| T-313 | DONE | `c224915b` | **A real product defect.** A `check_order_limits` refusal in `ExitOrdersHandler.execute()` incremented `orders_failed` and `continue`d without setting `result["error"]`, so the exit-orders cycle heartbeat `ok` while the position sat unprotected and the leg was retried every 5 min. RED `2 failed, 12 passed` (`result['error']` unset; heartbeat `ok` != `error`); GREEN `14 passed` ×3 (0.57 s). Both funnels now driven end to end with stub clients (`execute()` / `ib_order_manage` with `output()` silenced). Mutations: delete `if violation:` → 2 failed; delete `continue` → 2 failed; `violation = None and …` → 2 failed; delete `ib_order_manage` `return` → 1 failed. Scoped sweep of 22 files referencing either module: `331 passed`. |
| T-318 | DONE | `dd517103` | `test_flex_sftp_pull.py`'s duplicate re-pull ran on the LIVE ET clock and would have gone deterministically red on the CI-gated path from Tuesday 2026-09-01 (R-389 makes a duplicate-only re-pull an error after the cutover). RED `1 failed, 16 passed` with `now=2026-09-01 08:00 ET` on both runs (`:432 assert 1 == 0`), also reproduced with `flex_sftp_pull.datetime.now` frozen. GREEN `61 passed` ×3 across the four files; frozen at 2026-09-01, 2026-08-28 and 2026-12-01 → `61 passed` each. Every `pull.run(` in `scripts/tests` + `tests/` now carries `now=` (one `AFTER_FIRST_DELIVERY` constant, paren-matched scan: zero unpinned). Second run asserts the R-389 contract (`code_again == 1`, last heartbeat `error`). Mutation (count duplicates as ingested) → `2 failed`. Source untouched. |
| T-314 | DONE | `e49c7ab6` | **Three real product defects, not one.** (a) The R-423 lock lived in `/tmp`, a different inode on the host (`radon-refresh`) and inside the app container (`/regime/scan`), so the double-scan was still open; moved to `data/radon-cri-scan.lock`, the one bind both sides share (`RADON_CRI_SCAN_LOCK` still overrides; gitignored). (b) `cri_scan.py` never imported `os`, so `_acquire_scan_lock` (called unconditionally at `main():1695`) raised `NameError` on every run since `8d791c4a` — lead confirmed no `import os` in the landed tree. (c) The lock-loser branch called `cached_scan_if_fresh(force=True)`, which is 'never serve', so a loser could never print cache and always exited 75. RED `4 failed, 9 passed` (`NameError: name 'os'`; no `scan_lock_path`); GREEN `13 passed` ×3 (0.4 s); 14 files referencing `cri_scan` → `229 passed`. Three source greps (`fcntl`/`LOCK_NB`/branch text) replaced by behaviour: a test-held flock makes `main() --json` print the cached payload with NO IB client, or exit 75 without cache, under a bounded join; default lock path resolves under the `data` bind parsed from `radon-app-runtime.sh`; `_scan_gate_for` at `MAX_SUBJECT_SCAN_GATES` fires the saturation writer once. Mutations: `except OSError: pass` → 2 failed; drop `LOCK_NB` → 2 failed (blocked); lock back at `/tmp` → 1 failed; `if False:` → 1 failed. |
| T-325 | DONE | `da1c0b39` | `tracked_files` walked `os.walk(ROOT)` skipping only `.git`, so the secret scanner redded on the constant-folded `.pyc` of `test_next_clerk_guard.py` (a gitignored artifact) while never asserting the scanned set IS the tracked set. RED `1 failed` on the AC invocation naming `__pycache__/test_next_clerk_guard.cpython-313-pytest-9.1.1.pyc`. Now `git ls-files -z` (219 files = fallback pruned walk count), with companions: the clerk guard IS scanned, no `__pycache__` path is, and a runtime-built `pk_live_`+20-alnum planted in a tmp file reds the scanner. GREEN `1 passed` on the AC invocation; `34 passed` ×3 (0.35 s). Mutations: plant the key in tracked `cloud/README.md` → `1 failed` naming it; delete the `pk_live_` pattern → `1 failed`. **Moves the darwin cloud baseline 35 → 34.** |
| T-320 | DONE | `660a0daa` | The component that OWNS the `/api/orders/modify` fetch was only source-grepped (`/outsideRth/` matched the destructure). New `modify-order-outside-rth-wire.test.tsx` renders `OrderActionsProvider` via `renderHook`, stubs `fetch`, and deep-equals the payload: `{ orderId: 2, permId: 1002, outsideRth: false }` on an EXT un-tick, no `outsideRth` key on a price-only change, full URL + `POST`. Source correct today, so RED by mutation: omit the key → `2 failed` (grep test stayed green — confirms the finding); `outsideRth ?? false` → `1 failed` on the price-only EXT case. GREEN `3 passed` ×3 (~0.5 s); 30 files referencing `OrderActionsContext` → `152 passed`. Grep block at `modify-order-ticker-detail.test.ts:95-114` deleted. |
| T-323 | DONE | `8ae22e2e` | REL-134's confinement regex demanded whitespace/EOL after `/var/lib/radon`, so `:rw`, `:z` and `--mount dst=` all escaped it. RED: `-v "${STATE_DIR}:/var/lib/radon:rw"` mutant → `2 failed, 14 passed` (old assertions `4 passed` on the same mutant); `--mount type=bind,…,dst=/var/lib/radon` mutant → `2 failed` (old: `4 passed`). Now parses the fake docker's `run` argv (`shlex`), collects every `-v`/`--volume`/`--mount` destination in all six spellings, and asserts nothing equals or parents `/var/lib/radon` and only `media`/`ib-lease` sit under it; 12-case parser table pins the spellings. GREEN `16 passed` ×3 (~4.8 s); `test_app_runtime.py` `40 passed`. |
| T-324 | DONE | `03308274` | R-380 was asserted by token ORDER over `restart_services`' text. RED: `return 1` before a dead `start_services_after_transition` at `deploy.sh:1311-1312` → `1 failed, 2 passed` on the new behavioural test (regex smoke still passed — confirms the finding); `refresh_control_plane || return 1` → both fail. Harness sources `deploy.sh` (`bash` 5.3 via Homebrew), stubs 8 collaborators with a call-order log, runs `restart_services` under `set -e`; asserts rc 1, the app tier restarted, exact order, `install_release_units` not called, plus a green-path control. GREEN `20 passed` ×3 (~13 s). Regex kept as structural smoke only. |
| T-319 | DONE | `0a6450c3` | The R-405 'ET, not process-local' tests ran under the suite's `TZ=America/New_York` pin, so ET and process-local were indistinguishable, and `:112-117` asserted a function's source length > 0. New `web/tests/helpers/processTimeZone.ts` sets `TZ=UTC` (the production zone) in `beforeAll`/`afterAll`, throws if the override did not take effect (probe 21 → 1 → 21 on node v24.14.0), and both blocks assert the non-ET day is read. RED by mutation (local getters in `holdTime.ts:44-45` and `flowReportStaleness.ts:76-81`) → `3 failed / 25 passed` (`isEarlierLocalDay` flips; `'2026-08-28 · today'` vs `'2026-08-27 · 1 day old'`); HEAD under the same setup was untouchable by the old test. GREEN `28 passed` ×3 (~200 ms); importers `72 passed`. Source-length case deleted; `vitest.config.ts` untouched. |
| T-345 | DONE | `836b3dcd` | **A real product defect.** `useOrderRisk.ts:741` armed a stock/futures close-out under `no-portfolio` coverage while the option close-out at `:832-835` refused; git shows no argument for the asymmetry (`66e301cd` shipped both as `true`; `4eaaf5e9`, a 40-file DeepSec sweep, tightened only the option path; the `okToSubmit` docblock at `:213` already says no-portfolio hard-blocks). RED `1 failed / 19 passed` (`expected true to be false`); GREEN `20 passed` ×3 (~0.75 s); all 12 importers `162 passed`. Mutations: revert `:741` → 1 failed; drop `coverageStatus === "resolved"` at `:832` → 1 failed. Operator copy already renders 'Coverage indeterminate: portfolio not in scope'. `residualRisk` half NOT mirrored — stock retained-short-call risk is pinned advisory by `order-risk-linear.test.tsx:208-246` (bce9b659); that is an operator decision. |
| T-312 | DONE (P0) | `46e7b300` | **The gate itself.** A docs-only push diffed `github.event.before...HEAD`, classed the range as documentation, skipped every test job, and `deploy` (which accepts `skipped`) shipped the previous tip's runtime as a green release — live at audit HEAD `fda36450` (run `33295066378`). `path_filter.py` now resolves a push base itself: the newest `main` SHA whose ci.yml push run concluded `success` (one `gh api` call, `GREEN_RUN_LOOKBACK=30`) AND that HEAD descends from; anything unresolvable (API error, no `gh`, no green run, none on HEAD's ancestry) → empty range → both gates run. PR events keep the PR base. RED: probe with `BASE_SHA=<red before>` → `python=false web=false`; ci.yml contract `'github.event.before' not in BASE_SHA` failed. GREEN `31 passed` ×3 (~8 s); 7 `scripts/tests` ci.yml readers `86 passed`. Mutations: honour `BASE_SHA` on push → 2 failed; restore `|| github.event.before` → 1 failed. CLI smoke: live push → `base=6d10f1e1 files=1 python=False web=False`; `gh` off PATH → `python=true web=true`. **ci.yml hunks for human review:** `changes` job gains `permissions: {contents: read, actions: read}`; `BASE_SHA` drops `|| github.event.before`; `GH_TOKEN: ${{ github.token }}` added. |
| T-317 | DONE | `a3535fb3` | (T-277 class) 22 CI-green pytest tests flipped red on this clone once `14065b74` provisioned `web/.env` — RED reproduced with a FAKE `web/.env` (`libsql://fake-t317…`): `22 failed, 35 passed` (10/7/3/1/1, every one `FlexTokenLocked`). New autouse `_strip_turso_credentials` in `scripts/tests/conftest.py` delenvs `TURSO_DB_URL`/`TURSO_AUTH_TOKEN` AND wraps `dotenv.load_dotenv`, because a producer first-imported inside a test body otherwise re-populates the keys (demonstrated: delenv-only `1 failed`, wrapper `1 passed`). ONE deliberate fail-closed test through `cash_flow_sync` with the store patched to raise (`TestUnreadableDurableStoreFailsClosed`). GREEN with the fake env present: five files `57 passed`, + provisioning test `72 passed`, 10 referencers `141 passed`; ×3 `29 passed`. Mutations: drop `autouse` → `23 failed`; `active_until` → `None` → the new fail-closed test reds. Loader count is **50**, not 22 (`grep -rl 'load_dotenv(.*web.*\.env' scripts`); the three `setup_*_weekend.sh`/`setup_ci_performance.sh` comments and the provisioning test now say so. |
| T-321 | DONE | `e8e28d2c` | (T-311 class) The static `useSearchParams` mock returned the ORIGINAL string every render, so `syncUrl` was unstable and produced the spurious write #1 (`…legs=BUY%3A1x970C&src=leap`, no `expiry=`) that `.at(-1)` raced against — RED on the old mock: `1 failed / 2 passed`. Mock is now a `useSyncExternalStore` store with a stable router and stable params per URL; the FINAL URL is asserted under `waitFor` to carry the fallback expiry and neither `legs=` nor `src=`, with no `PREFILLED FROM LEAP SCAN`. Under the stateful mock the product is CORRECT (exactly one write) so no source change. Mutation: delete the `useChainUrlState.ts` strip → `1 failed` (`not to contain 'legs='`). GREEN `--sequence.shuffle` 10/10 `3 passed` (1.32–1.40 s, seeds logged); 5 related files `36 passed`; `tsc` clean. |
| T-316 | DONE | `8fd21660` | **A real product defect.** R-383's identical-cash carve-out suppressed a SECOND equal-price partial from the other writer as a duplicate of the daemon's ONE counted fill, publishing `{'c1': 6000.0}` against a true 6666.67 with only `info` logs — RED `2 failed, 11 passed` (`published {'c1': 6000.0} against a true 6666.67`), in-process replay of the five rows pasted. Carve-out KEPT (AC option 2): `_already_journaled_under_other_namespace` now counts `counted` vs `suppressed` per fingerprint, so a suppression with no counted counterpart left is a distinct partial → taint + WARNING naming the contract; the existing two-row case stays `3000.0`/no warning and the symmetric 2+2 dual-write still publishes. GREEN `13 passed` ×3 (0.13 s); every `journal_realized` referencer `71 passed`. Mutations: carve-out unconditional → `4 failed`; revert the counterpart check → `2 failed`. **Reliability loop to review:** R-406's flat-position reset still clears the taint after the withheld close, so a re-entry next cycle replays against a 10-short inventory; the stronger alternative (COUNT the unmatched row) re-opens R-383 policy and was left for the loop. |
| T-315 | DONE | `6d4125cb` | **A real product defect (T-253's client half).** `resolveEntryCost` summed a blended basis for legs on disagreeing basis sources, so the P&L cell, portfolio Open P&L, unrealized breakdown, same-day Today P&L and the close-ticket realised figure all rendered `mv − blend` while Entry Cost showed `—` on the same row; the old test's fixture had `market_value: null` everywhere so the P&L branch was unreachable. RED with leg marks: `10 failed / 10 passed` (`not include '+$3,500'`, `expected 3500 to be null`, `expected 7000 to be 3500`). ONE choke point: `resolveEntryCost` → `null` on `hasBlendedLegBasis`; `getPnlDollars`/`getAvgEntry`/`getInitialValue`/Today P&L/`computeUnrealizedBreakdown` propagate null, `MetricCards` sums via `sumUnrealizedBreakdown` and appends `N UNMEASURED BASIS`, the close ticket carries `closeOut: { entryCostDollars: null }` → `estimatedPnl: null`, no realised line (not `closeOut: null`, which would route a pure close through the opening-risk model as a naked SELL). GREEN `20 passed` ×3 (~1 s); 71 importer files `907 passed`; `tsc` + eslint clean. Mutation: delete the gate → `12 failed` (`+$3,500` back). UI e2e evidence recorded below. |
