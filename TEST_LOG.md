
| T-352 | DONE | (this commit) | The prologue page seam (wrapper `PHASE="prologue"` ↔ `weekend_notify.py` `choices=[...]`) is now crossed by an executed test: new `scripts/tests/test_prologue_page_wire.py` runs all five wrappers (the three new ones plus `testing_weekend.sh` / `reliability_weekend.sh`, same prologue) through two real refusals (marker absent → rc 2, live-pid lock → rc 3) with a `python3` stub that records NUL-separated argv, replays that exact argv into the real `weekend_notify.main()` with `_http_post` patched, and asserts one POST with `title == "radon <loop> prologue"`, message starting with the REFUSED status, priority 0. RED: `PHASE="pre-flight"` in `ci_performance_nightly.sh` → `2 failed / 8 passed`; `choices=["audit","remediate"]` → `10 failed`; both reverted. GREEN 10 (39.5 s). No source changed; wrappers untouched. Lead re-ran after T-351's notifier change landed: 10 passed. |
