# Operator exploration review

Three distinct compositions, desktop 1280 × 830 and mobile 390px, reviewed in Chrome on 2026-09-25. Full comparison visually inspected in both Clear dark and light themes. Mobile document width and scroll width both 390px for A, B, C. Primary action bottom positions: A 499px, B 390px, C 576px; each above the mobile navigation at a 752px viewport height.

Manual browser walkthrough: C simulated request → awaiting approval → verification → recovery dialog; A emergency controls distinguish halt/cancel/kill, typed KILL enables only the simulated confirmation; B service selection opens Data writers with overdue scheduled work and neutral on-demand state. JavaScript syntax check passed. No local test suites were run. Production controls were not activated.

This is a local static prototype, not a deployed application. Search, full inventories, host trend charts, access administration and durable history are specified via disclosure placeholders rather than built. Synthetic data and simulated actions are labelled in every concept. Source audit and research are in research.md. Existing real host capability gates and lockouts must remain authoritative during implementation.

Recommendation: A gives recurring operator checks the clearest priority and supports multiple simultaneous conditions. B suits deeper subsystem investigation. C makes a multistep recovery explicit on mobile. The user selected A for desktop and mobile.

Review: http://127.0.0.1:8768/ . If the local preview server is no longer running: python3 -m http.server 8768 --bind 127.0.0.1 --directory docs/design/operator-action-queue . The server exposes only this synthetic prototype directory. The selected reference and research are archived here; the task-specific temporary lab was removed by moving it to this durable location. The default view opens A; the comparison remains available through the Compare button.
