## Issue discovered

<what went wrong, in plain language>

## What was done to fix it

<what this PR actually changed>

<!-- ci-performance time-saving fixes: include the CI build time table
     | Job | Before | After | % change |
     % change = (after - before) / before * 100 (negative = faster).
     Cite Actions runs; do not invent timings. If after is still
     VALIDATING / INSUFFICIENT_SAMPLE: After = pending, % = TBD until N samples.
-->

## Next

Fixed with green deployment
