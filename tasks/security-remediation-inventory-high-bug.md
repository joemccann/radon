# HIGH_BUG remediation inventory (122 findings)

Source: `/Users/joemccann/dev/apps/finance/radon/.deepsec/data/radon/reports/report.md`, `HIGH_BUG` section only. Evidence was checked against worktree commit `1bed485edf7eb2a2a20aca1be5a40002f245b086`.

## Ownership clusters

- `BKP` backup, restore, and nightly maintenance; owner: cloud/data reliability.
- `OPS` service scheduling, health, incident, and scraper durability; owner: cloud/runtime reliability.
- `DATA` provider, persistence, portfolio, and accounting correctness; owner: Python/data pipeline.
- `API-GUARD` server-side order identity, validation, authorization, and idempotency; owner: API/security. P0 trading safety.
- `RISK-CORE` canonical order-risk and signed-pricing logic; owner: web trading platform. P0 trading safety.
- `ORDER-UI` desktop/mobile live-order surfaces; owner: web trading platform. P0 trading safety.
- `CHAT-GUARD` assistant proposal schema and order-risk chokepoint; owner: assistant + web trading platform. P0 trading safety.
- `SIGNAL` trading-signal freshness, normalization, and request isolation; owner: analytics/web data.

Disposition vocabulary: `confirmed` is a distinct actionable defect; `duplicate` is a separately reported entry whose fix is wholly owned by the cited primary finding. No finding is omitted when duplicate.

## Inventory

| ID | Exact finding and file(s) | Disposition and current-code evidence | Minimal patch | Exact regression target | Owner |
|---:|---|---|---|---|---|
| HB-001 | Virtual-table shadow objects make generated dumps unrestorable<br>`cloud/scripts/db_backup.py` | confirmed. `dump_database()` treats every non-internal `sqlite_master` table as user-owned; `_INTERNAL_PREFIXES` does not identify FTS shadow tables, and all non-table SQL is replayed unchanged. | Classify virtual/shadow/provider objects, omit generated objects, and rebuild FTS/vector artifacts from migrations/canonical rows. | `cloud/tests/test_db_backup.py::test_production_fts_dump_round_trips_without_shadow_collisions` | BKP |
| HB-002 | The backup is not taken from a consistent database snapshot<br>`cloud/scripts/db_backup.py` | confirmed. `iter_table_rows()` issues independent `LIMIT/OFFSET` reads; the only `BEGIN TRANSACTION` is written to the destination stream. | Hold one source snapshot/transaction and keyset-page within it; abort if snapshot semantics are unavailable. | `cloud/tests/test_db_backup.py::test_dump_uses_one_source_snapshot_under_concurrent_mutation` | BKP |
| HB-003 | A late paging failure silently duplicates previously dumped rows<br>`cloud/scripts/db_backup.py` | confirmed. The `except` around the generator falls back to unpaged `SELECT *` after prior pages may already have been yielded. | Permit fallback only before the first emitted row; otherwise fail the dump or resume from a stable key. | `cloud/tests/test_db_backup.py::test_late_page_failure_aborts_without_replaying_rows` | BKP |
| HB-004 | Migration timeout starts API with an unverified schema<br>`cloud/services/radon-api.service`, `scripts/db/migrate.py` | confirmed. `ExecStartPre` explicitly accepts `timeout` rc 124 without checking applied versions. | Fail closed on 124 unless a bounded schema-version verifier proves all migrations present; make migration/version record atomic. | `cloud/tests/test_systemd_services.py::test_api_migration_timeout_requires_verified_current_schema` | BKP |
| HB-005 | Backup executable produces invalid dumps for FTS5 tables<br>`cloud/services/radon-db-backup.service`, `cloud/scripts/db_backup.py` | duplicate of HB-001. The unit directly executes the affected dumper and adds no restore validation. | Fix HB-001 and require post-dump restore/integrity verification before publication. | `cloud/tests/test_systemd_services.py::test_db_backup_service_runs_verified_backup` | BKP |
| HB-006 | Backup is not taken from a consistent database snapshot<br>`cloud/services/radon-db-backup.service`, `cloud/scripts/db_backup.py` | duplicate of HB-002. The one-hour process bound does not provide source snapshot isolation. | Fix HB-002; publish only after consistent-snapshot and restore checks succeed. | `cloud/tests/test_db_backup.py::test_service_dump_snapshot_is_consistent` | BKP |
| HB-007 | Persistent catch-up can run backup concurrently with retention<br>`cloud/services/radon-db-backup.timer`, `cloud/services/radon-db-retention.timer`, `cloud/services/radon-portfolio-archive.timer` | confirmed. Backup, retention, and archive timers are independently `Persistent=true`; calendar offsets and randomized delay are their only sequencing. | Add one shared `flock`/orchestrator with archive -> retention -> backup ordering for scheduled and catch-up runs. | `cloud/tests/test_systemd_services.py::test_database_maintenance_units_share_serialization_lock` | BKP |
| HB-008 | Partial retention failures are reported as successful<br>`cloud/services/radon-db-retention.service`, `scripts/db_retention_sweep.py` | confirmed. The unit trusts the sweep exit code; the sweep's aggregate success path permits failed policies while recording an `ok` heartbeat. | Exit nonzero and record `error`/alertable degraded state when any required policy fails. | `scripts/tests/test_db_retention.py::test_partial_policy_failure_is_nonzero_and_unhealthy` | BKP |
| HB-009 | Persistent retention catch-up bypasses maintenance sequencing<br>`cloud/services/radon-db-retention.timer`, `cloud/services/radon-db-backup.timer`, `cloud/services/radon-portfolio-archive.timer` | duplicate of HB-007. The retention timer is independently persistent with no lock/dependency chain. | Apply the HB-007 shared lock/orchestrator to retention. | `cloud/tests/test_systemd_services.py::test_retention_catchup_cannot_overlap_archive_or_backup` | BKP |
| HB-010 | Lookback query eventually freezes forecasts on the oldest observations<br>`cloud/services/radon-forecast-nightly.service`, `scripts/db/writer.py` | confirmed. The service requests `--lookback 250`; writer history uses ascending order plus limit, so the oldest 250 win once history grows. | Select newest N in a descending inner query, then reorder ascending for model input. | `scripts/tests/test_forecast_writers.py::test_history_lookback_keeps_latest_250_rows` | DATA |
| HB-011 | Indeterminate probes can falsely resolve active P1 incidents<br>`cloud/services/radon-incident-watchdog.service`, `scripts/incident_watchdog/store.py` | confirmed. A one-cycle command feeds classifications to store logic that resolves open fingerprints merely absent from the cycle, including `unknown` probes. | Carry probe ownership/state into reconciliation; resolve only on definitive healthy observation. | `scripts/tests/test_incident_watchdog.py::test_down_to_unknown_does_not_resolve_open_incident` | OPS |
| HB-012 | Hourly ingest performs an unbounded journal read<br>`cloud/services/radon-knowledge.service`, `scripts/knowledge/sources/journal.py` | confirmed. `--source all` reaches the journal connector's unpaginated full-payload query. | Cursor-page by stable `trade_id`/timestamp and process bounded batches. | `scripts/tests/test_knowledge_sources.py::test_journal_source_pages_large_corpus_with_bounded_selects` | DATA |
| HB-013 | Provider failures erase the last good LEAP cache and report success<br>`cloud/services/radon-leap.service`, `scripts/leap_scanner_uw.py` | confirmed. The unit trusts the wrapper/scanner while per-ticker failures can still produce an empty cache and `ok` heartbeat. | Track requested/success/failed counts; preserve last good cache and fail/heartbeat error when zero valid tickers result. | `scripts/tests/test_leap_scanner.py::test_all_provider_failures_preserve_cache_and_fail_health` | SIGNAL |
| HB-014 | The advertised direct fallback is silently nonfunctional<br>`cloud/services/radon-leap.service`, `scripts/run_leap_refresh.sh` | confirmed. Unit omits `RADON_PYTHON_BIN`, allows only 360 seconds, and wrapper status is captured after a completed `if`. | Pin `.venv` Python, capture fallback rc immediately, and give each bounded phase a compatible timeout. | `scripts/tests/test_run_leap_refresh_wrapper.py::test_http_and_direct_failure_propagates_nonzero_with_venv_python` | OPS |
| HB-015 | Scheduled LEAP refresh failures are reported as successful<br>`cloud/services/radon-leap.timer`, `scripts/run_leap_refresh.sh` | duplicate of HB-014. Timer success is entirely the wrapper's masked exit status. | Apply HB-014 and assert timer/unit failure visibility. | `cloud/tests/test_systemd_services.py::test_leap_timer_surfaces_dual_refresh_failure` | OPS |
| HB-016 | Timed-out trading handlers continue and can overlap later executions<br>`cloud/services/radon-monitor.service`, `scripts/monitor_daemon/daemon.py`, `scripts/monitor_daemon/handlers/exit_orders.py` | confirmed. `WatchdogSec` covers only the loop; timed-out Python threads cannot be killed, remain due until completion, and order mutation can run twice. | Put order-mutating handlers in killable workers, add per-handler in-flight claims, refresh broker orders, and enforce durable placement idempotency. | `scripts/tests/test_monitor_daemon/test_exit_orders_guard_durability.py::test_timed_out_exit_worker_cannot_overlap_or_duplicate_sell` | API-GUARD |
| HB-017 | A crashed Playwright browser leaves the service permanently alive but unusable<br>`cloud/services/radon-newsfeed.service`, `scripts/newsfeed/index.js`, `scripts/newsfeed/scheduler.js` | confirmed. `Restart=on-failure` cannot help while the scheduler catches cycle errors and retains a dead cached browser handle. | Invalidate/close browser context on crash; recreate once, then exit nonzero after bounded consecutive recovery failures. | `web/tests/newsfeed-scraper.test.ts::crashed_browser_is_recreated_then_process_fails_boundedly` | OPS |
| HB-018 | OI refresh failures are reported as successful<br>`cloud/services/radon-oi-changes.service`, `scripts/run_oi_changes_refresh.sh` | confirmed. Unit invokes the wrapper whose failure status is captured after an `if`, yielding zero on the no-branch path. | Capture the fetch rc in `else` and propagate it unchanged. | `scripts/tests/test_run_oi_changes_refresh_wrapper.py::test_failed_fetch_exits_nonzero` | OPS |
| HB-019 | Persistent catch-up defeats maintenance-job isolation<br>`cloud/services/radon-portfolio-archive.timer`, `cloud/services/radon-db-retention.timer`, `cloud/services/radon-db-backup.timer` | duplicate of HB-007. Archive is also independently persistent and lacks a shared maintenance lock. | Apply HB-007 orchestration/lock to archive. | `cloud/tests/test_systemd_services.py::test_archive_catchup_precedes_retention_and_backup` | BKP |
| HB-020 | Start limit conflicts with the one-minute timer cadence<br>`cloud/services/radon-skew.service`, `cloud/services/radon-skew.timer` | confirmed. Service allows five starts/300s while its healthy timer cadence is one minute. | Raise burst to at least 10 or disable the start limiter for the bounded oneshot. | `cloud/tests/test_systemd_services.py::test_one_minute_skew_timer_does_not_trip_start_limit` | OPS |
| HB-021 | Persistent child timer can certify stale parent data as fresh<br>`cloud/services/radon-skew2d.timer`, `cloud/services/radon-skew.timer`, `scripts/fetch_skew2d.py` | confirmed. Child is persistent, fixed-time scheduled, and source freshness is not causally verified. | Order child after successful parent finalization and reject/retry source data older than the expected completed session. | `scripts/tests/test_fetch_skew2d.py::test_stale_parent_session_cannot_emit_fresh_ok_child` | SIGNAL |
| HB-022 | ExecStart wrapper reports failed refreshes as successful<br>`cloud/services/radon-vcg-refresh.service`, `scripts/run_vcg_refresh.sh` | confirmed. The executed wrapper captures `$?` after an `if`, masking dual failure. | Preserve fallback rc immediately and exit with it. | `scripts/tests/test_run_vcg_refresh_wrapper.py::test_dual_failure_exits_nonzero` | OPS |
| HB-023 | Synchronized watchdog buckets can lose alerts<br>`cloud/services/radon-watchdog-continuous.timer`, `scripts/watchdog/notify.py` | confirmed. Bucket timers share minute boundaries and shared unlocked JSON/health rows. | Serialize bucket dispatch or lock+atomically replace digest state; use failure-preserving per-bucket health. | `scripts/tests/test_watchdog/test_dispatcher_writer_semantics.py::test_concurrent_buckets_preserve_all_alerts_and_failures` | OPS |
| HB-024 | Hourly digest flush races concurrent alert enqueues<br>`cloud/services/radon-watchdog-daily.timer`, `scripts/watchdog/notify.py` | confirmed. Daily fires at `*:00` with other buckets; flush/read/clear can discard a concurrent enqueue. | Put enqueue+flush under the HB-023 lock/transaction and clear only delivered item IDs. | `scripts/tests/test_watchdog/test_notify.py::test_digest_flush_does_not_drop_concurrent_enqueue` | OPS |
| HB-025 | Timezone drift schedules both CTA refreshes before market close<br>`config/com.radon.cta-sync.plist`, `scripts/cta_sync_service.py` | confirmed. Fixed host-local 13:15/14:00 calendar entries are not ET-aware and both precede 16:00 ET on the current Chicago host. | Gate/schedule against `America/New_York`, with post-close assertion independent of host timezone. | `scripts/tests/test_cta_sync_runtime.py::test_launchd_schedule_is_post_close_in_chicago_and_pacific` | SIGNAL |
| HB-026 | Timezone drift stops market-data refreshes before the close<br>`config/com.radon.data-refresh.plist`, `scripts/run_data_refresh.sh` | confirmed. Fixed host-local schedule ends at 13:15, so intended ET close coverage varies with host timezone. | Use an ET-aware wrapper/interval scheduler and validate final run at or after 16:15 ET. | `scripts/tests/test_data_refresh_schedule.py::test_final_refresh_covers_1615_et_across_host_timezones` | SIGNAL |
| HB-027 | Legacy scheduler can place duplicate live exit orders<br>`config/com.radon.exit-order-service.plist`, `scripts/exit_order_service.py` | confirmed. Obsolete five-minute one-shot remains installable and bypasses monitor-daemon durable claims/idempotency. | Remove/disable the legacy job and installer references; if retained, route through the canonical claimed/idempotent handler. | `scripts/tests/test_legacy_exit_order_service.py::test_legacy_launchagent_cannot_place_orders` | API-GUARD |
| HB-028 | Unbounded IB calls can permanently wedge the shared data pool<br>`scripts/api/routes/historical.py`, `scripts/clients/ib_client.py` | confirmed. Routes hold `pool.acquire("data")` across unbounded `asyncio.to_thread()` IB calls; coroutine cancellation cannot stop the worker using the client. | Add bounded client operations/acquisition and quarantine/reconnect clients after operation timeout before releasing the role. | `scripts/api/tests/test_historical_pool.py::test_timed_out_ib_call_retires_client_without_releasing_live_worker` | API-GUARD |
| HB-029 | Head-timestamp endpoint calls a nonexistent IBClient method<br>`scripts/api/routes/historical.py`, `scripts/clients/ib_client.py` | confirmed. Route calls `client.get_head_timestamp`; the client exposes no such method. | Implement bounded `reqHeadTimeStamp` wrapper and normalize its response. | `scripts/api/tests/test_historical_pool.py::test_head_timestamp_uses_bounded_client_method` | DATA |
| HB-030 | GEX cooldown can return and publish data for the wrong ticker<br>`scripts/api/server.py` | confirmed. `_gex_last_scan`, lock, and `data/gex.json` are global; cooldown return never checks cached ticker. | Restrict to SPX or key cooldown/lock/cache by normalized ticker and validate payload ticker. | `scripts/api/tests/test_gex_route.py::test_cooldown_never_returns_another_tickers_cache` | SIGNAL |
| HB-031 | Execution corrections cannot safely supersede applied lifecycle rows<br>`scripts/capture_position_return_capital.py` | confirmed. `_load_executions()` selects the latest correction suffix, but append-only IDs still use corrected `exec_id` and `INSERT OR IGNORE` never supersedes prior lifecycle rows. | Use correction root as stable identity and transactionally void/supersede prior events; make conflicts errors. | `scripts/tests/test_position_reconcile_spine.py::test_execution_correction_supersedes_prior_lifecycle_once` | DATA |
| HB-032 | UW failures are published as valid trading-discovery results<br>`scripts/discover.py` | confirmed. UW exceptions become empty data/partial candidates and `main()` can mirror/alert without degraded-state metadata. | Preserve typed provider failure, fail required market fetches, skip affected tickers, and block mirroring/alerts on degraded results. | `scripts/tests/test_discover.py::test_required_uw_failure_cannot_publish_or_alert` | SIGNAL |
| HB-033 | Edge evaluation passes after mandatory data-fetch failures<br>`scripts/evaluate.py` | confirmed. Parallel errors use function-name keys while milestone reads default to empty data; M3/M3B/price availability can therefore pass open. | Return typed stable milestone results and fail the sequential edge gate on required fetch/freshness errors. | `scripts/tests/test_evaluate.py::test_m3_m3b_or_price_fetch_failure_stops_at_edge_gate` | SIGNAL |
| HB-034 | Malformed or incomplete API responses are reported as healthy<br>`scripts/health_service/probes.py` | confirmed. JSON decode/empty bodies collapse to `{}` with `state='up'`; `_nested_api_state` accepts absent required fields. | Require object payload plus exact required typed fields; malformed/incomplete becomes `unknown`, never healthy. | `scripts/tests/test_health_service.py::test_lite_probe_fails_closed_for_204_invalid_empty_and_incomplete_json` | OPS |
| HB-035 | Multi-account positions are combined with only the first account's financial state<br>`scripts/ib_sync.py` | confirmed. Account summary/PnL chooses `managedAccounts()[0]` while positions span visible accounts; lookup keys omit account ID. | Require one explicit account end-to-end or emit account-separated snapshots; account-scope every basis/date/PnL key. | `scripts/tests/test_ib_sync_multi_account.py::test_positions_never_cross_account_financial_state` | DATA |
| HB-036 | No-quote and sentinel prices can corrupt portfolio valuation<br>`scripts/ib_sync.py` | confirmed. `_normalize_market_price()` does not exclude zero, infinity, or IB positive sentinels before preferred resolution. | Accept only finite positive non-sentinel prices, then fall back to valid midpoint/close. | `scripts/tests/test_ib_sync_market_price.py::test_zero_infinity_and_ib_sentinels_use_valid_fallback` | DATA |
| HB-037 | Bid or ask updates leave a stale midpoint marked as a real trade<br>`scripts/ib_tick_handler.js` | confirmed. A derived `last` survives side changes while `lastIsCalculated` is reset false and midpoint recomputation is skipped. | Track last source; recompute/clear derived midpoint on BID/ASK and set false only on actual LAST. | `scripts/ib_tick_handler.test.js::derived_midpoint_remains_calculated_and_tracks_book_updates` | SIGNAL |
| HB-038 | An unsafe mirror-path override can make rsync delete arbitrary local data<br>`scripts/incident_responder.py` | confirmed. Environment-controlled mirror destination reaches destructive `rsync --delete` without canonical-root containment checks. | Resolve/canonicalize target, require approved dedicated root and sentinel, reject symlinks/root/broad paths before rsync. | `scripts/tests/test_incident_responder.py::test_mirror_delete_rejects_root_parent_symlink_and_unapproved_override` | API-GUARD |
| HB-039 | Torn WAL tail corrupts the first finding written after resume<br>`scripts/lib/checkpoint.py` | confirmed. Recovery ignores malformed final JSONL but `_append_finding()` opens append mode without truncating the torn tail. | Repair to last complete newline, flush+fsync, then append. | `scripts/tests/test_checkpoint.py::test_resume_truncates_torn_wal_tail_before_append` | DATA |
| HB-040 | Global tick timestamp misclassifies subscription health<br>`scripts/lib/staleDataMachine.js`, `scripts/ib_realtime_server.js` | confirmed. `decideStaleAction()` receives one `lastTickAt` and aggregate `activeSubscriptions`, so one symbol masks another and rejected subscriptions still count. | Track expected/freshness per subject or a dedicated liquid probe; remove rejected/unentitled subjects and require corroboration before restart. | `scripts/lib/staleDataMachine.test.js::per_subscription_staleness_cannot_be_masked_or_escalate_unentitled_subjects` | OPS |
| HB-041 | Failed browser initialization leaks Chromium processes on every scrape cycle<br>`scripts/newsfeed/browser.js` | confirmed. `launcher.launch()` succeeds before storage/context/page operations, with no cleanup catch before returning the handle. | Wrap post-launch setup; close partial context/browser and quarantine corrupt storage before rethrow. | `web/tests/newsfeed-scraper.test.ts::failed_context_or_page_initialization_closes_browser` | OPS |
| HB-042 | Startup seeding overwrites existing data after any read error<br>`scripts/newsfeed/paths.js` | confirmed. Existing-file `readFile(...).catch(() => "")` converts every read error into an empty file that is overwritten with `[]`. | Seed only ENOENT/verified zero-byte; propagate all other reads; use atomic replacement. | `web/tests/newsfeed-scraper.test.ts::seed_preserves_existing_file_on_read_error` | DATA |
| HB-043 | Read failures silently replace post history with an empty dataset<br>`scripts/newsfeed/store.js` | confirmed. `loadExistingPosts()` returns `[]` for malformed shape/JSON and all non-ENOENT filesystem errors. | Return empty only on ENOENT; validate, propagate, and quarantine corrupt content. | `web/tests/newsfeed-scraper.test.ts::load_existing_posts_fails_closed_without_empty_history` | DATA |
| HB-044 | Whole-file persistence is neither atomic nor serialized<br>`scripts/newsfeed/store.js` | confirmed. `persistPosts()` writes live/archive files directly with no lock or rename. | Serialize writers and use same-directory temp write, fsync, atomic rename. | `web/tests/newsfeed-scraper.test.ts::concurrent_or_interrupted_persist_is_atomic_and_lossless` | DATA |
| HB-045 | Position reversals duplicate the same executions across two lifecycle events<br>`scripts/position_return_capital.py` | confirmed. `replay_transactions()` passes the same full `items` to CLOSE and new OPEN when a leg crosses zero. | Split execution quantity into closing/opening portions or emit one reversal event; assert exact-once quantity allocation. | `tests/test_position_return_capital.py::test_reversal_allocates_each_execution_quantity_once` | DATA |
| HB-046 | Demo administration has no safe allowlist configuration<br>`web/app/api/admin/demo-users/route.ts`, `web/lib/demo/adminAuth.ts`, `web/middleware.ts` | confirmed. Both handlers call `requireDemoAdmin()` backed by the same `ALLOWED_USER_IDS` whose middleware meaning conflicts with trial-user access. | Introduce fail-closed `DEMO_ADMIN_USER_IDS`/role claim independent of general demo access. | `web/tests/demo-admin.test.ts::demo_admin_authorization_is_independent_of_trial_allowlist` | API-GUARD |
| HB-047 | One malformed journal payload hides the entire blotter<br>`web/app/api/blotter/route.ts` | confirmed. One `result.rows.map(JSON.parse)` throws; the broad catch returns `null`, then GET emits `emptyBlotter()` with 200. | Parse per row, report degraded/quarantined rows, and error when no trustworthy result remains. | `web/tests/blotter-from-journal.test.ts::malformed_row_cannot_turn_nonempty_journal_into_empty_success` | DATA |
| HB-048 | NQ-SPX skew output contains absolute NQ skew and mixes incompatible metrics<br>`web/app/api/internals/route.ts` | confirmed. MenthorQ computes `nqPos - spxPos`; long-range mapping assigns absolute NQ RR to `nq_skew`, then both publish through identical fields. | Separate typed CTA-position and option-RR series; compute date-aligned NQ-SPX only for the named spread. | `web/tests/internals-skew-chart.test.ts::nq_spx_field_has_one_formula_and_unit_across_sources` | SIGNAL |
| HB-049 | Sync can report success after importing a stale reconciliation snapshot<br>`web/app/api/journal/sync/route.ts`, `scripts/api/server.py`, `scripts/ib_reconcile.py`, `web/lib/journalDb.ts` | confirmed. Route awaits `/journal/reconcile` then independently calls `importLatestReconciliationToJournal()` without an exact fresh snapshot ID. | Persist transactionally, return snapshot ID only after success, import that ID, and require timestamp >= request start. | `web/tests/journal-sync.test.ts::sync_imports_only_the_fresh_returned_reconciliation_snapshot` | DATA |
| HB-050 | Cancel-and-replace can leave orders partially cancelled or replacement status indeterminate<br>`web/app/api/orders/modify/route.ts`, `scripts/api/server.py` | confirmed. Route cancels sequentially before full replacement preflight, times out placement at 20s, and converts all exceptions including indeterminate status to 500. | Move to backend state machine; validate/preflight first, record every cancel, use durable key, and preserve 504/indeterminate status. | `web/tests/orders-manage.test.ts::replace_preflight_and_indeterminate_outcome_never_hide_partial_cancel` | API-GUARD |
| HB-051 | Unbounded combo ratios bypass effective contract limits<br>`web/app/api/orders/place/route.ts`, `scripts/ib_place_order.py` | confirmed. `ratio` is an unrestricted number forwarded to IB; top-level quantity limit ignores `quantity * ratio`. | Require bounded positive integer ratios/leg count and cap effective contracts at final server placement chokepoint. | `web/tests/orders-route.test.ts::combo_ratio_and_effective_contract_limits_fail_closed` | API-GUARD |
| HB-052 | Caller-supplied conId is not bound to the submitted instrument<br>`web/app/api/orders/place/route.ts`, `scripts/api/server.py`, `scripts/ib_place_order.py` | confirmed. Route forwards `conId` for option/future without comparing resolved contract attributes to reviewed fields. | Server-qualify conId and exact-match symbol/secType/expiry/strike/right/exchange before placement. | `scripts/api/tests/test_orders_place_safety_contract.py::test_conid_must_match_every_submitted_contract_attribute` | API-GUARD |
| HB-053 | UTC cache rollover can serve a two-session-old close<br>`web/app/api/previous-close/route.ts` | confirmed. `cacheKey()` uses UTC date while selection uses ET date, allowing Monday's Friday close under Tuesday's key. | Use shared holiday/session-aware ET key for cache and selection. | `web/tests/previous-close-yahoo-daily-array.test.ts::cache_rollover_at_et_midnight_uses_expected_session` | SIGNAL |
| HB-054 | Concurrent ratings requests can permanently corrupt the disk cache<br>`web/app/api/ticker/ratings/route.ts`, `scripts/fetch_analyst_ratings.py` | confirmed. GETs independently invoke the fetcher; legacy JSON cache mutation is unlocked and truncating. | Coalesce requests; inter-process lock plus atomic writer, or make DB canonical and quarantine corrupt JSON. | `scripts/tests/test_fetch_analyst_ratings.py::test_concurrent_cache_updates_are_atomic_and_lossless` | DATA |
| HB-055 | Session-date logic causes repeated off-hours and holiday scans<br>`web/app/api/vcg/route.ts` | confirmed. `todayET()` is raw calendar date and `isMarketOpenNow()` is weekday/time only; prior-session cache repeatedly triggers background scans. | Use shared exchange calendar for expected session/open state and suppress fresh prior-session rescans. | `web/tests/vcg-route-freshness.test.ts::weekend_preopen_and_holiday_cache_do_not_rescan` | SIGNAL |
| HB-056 | Confirmed option proposals are submitted as stock orders<br>`web/components/ChatLauncher.tsx`, `web/lib/chat.ts` | duplicate of HB-100. Launcher globally exposes the same `ChatPanel -> placeProposedOrder()` hardcoded-stock path. | Fix the canonical discriminated proposal/placement flow in HB-097/HB-100. | `web/tests/chat-launcher-focus.test.tsx::launcher_rejects_ambiguous_option_proposal_before_confirmation` | CHAT-GUARD |
| HB-057 | Chat order confirmation bypasses the mandatory order-risk gate<br>`web/components/ChatLauncher.tsx`, `web/components/ChatPanel.tsx` | duplicate of HB-101. Launcher passes no portfolio and exposes the same direct confirm action. | Apply HB-101 canonical risk-gated confirmation and server policy. | `web/tests/chat-launcher-focus.test.tsx::launcher_cannot_confirm_without_resolved_order_risk` | CHAT-GUARD |
| HB-058 | Options proposals are executed as stock orders<br>`web/components/ChatPanel.tsx`, `web/lib/chat.ts` | duplicate of HB-100. Confirmation renders assistant summary but invokes the hardcoded-stock mapper. | Apply HB-100 and render summary from the validated canonical payload. | `web/tests/chat-order-confirm.test.tsx::option_summary_and_submitted_instrument_are_identical` | CHAT-GUARD |
| HB-059 | Chat order placement bypasses the mandatory risk gate<br>`web/components/ChatPanel.tsx`, `web/lib/chat.ts` | duplicate of HB-101. `confirmProposal()` calls placement directly; button only checks `isPlacing`. | Apply HB-101 and disable until branded risk state permits. | `web/tests/chat-order-confirm.test.tsx::confirm_button_requires_resolved_ok_order_risk` | CHAT-GUARD |
| HB-060 | Superseded order proposals remain live and confirmable<br>`web/components/ChatPanel.tsx` | confirmed. New submissions append messages/status but do not synchronously clear proposal; later proposal updates can leave old state confirmable. | Clear/invalidate proposal at new-turn start; bind confirmation to turn ID/hash and reject superseded intent. | `web/tests/chat-order-confirm.test.tsx::new_turn_invalidates_prior_order_proposal` | CHAT-GUARD |
| HB-061 | Gate-3 correlation warning is not connected to any production surface<br>`web/components/CorrelationRiskBanner.tsx`, `web/lib/order/risk/useOrderRisk.ts` | confirmed. Component computes the warning but no production order surface imports/renders it. | Fold correlation into canonical `OrderRiskGate` state and render it on every order surface; block if policy requires. | `web/tests/order-risk-chokepoint.test.tsx::all_live_order_surfaces_render_correlation_gate` | RISK-CORE |
| HB-062 | Exact 1st-percentile CTA signals can be inverted to the 100th percentile<br>`web/components/CtaBriefing.tsx` | confirmed. Local percentile normalization treats exact `1` ambiguously as fraction and scales to `100`. | Centralize contract-aware normalization; scale only values proven to be fractional, preserving integer percentile 1. | `web/tests/cta-page.test.ts::exact_first_percentile_remains_one_in_briefing` | SIGNAL |
| HB-063 | CTA callouts can reverse exact 1st-percentile signals<br>`web/components/CtaPage.tsx` | confirmed. A second local `pctile()` repeats the exact-1 ambiguity on page callout classification. | Replace local copy with HB-062 shared normalized helper. | `web/tests/cta-page.test.ts::exact_first_percentile_selects_low_not_high_callout` | SIGNAL |
| HB-064 | Opening the CTA page starts an unbounded regime-scan loop<br>`web/components/CtaPage.tsx`, `web/lib/useRegime.ts` | confirmed. Page calls `useRegime(true)`, enabling active scan behavior instead of cache-only CTA reads. | Use cache/read-only mode; refresh through the bounded scheduled/shared regime owner. | `web/tests/cta-page-freshness.test.ts::mount_and_poll_never_trigger_regime_scan` | SIGNAL |
| HB-065 | Stop orders are silently submitted as ordinary limit orders<br>`web/components/InstrumentDetailModal.tsx`, `web/components/SingleLegOrderTicket.tsx` | confirmed. Modal does not expose/forward order-type semantics for the stop-capable path, so the reusable ticket defaults to limit placement. | Thread selected order type/stop price through one canonical builder and review payload. | `web/e2e/stop-order-desktop.spec.ts::instrument_modal_submits_stp_not_lmt` | ORDER-UI |
| HB-066 | Indeterminate portfolio coverage does not block live order submission<br>`web/components/InstrumentDetailModal.tsx`, `web/components/SingleLegOrderTicket.tsx` | confirmed. Portfolio defaults to `null`; ticket `canSubmit` is local-price validity and is not coupled to risk state. | Preserve `undefined` pending state and make submit require `riskState.okToSubmit === true`. | `web/tests/order-risk-linear.test.tsx::instrument_modal_blocks_pending_and_null_coverage` | ORDER-UI |
| HB-067 | SELL combo modifications model the opposite position<br>`web/components/ModifyOrderModal.tsx` | confirmed. Risk `chainLegs` preserve combo leg direction while envelope action is SELL, instead of modeling close/reverse semantics consistently. | Build modify risk input through canonical combo direction transformer shared with submitted payload. | `web/tests/modify-order-negative-risk-reversal.test.tsx::sell_combo_review_models_submitted_direction` | ORDER-UI |
| HB-068 | Combo reference prices ignore edited legs and leg ratios<br>`web/components/ModifyOrderModal.tsx` | confirmed. Implied reference derives from original order legs and sums unit prices without edited ratios. | Recompute signed natural quote from normalized edited legs and ratios; invalidate manual price on structure edit. | `web/tests/open-order-combo-modify.test.ts::edited_ratio_legs_drive_reference_and_payload_price` | ORDER-UI |
| HB-069 | Modify submission bypasses unresolved and linear-order risk checks<br>`web/components/ModifyOrderModal.tsx` | confirmed. `canSubmit` permits non-combo/unsupported risk paths and the button is not gated by resolved `okToSubmit`. | Route every replacement through canonical risk input/gate; require resolved permit before request. | `web/e2e/modify-order-confirmation.spec.ts::modify_is_disabled_until_canonical_risk_permits` | ORDER-UI |
| HB-070 | Stale modal state can misclassify an opening order as a position close<br>`web/components/PositionTable.tsx`, `web/components/InstrumentDetailModal.tsx` | confirmed. `activeInstrument` stores the prior leg/ticker/expiry and is not invalidated when portfolio data changes. | Store stable position+leg identity, re-resolve each render, and close modal if identity no longer matches. | `web/tests/position-table-leg-row-runtime.test.ts::portfolio_refresh_invalidates_stale_trade_modal_identity` | ORDER-UI |
| HB-071 | Live COR1M values do not update the crash-trigger decision<br>`web/components/RegimePanel.tsx` | confirmed. Live values update strip display, while crash-trigger calculation remains tied to cached regime data. | Derive display and trigger from one active live-or-cached COR1M selector with freshness provenance. | `web/tests/use-skew-realtime.test.tsx::live_cor1m_updates_crash_trigger_and_display_together` | SIGNAL |
| HB-072 | Risk gate cannot block live order submission<br>`web/components/SingleLegOrderTicket.tsx` | confirmed. `riskGate` is rendered in confirmation, but `canSubmit` ignores its state and placement occurs inside the ticket. | Accept branded risk state/submit permit, not arbitrary ReactNode; require `okToSubmit` before placement. | `web/tests/order-risk-linear.test.tsx::single_leg_ticket_cannot_submit_when_gate_blocks` | ORDER-UI |
| HB-073 | A mismatched posId can submit an order for the wrong instrument<br>`web/components/TickerDetailContent.tsx` | confirmed. Numeric `positionId` lookup is not checked against route ticker before passing the position to ticker order surfaces. | Require position ID and canonical ticker/account identity to match; otherwise ignore/error and disable trade. | `web/tests/ticker-chain-position-focus.test.tsx::cross_ticker_posid_cannot_reach_order_builder` | ORDER-UI |
| HB-074 | Unscoped posId can construct an order for the wrong instrument<br>`web/components/TickerWorkspace.tsx`, `TickerDetailContent.tsx` | duplicate of HB-073. Workspace forwards arbitrary query `posId` without ticker/account scope. | Apply HB-073 validation at route/workspace boundary and again before placement. | `web/e2e/ticker-page.spec.ts::foreign_posid_is_rejected_and_order_disabled` | ORDER-UI |
| HB-075 | Heuristic combo groups become destructive live-order targets<br>`web/components/WorkspaceSections.tsx`, `web/lib/openOrderCombos.ts` | duplicate of HB-107. Workspace exposes cancel/modify for heuristic groups created by the library. | Fix HB-107; require explicit correlation for actionable controls, singles otherwise. | `web/e2e/open-order-combo.spec.ts::heuristic_group_has_no_cancel_or_modify_action` | ORDER-UI |
| HB-076 | Option average volume is presented as open interest<br>`web/components/mobile/MobileChainLadder.tsx` | confirmed. Both compact and detail OI labels render `avgVolume`. | Bind OI label to open-interest field; label average volume separately or omit it. | `web/tests/mobile-chain-ladder.test.tsx::oi_label_never_reads_average_volume` | SIGNAL |
| HB-077 | Stop-order review uses a different price than the submitted order<br>`web/components/mobile/MobileOrderTicket.tsx` | confirmed. Review/notional uses `parsedPrice`; STP payload uses `parsedStop` through `ibPlaceFields`. | Use `riskPriceForOrderType()` for review/risk/success text while keeping placement fields canonical. | `web/e2e/mobile-stop-order.spec.ts::review_risk_and_submitted_stop_share_one_price` | ORDER-UI |
| HB-078 | Missing quotes can convert a credit combo into a debit order<br>`web/components/mobile/MobileOrderTicket.tsx`, `web/lib/optionsChainUtils.ts` | confirmed. `isDebit` can be indeterminate; manual fallback then treats entered magnitude as positive and sends the wrong sign. | Block while sign is unresolved or require explicit credit/debit intent validated against signed natural quote. | `web/tests/options-chain-utils.test.ts::mobile_combo_with_missing_quotes_cannot_infer_or_flip_credit_sign` | ORDER-UI |
| HB-079 | Partial live quotes produce incomplete position P&L<br>`web/components/mobile/MobilePositionList.tsx` | confirmed. `getOptionRtMv()` returns a total when `any` leg is priced, silently omitting missing active legs. | Require every economically active leg to have a finite quote or render explicitly unavailable/partial without P&L. | `web/tests/mobile-position-short-pnl.test.tsx::missing_active_leg_suppresses_complete_position_pnl` | SIGNAL |
| HB-080 | Live submission remains enabled without resolved portfolio coverage<br>`web/components/ticker-detail/FuturesOrderForm.tsx` | confirmed. `portfolio` defaults to `null`; `submitDisabled` only checks selected conId. | Preserve pending coverage and couple submit to canonical resolved risk permit. | `web/tests/futures-quote-fallback.test.ts::futures_submit_blocks_pending_or_indeterminate_portfolio` | ORDER-UI |
| HB-081 | Ticker changes can briefly submit a stale futures conId<br>`web/components/ticker-detail/FuturesOrderForm.tsx` | confirmed. `selectedConId` persists across symbol changes and selection effect only runs when it is null. | Reset selection on symbol/request generation and require selected contract symbol equals current ticker at build/submit. | `web/tests/futures-chain-api.test.ts::ticker_change_invalidates_prior_futures_contract_selection` | ORDER-UI |
| HB-082 | Coverage-indeterminate index-option orders can still be submitted<br>`web/components/ticker-detail/IndexOptionOrderForm.tsx` | confirmed. `portfolio` defaults null and submit disablement only checks selected conId. | Preserve pending/null distinction and require canonical risk permit. | `web/tests/index-options-chain-api.test.ts::index_option_submit_blocks_indeterminate_coverage` | ORDER-UI |
| HB-083 | Cross-ticker chain refresh can place the previously viewed option<br>`web/components/ticker-detail/IndexOptionOrderForm.tsx` | confirmed. Expiry/conId state survives symbol changes while two asynchronous chain requests can settle out of order. | Reset on symbol, abort/version fetches, and exact-match selected contract to active symbol+expiry at submit. | `web/tests/index-options-chain-api.test.ts::cross_ticker_out_of_order_chain_cannot_submit_stale_contract` | ORDER-UI |
| HB-084 | Orders remain submittable with indeterminate portfolio coverage<br>`web/components/ticker-detail/OptionsChainTab.tsx` | confirmed. `useOrderRisk(riskInput, portfolio ?? null)` erases pending `undefined`; place button ignores risk state. | Pass coverage state unchanged and require `riskState.okToSubmit` in handler and button. | `web/tests/options-chain-implied.test.tsx::chain_submit_requires_resolved_portfolio_risk` | ORDER-UI |
| HB-085 | Indeterminate combo pricing can submit a credit order with the wrong limit sign<br>`web/components/ticker-detail/OptionsChainTab.tsx`, `web/lib/optionsChainUtils.ts` | confirmed. When `isDebit` is null, entered price remains positive; submit can proceed despite unresolved signed quote. | Preserve signed quote, block unresolved sign, or require explicit reviewed credit/debit choice. | `web/e2e/order-combo.spec.ts::unresolved_combo_sign_blocks_submission` | ORDER-UI |
| HB-086 | Submit buttons ignore unresolved portfolio risk state<br>`web/components/ticker-detail/OrderTab.tsx` | confirmed. Both submit buttons disable on local validity/loading/naked-short warning, not canonical risk-state resolution. | Make handler and button require `riskState.okToSubmit === true`; retain pending versus absent coverage. | `web/tests/order-tab-risk-gate.test.tsx::new_and_combo_submit_require_resolved_risk_permit` | ORDER-UI |
| HB-087 | Short options default to selling more while confirmation models a buy<br>`web/components/ticker-detail/OrderTab.tsx` | confirmed. Existing positions default envelope action SELL, but short single-leg mapping changes leg action to BUY, so UI action/payload/risk semantics diverge. | Derive close action from held direction and generate display, risk, and payload from one canonical intent. | `web/tests/order-tab-close-position-sign.test.ts::short_option_default_is_buy_to_close_everywhere` | ORDER-UI |
| HB-088 | Stock orders are passed through the option risk model with no legs<br>`web/components/ticker-detail/OrderTab.tsx` | confirmed. Non-option builder yields stock input, while guard/risk flow still casts through option `OrderPayload`/empty leg assumptions. | Add canonical stock risk variant with share/notional/side fields; reject empty-leg option inputs. | `web/tests/order-risk-linear.test.tsx::stock_order_uses_stock_risk_variant_not_empty_option_legs` | RISK-CORE |
| HB-089 | Combo close orders discard held leg ratios<br>`web/components/ticker-detail/OrderTab.tsx` | confirmed. Close payload maps every leg with `ratio: 1` even though held positions can encode unequal contracts. | Normalize held leg quantities by GCD into exact ratios and use the same ratios in risk and payload. | `web/tests/order-tab-close-position-sign.test.ts::ratio_combo_close_preserves_held_leg_ratios` | ORDER-UI |
| HB-090 | Every combo SELL is classified as a risk-free close<br>`web/components/ticker-detail/OrderTab.tsx`, `web/lib/order/positionTrade.ts` | duplicate of HB-109. SELL selects `closeOut` without comparing requested quantity to held combo units. | Apply HB-109 canonical cap/split and consume it from OrderTab. | `web/tests/order-tab-close-position-sign.test.ts::oversized_combo_sell_is_not_closeout` | ORDER-UI |
| HB-091 | Order state and confirmation survive ticker changes<br>`web/components/ticker-detail/OrderTab.tsx` | confirmed. Hook state lives inside `NewOrderForm` across ticker prop changes; limit/action/confirm state is not reset or bound to ticker generation. | Key/reset intent on ticker/position identity and revalidate canonical payload immediately before submit. | `web/tests/order-tab-ticker-state.test.tsx::ticker_change_invalidates_form_and_confirmation` | ORDER-UI |
| HB-092 | Ratio-position marks are multiplied by the wrong unit count<br>`web/components/ticker-detail/PositionTab.tsx` | confirmed. Signed spread last already reflects leg ratios, then market value multiplies by `position.contracts`, which may be aggregate leg count rather than combo units. | Use canonical `heldComboUnits(position)` and normalized quote units for mark/MV. | `web/tests/position-tab-trade.test.tsx::ratio_spread_mark_uses_combo_units_once` | SIGNAL |
| HB-093 | Over-closing a combo is incorrectly presented as a riskless close<br>`web/components/ticker-detail/PositionTradeTicket.tsx`, `web/lib/order/positionTrade.ts` | duplicate of HB-109. Ticket computes held quantity but accepts larger parsed quantity and builds the same unconditional SELL closeOut. | Apply HB-109, cap close-only surface, and block handler as defense in depth. | `web/tests/position-trade.test.ts::overclose_is_blocked_before_risk_and_placement` | ORDER-UI |
| HB-094 | Stop orders pass NaN into the risk confirmation<br>`web/components/ticker-detail/PositionTradeTicket.tsx` | confirmed. STP accepts missing limit, yet builder/success text receive `parsedPrice`; placement later deletes limit and uses `parsedStop`. | Use `riskPriceForOrderType()` for builder, confirmation, and notification. | `web/tests/position-trade.test.ts::stop_order_risk_values_are_finite_and_use_stop_price` | ORDER-UI |
| HB-095 | Test-only flags can disable realtime authentication in production<br>`web/lib/RealtimeAuthContext.tsx` | confirmed. Production client code honors public/server `RADON_AUTHLESS_TEST` values without build/runtime localhost guard and supplies no token getter. | Inject authless provider only from test harness or require explicit test build plus localhost check; never honor server-only flag client-side. | `web/tests/realtime-socket-auth.test.ts::production_origin_ignores_authless_test_flags` | API-GUARD |
| HB-096 | Destructive calls bypass prerequisite reads and the order-risk chokepoint<br>`web/lib/assistant/loop.ts` | confirmed. Loop returns the first destructive call before executing sibling reads; `proposalFor()` copies unvalidated model input. | Execute/validate prerequisite reads first, reject unresolved destructive proposals, and emit only typed canonical intents for HB-101 gating. | `web/tests/assistant-loop-hardening.test.ts::destructive_call_waits_for_required_reads_and_runtime_validation` | CHAT-GUARD |
| HB-097 | Options proposals are confirmed as stock orders<br>`web/lib/assistant/tools.ts`, `web/lib/chat.ts` | duplicate of HB-100. Tool advertises options but schema has only ticker/action/quantity/structure/limit and cannot identify a contract. | Apply discriminated proposal schema and HB-100 canonical mapper; reject structure-only options. | `web/tests/assistant-tool-loop.test.ts::option_proposal_requires_complete_contract_identity` | CHAT-GUARD |
| HB-098 | Prior journal truncation can silently corrupt realized P&L<br>`web/lib/assistant/tools.ts`, `web/lib/journal/journalRangeDb.ts` | confirmed. In-window rows check `WINDOW_ROW_LIMIT`; `fetchPriorRowsForTickers()` shares a capped oldest-first 500-row result with no truncation refusal. | Cursor-page prior rows per ticker or return truncation metadata and refuse authoritative P&L. | `web/tests/assistant-journal-tools.test.ts::realized_pnl_refuses_or_pages_truncated_prior_rows` | DATA |
| HB-099 | Short-stock opens and covers are classified in the wrong direction<br>`web/lib/blotter/fromJournal.ts` | confirmed. Generic SELL is opening-false/closing-true; lot matching assigns closure to SELL and can leave cover BUY as open long. | Classify against running signed stock inventory or persist explicit stock open/close actions; attach P&L to reducing execution. | `web/tests/blotter-from-journal.test.ts::sell_then_buy_short_round_trip_opens_short_and_closes_on_cover` | DATA |
| HB-100 | Option proposals are submitted as stock orders<br>`web/lib/chat.ts`, `web/lib/assistant/tools.ts` | confirmed. `placeProposedOrder()` hardcodes `type: "stock"` and discards all option identity while using assistant summary separately. | Define runtime-validated stock/option/combo union, build via canonical payload builder, and summarize exact payload. | `web/tests/chat.test.ts::validated_option_proposal_submits_option_or_is_rejected_never_stock` | CHAT-GUARD |
| HB-101 | Assistant orders bypass the mandatory order-risk chokepoint<br>`web/lib/chat.ts`, `web/components/ChatPanel.tsx` | confirmed. Model-controlled record is posted directly to `/api/orders/place` without branded `AugmentedOrderSummary`, coverage, or `okToSubmit`. | Require canonical `OrderRiskGate` result for client confirmation and enforce quantity/notional/risk policy server-side. | `web/tests/order-risk-chokepoint.test.tsx::assistant_surface_cannot_place_without_branded_augmented_summary` | CHAT-GUARD |
| HB-102 | Stock acquisitions from assignment or exercise are incorrectly realized as closes<br>`web/lib/journal/realizedPnl.ts` | confirmed. `closeAgainstFallback()` treats any assignment/exercise code with current basis as close, including BUY delivery from flat inventory. | Use event-time inventory evidence; BUY delivery without proven short stock opens inventory, never fallback-close. | `web/tests/journal-realized-pnl.test.ts::buy_assignment_or_exercise_from_flat_opens_stock` | DATA |
| HB-103 | Every unmatched expired option is assumed to have expired worthless<br>`web/lib/journal/realizedPnl.ts` | confirmed. `synthesizeWorthlessExpiry()` needs only residual option and elapsed expiry; it does not require explicit expiry code/moneyness proof. | Synthesize only explicit worthless lapse or verified OTM settlement; otherwise retain unresolved residual. | `web/tests/journal-realized-pnl.test.ts::unmatched_expired_option_without_worthless_proof_stays_unresolved` | DATA |
| HB-104 | Stale reconciliation can overwrite a richer canonical journal row<br>`web/lib/journalDb.ts` | confirmed. Existing rows are read before a loop of unconditional `ON CONFLICT ... DO UPDATE payload`, allowing a stale source to replace concurrent canonical detail. | Import with `DO NOTHING` or transactional source-precedence merge limited to permitted fields. | `web/tests/journal-sync.test.ts::reconciliation_cannot_overwrite_richer_concurrent_journal_row` | DATA |
| HB-105 | Execution-ID matching can duplicate corrections or discard new fills<br>`web/lib/journalImport.ts` | confirmed. `execIdParts()` exact-splits composite IDs; correction roots are not normalized and any partial composite match skips the entire trade. | Normalize correction roots, supersede atomically, and import/reconcile composite constituents individually. | `web/tests/journal-sync.test.ts::corrections_supersede_and_partial_composites_keep_new_fills` | DATA |
| HB-106 | Net-flat round trips are persisted with zero quantity and no notional<br>`web/lib/journalImport.ts` | confirmed. CLOSED import derives contracts/shares and `total_cost` from absolute `net_quantity`; net-flat rows therefore store zero. | Extend source with gross buy/sell quantities/values/basis/proceeds or reject lossy row for rehydration. | `web/tests/journal-sync.test.ts::net_flat_round_trip_preserves_gross_quantity_basis_and_proceeds` | DATA |
| HB-107 | Unrelated live orders can be grouped and modified as one combo<br>`web/lib/openOrderCombos.ts` | confirmed. Group key is symbol/expiry/orderType/TIF/abs quantity; `isLikelyCombo()` lacks BAG/orderRef/OCA/parent correlation. | Actionable groups require explicit stable broker correlation; heuristic grouping, if retained, is display-only. | `web/tests/open-order-combo-modify.test.ts::uncorrelated_same_shape_orders_remain_non_actionable_singles` | API-GUARD |
| HB-108 | Absolute-value normalization corrupts credit-combo quotes<br>`web/lib/optionsChainUtils.ts` | confirmed. Signed `netBid/netAsk` are converted with `Math.abs()` before bid/ask/mid return. | Return signed natural bid/ask and arithmetic signed midpoint; remove consumer sign reconstruction. | `web/tests/options-chain-utils.test.ts::signed_debit_credit_and_zero_crossing_quotes_are_preserved` | RISK-CORE |
| HB-109 | Oversized combo sells are falsely classified as riskless closes<br>`web/lib/order/positionTrade.ts` | confirmed. Combo SELL always sets `isClosing`/`closeOut`; calculated `comboUnits` does not cap or split `quantity`. | Reject/cap above held units or split excess into reversed opening structure through full portfolio risk. | `web/tests/position-trade.test.ts::combo_sell_above_held_units_is_blocked_or_risk_models_excess` | RISK-CORE |
| HB-110 | Per-leg expiries are discarded when constructing orders<br>`web/lib/order/positionTrade.ts` | confirmed. Combo/single builders use `position.expiry` instead of `leg.expiry ?? position.expiry`, including risk input. | Resolve effective expiry per leg once and reuse in payload/risk/identity. | `web/tests/position-trade.test.ts::calendar_and_diagonal_orders_preserve_each_leg_expiry` | RISK-CORE |
| HB-111 | Portfolio collateral can be reused despite already covering open shorts<br>`web/lib/order/risk/internal/computeOrderRisk.ts` | confirmed. Augmentation indexes only LONG stock/options and never subtracts obligations from existing short legs before covering a new sell. | Compute account-scoped projected post-order portfolio and consume existing short obligations before new coverage. | `web/tests/order-risk.test.ts::encumbered_long_option_and_shares_cannot_cover_a_second_short` | RISK-CORE |
| HB-112 | Expiry-blind netting misclassifies reverse calendars as bounded<br>`web/lib/order/risk/internal/computeOrderRisk.ts` | confirmed. Boundedness groups by right/strike and ignores expiry, so an earlier long offsets a later short. | Require covering long expiry >= short expiry; evaluate each expiry boundary or mark unsupported mixed-expiry risk indeterminate. | `web/tests/complex-risk-profile.test.ts::reverse_calendar_with_earlier_long_is_unbounded_or_indeterminate` | RISK-CORE |
| HB-113 | Put structures ignore losses at intermediate strikes<br>`web/lib/order/risk/internal/computeOrderRisk.ts` | confirmed. Put loss logic evaluates only `S=0`; extrema at unique middle strikes are absent. | Evaluate signed aggregate payoff at zero, every unique strike, and infinity boundary. | `web/tests/complex-risk-profile.test.ts::put_butterflies_broken_wings_and_ratios_use_global_payoff_extrema` | RISK-CORE |
| HB-114 | Closing a protective option is incorrectly treated as risk-free<br>`web/lib/order/risk/useOrderRisk.ts` | confirmed. Every `closeOut` short-circuits to zero margin/`okToSubmit: true` without projected retained-position risk. | Apply close to portfolio copy and recompute residual risk before permitting; include exact closing leg identity/quantity. | `web/tests/order-risk.test.ts::closing_protective_long_reveals_and_blocks_new_naked_exposure` | RISK-CORE |
| HB-115 | What-if margin remains stale after order-price changes<br>`web/lib/order/risk/useWhatIfMargin.ts` | confirmed. Effect/cache key intentionally excludes price although request body includes `limitPrice`; dependencies contain only structural key. | Key/debounce/cancel using complete normalized request including effective limit price. | `web/tests/order-risk-telemetry.test.tsx::price_edit_refetches_and_cannot_reuse_stale_whatif_margin` | RISK-CORE |
| HB-116 | Four-second fallback permits duplicate real-money orders<br>`web/lib/orders/orderIdempotency.ts`, `web/app/api/orders/place/route.ts`, `scripts/api/server.py` | confirmed. Content-key success TTL is 4,000 ms and production callers lack durable user-intent keys; retry can place again. | Generate cryptographic intent key at confirmation, persist across retries, bind principal+canonical payload, and retain result durably server-side. | `web/tests/orders-route.test.ts::accepted_order_retry_after_four_seconds_returns_original_result_once` | API-GUARD |
| HB-117 | Chart assumes fields absent from the TWR payload<br>`web/lib/performanceChart.ts`, `scripts/perf_twr_builder.py`, `web/lib/types.ts` | confirmed. Chart reads `summary.starting_equity` and `series[].benchmark_close` without validated presence, then performs arithmetic that can yield NaN. | Align shared producer/schema or derive from emitted normalized TWR; reject non-finite chart inputs. | `web/tests/performance-chart-model.test.ts::actual_twr_payload_produces_only_finite_chart_geometry` | DATA |
| HB-118 | Historical realized P&L uses the latest portfolio basis<br>`web/lib/portfolio/stockBasisDb.ts`, `web/lib/journal/realizedPnl.ts` | confirmed. `fetchPortfolioStockBasis()` always queries newest snapshot and returns one basis per ticker, independent of event time. | Resolve snapshot at/before delivery timestamp or persist basis on event; account-scope lookup. | `web/tests/journal-realized-pnl.test.ts::assignment_uses_basis_as_of_delivery_not_latest_snapshot` | DATA |
| HB-119 | Missing legs produce a partial position valuation<br>`web/lib/positionUtils.ts` | confirmed. Multi-leg `resolveMarketValue()` filters null legs and sums any remaining known legs as a complete number. | Return null unless every active leg is finite, or use an explicit partial type barred from complete P&L. | `web/tests/position-pnl-sign.test.ts::multileg_market_value_is_unavailable_when_any_active_leg_is_missing` | DATA |
| HB-120 | Out-of-order requests can attach informed-flow data to the wrong ticker<br>`web/lib/useInformedFlow.ts` | confirmed. Fetch has no abort/generation; cleanup only clears interval and prior data remains while ticker changes. | Abort/version each load, clear mismatched state/loading on ticker switch, and commit only active ticker response. | `web/tests/informed-flow-panel.test.tsx::out_of_order_prior_ticker_response_is_ignored` | SIGNAL |
| HB-121 | Backfilled closes remain stale across trading sessions<br>`web/lib/usePreviousClose.ts` | confirmed. Backfill map and fetched-symbol set have hook-lifetime scope with no ET session key/invalidation. | Key cached closes/fetched set by expected ET session and clear/refetch on rollover. | `web/tests/use-previous-close-indexes.test.ts::overnight_session_rollover_invalidates_backfilled_close` | SIGNAL |
| HB-122 | Previous ticker report can be displayed under a new ticker<br>`web/lib/useTickerFlowReport.ts` | confirmed. `load()` sets status loading but does not clear/key `data`; error path leaves previous payload intact and requests are not generation-guarded. | Store `{ticker,data}`, hide immediately on switch, abort/version requests, retain failure cache only for same ticker. | `web/tests/flow-report-staleness.test.ts::ticker_switch_never_renders_or_restores_previous_ticker_report` | SIGNAL |

## Cross-file overlap and guard integration

- Backup roots: HB-001/HB-005 and HB-002/HB-006 are exact script/service duplicates. HB-007/HB-009/HB-019 require one shared maintenance lock, not three timer-only fixes.
- Assistant live-order guard: HB-056/HB-058/HB-097/HB-100 are one schema-to-placement instrument-confusion chain; HB-057/HB-059/HB-096/HB-101 must converge on the same `OrderRiskGate` plus server policy. A UI-only patch is insufficient.
- Canonical order guard: HB-050/HB-051/HB-052/HB-107/HB-116 require final server-side validation/idempotency even after UI remediation.
- Coverage fail-closed: HB-061/HB-066/HB-069/HB-072/HB-080/HB-082/HB-084/HB-086/HB-101/HB-114 all depend on preserving `undefined` (pending) versus `null` (unavailable) and requiring an explicit branded permit.
- Combo close/over-close: HB-090/HB-093 duplicate HB-109's core defect; HB-067/HB-087/HB-089 add direction/ratio correctness at callers.
- Signed combo price: HB-078/HB-085 consume HB-108's signed quote; all manual fallbacks must block unresolved sign rather than reconstruct it.
- Instrument identity races: HB-052 is the server backstop for HB-073/HB-074/HB-081/HB-083/HB-091. Every payload is re-bound to current ticker/position/contract at submission.
- Journal/accounting: HB-031/HB-045/HB-049/HB-098/HB-099/HB-102-HB-106/HB-118 require stable execution correction roots, account/event-time identity, and non-lossy handoffs.
- Partial-data fail-closed: HB-013/HB-032-HB-034/HB-047/HB-079/HB-103/HB-117/HB-119/HB-120/HB-122 must distinguish unavailable/degraded from valid empty/zero.

## Reconciliation

- Report headings inventoried: **122 / 122**.
- Dispositions: **108 confirmed**, **14 duplicate**, **0 false-positive**, **0 already-fixed**.
- Duplicate IDs retained as distinct report items: HB-005, HB-006, HB-009, HB-015, HB-019, HB-056, HB-057, HB-058, HB-059, HB-074, HB-075, HB-090, HB-093, HB-097.
- Highest-consequence first implementation boundary: `API-GUARD` + `RISK-CORE` + `ORDER-UI` + `CHAT-GUARD`, followed by `BKP`, `DATA`, `SIGNAL`, and `OPS`.

## Risks and blockers

- No inventory blocker. Several fixes cross Next.js, FastAPI, IB, and systemd boundaries; shared guards must land before deleting duplicated caller checks.
- Live-order E2E must use mocked placement. Browser verification must not click a production submit control.
- Consistent Turso backup semantics need provider capability validation; if a durable remote read transaction is unavailable, the safe implementation is provider export/snapshot plus restore verification, not best-effort pagination.
