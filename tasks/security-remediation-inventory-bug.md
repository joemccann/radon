# BUG Security Remediation Inventory (130 Findings)

Source: `/Users/joemccann/dev/apps/finance/radon/.deepsec/data/radon/reports/report.md`, `## BUG (130)` only. Current-code evidence was checked at `1bed485edf7eb2a2a20aca1be5a40002f245b086`.

## Ownership clusters

| Cluster | Owner surface |
|---|---|
| C1 | Pi extensions and tool contracts |
| C2 | Cloud/systemd and local scheduling |
| C3 | Database mirror, persistence, and knowledge storage |
| C4 | Evaluation, scanner, health, portfolio, and relay scripts |
| C5 | Newsfeed durability and media |
| C6 | Web API data correctness and availability |
| C7 | Share-card transport and browser policy |
| C8 | Workflow execution and composer |
| C9 | Dashboard, chart, and telemetry components |
| C10 | Admin and mobile interaction surfaces |
| C11 | Ticker-detail state and futures risk |
| C12 | Journal, accounting, and provider libraries |
| C13 | Order-risk, position, quote, and portfolio libraries |
| C14 | Client caches, freshness, realtime hooks, and mutation ordering |

## Finding inventory

| ID | Exact title | File(s) | Disposition | Concise current-code evidence | Proposed minimal patch | Exact regression test target | Cluster |
|---|---|---|---|---|---|---|---|
| BUG-001 | Existing NVM directory is returned as an executable | `.pi/extensions/browser-tools.ts` | Confirmed | `candidates` includes `.nvm/versions/node`; the later `existsSync` loop returns it without a file/type check. | Remove the directory candidate; validate candidates with `statSync().isFile()` and executable access before return. | `.pi/tests/browser-tools.test.ts::falls back when NVM has no agent-browser executable` | C1 |
| BUG-002 | Market-open calculation ignores holidays and early closes | `.pi/extensions/startup-protocol.ts` | Confirmed | `isMarketOpen()` checks only ET weekday and fixed minute range `570 <= t < 960`. | Delegate to the repository US market calendar and surface its session close. | `.pi/tests/startup-protocol.test.ts::market state honors holiday and early close` | C1 |
| BUG-003 | Detached startup jobs have no timeout or output bound | `.pi/extensions/startup-protocol.ts` | Confirmed | Four detached `spawn` paths concatenate unbounded stdout/stderr and complete only on `close`; no `error` or deadline path exists. | Add one bounded subprocess helper with capped buffers, spawn-error completion, deadline, process-group termination, and exactly-once callback. | `.pi/tests/startup-protocol.test.ts::startup jobs time out with bounded output and complete dependencies` | C1 |
| BUG-004 | Service timeout cannot accommodate the configured retry sequence | `cloud/services/radon-cta-sync.service`, `scripts/run_cta_sync.sh` | Confirmed | Unit uses `TimeoutStartSec=900`; wrapper permits 3 x 300-second attempts plus 120/600-second backoffs. | Define one budget and fit attempt timeout/backoffs inside a unit timeout with cleanup slack. | `scripts/tests/test_cta_sync_service.py::test_service_timeout_covers_retry_envelope` | C2 |
| BUG-005 | CTA schedule does not explicitly use UTC | `cloud/services/radon-cta-sync.timer` | Confirmed | All four `OnCalendar` values omit a timezone although comments declare UTC. | Append `UTC` to every expression. | `cloud/tests/test_systemd_services.py::test_cta_timer_is_explicit_utc` | C2 |
| BUG-006 | Database failures are reported as successful service runs | `cloud/services/radon-demo-mirror.service`, `scripts/db/mirror_market_snapshots_to_demo.js` | Duplicate of BUG-021 | The service directly invokes the mirror whose `mirrorQuery` converts read/write exceptions to `0`, so Node exits zero. | Implement BUG-021 once; retain the service-level assertion that a child failure yields a failed unit. | `cloud/tests/test_systemd_services.py::test_demo_mirror_propagates_child_failure` | C2/C3 |
| BUG-007 | Boot catch-up can lose the daily audit result before networking is ready | `cloud/services/radon-drift-audit.service`, `cloud/scripts/drift_audit.py` | Confirmed | Unit lacks `network-online.target`; clean-audit telemetry exhaustion is nonfatal in the invoked writer. | Add `Wants/After=network-online.target` and make final health-write failure nonzero or retry via a bounded timer. | `cloud/tests/test_drift_audit.py::test_clean_audit_fails_when_health_result_cannot_publish` | C2 |
| BUG-008 | Complete forecast-pipeline failure still exits successfully | `cloud/services/radon-forecast-nightly.service`, `scripts/forecasting/nightly_forecast.py` | Confirmed | Stage helpers return error sentinels, but `main()` prints the summary and always returns `0`. | Aggregate required-stage outcomes, persist health, and return nonzero when any required stage fails. | `scripts/tests/test_nightly_forecast.py::test_main_nonzero_when_all_required_stages_fail` | C2/C4 |
| BUG-009 | Failed GARCH refresh is reported as successful | `cloud/services/radon-garch.service`, `scripts/run_garch_refresh.sh` | Confirmed | After the failed fallback `if`, `EXIT_CODE=$?` captures the compound `if` status, which is zero with no `else`. | Capture the scanner status in an explicit `else` and exit with it. | `scripts/tests/test_run_garch_refresh_wrapper.py::test_api_and_fallback_failure_exit_nonzero` | C2/C4 |
| BUG-010 | Fixed UTC schedule runs before the market opens during standard time | `cloud/services/radon-leap.timer` | Confirmed | Timer is fixed at `14:00 UTC`, which is 09:00 ET during standard time. | Schedule `10:00:00 America/New_York`. | `cloud/tests/test_systemd_services.py::test_leap_timer_runs_at_ten_et_across_dst` | C2 |
| BUG-011 | Image cache keys permanently bind updated posts to stale images | `cloud/services/radon-newsfeed.service`, `scripts/newsfeed/media.js`, `scripts/newsfeed/push_media.js` | Confirmed | Downloader filename is `postId-index`; existing destinations are skipped and rsync uses `--ignore-existing`. | Content-address by source URL/content hash, download atomically, and reserve immutable caching for versioned names. | `web/tests/newsfeed-image-url.test.ts::changed source URL creates a new immutable media path` | C5 |
| BUG-012 | Timer performs 149 unnecessary portfolio syncs per trading day | `cloud/services/radon-portfolio-sync.timer`, `scripts/run_portfolio_refresh.sh` | Confirmed | `13..21:*:00` includes all of 21:00-21:59 UTC; wrapper gates only on trading date. | Use ET session scheduling and gate with authoritative `market_state().is_open`, including early close. | `scripts/tests/test_portfolio_refresh_wrapper.py::test_refresh_skips_outside_current_exchange_session` | C2/C4 |
| BUG-013 | HTTP timeout fallback can launch duplicate scanner jobs | `cloud/services/radon-signals-refresh.service`, `scripts/run_signals_refresh.sh` | Confirmed | Curl aborts at 200 seconds while backend children may run 420/480 seconds; every curl failure starts a direct duplicate. | Fall back only on connection failure; use shared single-flight/run status for accepted requests and align deadlines. | `scripts/tests/test_run_signals_refresh_wrapper.py::test_accepted_request_timeout_does_not_start_direct_scan` | C2/C4 |
| BUG-014 | Documented UTC schedule uses the host's local timezone | `cloud/services/radon-signals-refresh.timer` | Confirmed | `OnCalendar=...13..21...` has no timezone suffix despite UTC comments. | Append `UTC` or use an explicit ET schedule. | `cloud/tests/test_systemd_services.py::test_signals_timer_has_explicit_timezone` | C2 |
| BUG-015 | Timer runs VCG scans outside trading hours | `cloud/services/radon-vcg-refresh.timer`, `scripts/run_vcg_refresh.sh` | Confirmed | Timer spans 13:00-21:55 host time; wrapper uses `_is_trading_day` only. | Use explicit ET cadence and gate each run with exchange-session open state. | `scripts/tests/test_run_vcg_refresh_wrapper.py::test_vcg_skips_premarket_postclose_and_early_close` | C2/C4 |
| BUG-016 | Materialized schedule drifts when the workstation timezone changes | `config/com.radon.cri-scan.plist`, `scripts/setup_cri_service.sh` | Confirmed | Plist materializes converted local `Hour` values at install time and has no timezone-change regeneration. | Run a periodic local cadence and gate inside the wrapper against ET, or regenerate/reload on timezone change. | `scripts/tests/test_local_scheduler_reliability.py::test_cri_schedule_is_timezone_independent` | C2 |
| BUG-017 | Market-hours schedule incorrectly uses the host's local timezone | `config/com.radon.vcg-refresh.plist`, `scripts/run_vcg_refresh.sh` | Confirmed | Launchd enumerates 09:30-16:00 in host-local time; wrapper checks only the date. | Use a timezone-neutral periodic launchd cadence plus ET session gate in the wrapper. | `scripts/tests/test_local_scheduler_reliability.py::test_vcg_launchd_uses_runtime_et_session_gate` | C2/C4 |
| BUG-018 | Kelly tool accepts invalid probability and sizing domains | `lib/tools/pi-tools.ts`, `lib/tools/wrappers/kelly.ts`, `scripts/kelly.py` | Confirmed | TypeBox uses unconstrained numbers; values are forwarded and downstream rejects only nonpositive odds. | Add bounded TypeBox constraints and identical runtime/Python guards for probability, odds, fraction, and bankroll. | `scripts/tests/test_kelly_domain_guards.py::test_pi_tool_and_cli_reject_invalid_domains` | C1/C4 |
| BUG-019 | Integer controls accept values that downstream CLIs reject or mis-handle | `lib/tools/pi-tools.ts`, `lib/tools/wrappers/scanner.ts`, `lib/tools/wrappers/vcg-scan.ts` | Confirmed | `top` and `days` are `Type.Number`; negative/fractional values reach integer CLIs or negative slicing. | Use bounded `Type.Integer` and validate again at wrapper/CLI boundaries. | `lib/tools/__tests__/schemas.test.ts::rejects_fractional_and_nonpositive_count_controls` | C1 |
| BUG-020 | Arbitrary VCG proxy symbols can replace the canonical snapshot | `lib/tools/pi-tools.ts`, `lib/tools/wrappers/vcg-scan.ts`, `scripts/vcg_scan.py` | Confirmed | `proxy` is unrestricted `Type.String` and JSON mode persists every proxy under the shared snapshot. | Use an HYG/JNK/LQD literal union at every boundary and prevent noncanonical exploratory writes. | `lib/tools/__tests__/schemas.test.ts::vcg proxy is allowlisted and exploratory runs do not publish canonical state` | C1/C4 |
| BUG-021 | Database failures are reported as successful mirror runs | `scripts/db/mirror_market_snapshots_to_demo.js` | Confirmed | Both source and destination exceptions return `0`; `main()` finishes with a success log and exit zero. | Accumulate failed table names, attempt independent tables, write health error, and throw after the loop. | `scripts/lib/demoMirrorReliability.test.js::mirror exits nonzero when any required table fails` | C3 |
| BUG-022 | Advertised snapshot windows are not enforced in the destination | `scripts/db/mirror_market_snapshots_to_demo.js` | Confirmed | Source queries apply limits, while destination only `INSERT OR REPLACE`s and never prunes old rows. | Apply per-table destination retention after successful upserts in the same batch/transaction. | `scripts/lib/demoMirrorReliability.test.js::mirror enforces latest per key and history retention` | C3 |
| BUG-023 | Uncomputed structure and Kelly gates are marked passed | `scripts/evaluate.py` | Confirmed | M5/M6 placeholders set `passed=True`; `PENDING` counts as a trade for exit status. | Mark placeholders `passed=False` with pending state and return a distinct incomplete exit code. | `scripts/tests/test_evaluate.py::test_pending_structure_and_kelly_are_not_passed_or_success_exit` | C4 |
| BUG-024 | IB Gateway unit failure is incorrectly classified as an edge outage | `scripts/health_service/probes.py` | Confirmed | Every unit state enters `serving_states`, including `radon-ib-gateway.service`, before dependency degradation is evaluated. | Classify gateway unit evidence with dependencies, not serving-path units. | `scripts/tests/test_health_probe.py::test_gateway_unit_failure_degrades_complete_production_unit_set` | C4 |
| BUG-025 | Stale cached unit failures remain authoritative indefinitely | `scripts/health_service/probes.py` | Confirmed | `down` serving states are returned before `units_age_secs` is checked, so stale retained unit data wins over live probes. | Exclude stale unit evidence from up/down decisions; return unknown when no fresh evidence remains. | `scripts/tests/test_health_probe.py::test_stale_cached_unit_down_does_not_override_healthy_live_probes` | C4 |
| BUG-026 | NAV history updates can truncate or lose historical data | `scripts/ib_sync.py` | Confirmed | `_append_nav_snapshot` performs unlocked read-modify-write and opens `nav_history.jsonl` with `"w"`. | Lock the transaction and atomically replace a flushed temporary sibling using the shared atomic I/O utility. | `scripts/tests/test_nav_history.py::test_concurrent_and_interrupted_updates_preserve_history` | C4 |
| BUG-027 | Chunk batching can temporarily or permanently truncate documents | `scripts/knowledge/store.py`, knowledge ingest caller | Confirmed | `upsert_documents` prunes above each call's maximum chunk while caller batches a flat chunk stream. | Keep all chunks for a document in one call or defer pruning until an authoritative document-finalization phase. | `scripts/tests/test_knowledge_store.py::test_document_split_across_batches_is_not_pruned` | C3 |
| BUG-028 | Failed writes are not rolled back before the connection is reused | `scripts/knowledge/store.py` | Confirmed | Multi-statement upsert/delete operations call `commit()` only on success and have no exception rollback around the cached connection. | Wrap each operation in an explicit transaction; rollback and reset/discard the connection on failure. | `scripts/tests/test_knowledge_store.py::test_failed_write_rolls_back_before_connection_reuse` | C3 |
| BUG-029 | Finding payload can overwrite reserved checkpoint metadata | `scripts/lib/checkpoint.py` | Confirmed | `record_finding` spreads payload after `_hash/_key/_at`; recovery trusts persisted `_hash`. | Reject reserved keys, write reserved metadata last, and recompute hash from validated `_key` on recovery. | `scripts/tests/test_checkpoint.py::test_payload_cannot_forge_reserved_metadata_or_resume_hash` | C4 |
| BUG-030 | Incremental update batches are incorrectly treated as safely regenerable snapshots | `scripts/lib/sendBackpressure.js`, relay dirty-map caller | Confirmed | Policy labels `batch/depth-batch/tape-batch` droppable, while caller clears dirty maps before send admission and does not remerge drops. | Clear dirty entries only after send, or merge dropped latest values back and force resync after pressure. | `scripts/lib/sendBackpressure.test.js::dropped incremental batch is retained for next successful send` | C4 |
| BUG-031 | Failed database writes are not retried and can be reported as healthy | `scripts/newsfeed/cycle.js` | Confirmed | DB exceptions are swallowed after local persist; unchanged next cycle writes an `ok` heartbeat and never retries the unchanged dataset. | Persist DB-dirty state, retry full merged data until success, and block healthy heartbeat while dirty. | `web/tests/newsfeed-db-dual-write.test.ts::failed upsert stays dirty retries and cannot heartbeat ok` | C5 |
| BUG-032 | Failed media uploads are not retried | `scripts/newsfeed/cycle.js` | Confirmed | `pushMedia` runs only when `imagesUpdated`; a failed transfer leaves no state and the next hydrated cycle is unchanged. | Persist media-dirty state and run idempotent upload until complete success. | `web/tests/newsfeed-cycle-ordering.test.ts::failed media push retries without a new image change` | C5 |
| BUG-033 | Apply mode can corrupt newsfeed data during an interrupted write | `scripts/newsfeed/migrate_relative_image_urls.js` | Confirmed | Apply mode writes directly with `fs.writeFile`, truncating the live/archive file first. | Write, flush, and validate a mode-preserving temporary sibling, then atomically rename; retain backup for the migration. | `web/tests/newsfeed-migrate-relative-image-urls.test.ts::apply uses atomic replacement and preserves original on failure` | C5 |
| BUG-034 | Apply mode can corrupt scrubbed newsfeed files | `scripts/newsfeed/scrub_generic_image_attributions.js` | Confirmed | Scrub apply directly calls `fs.writeFile` on each selected JSON file. | Reuse one atomic JSON replacement helper with validation and optional backup. | `web/tests/newsfeed-scrub-generic-image-attributions.test.ts::apply is atomic across live and archive files` | C5 |
| BUG-035 | Non-atomic taxonomy updates can lose or corrupt tags | `scripts/newsfeed/taxonomy.js` | Confirmed | Promise chain serializes only one process; cross-process readers both overwrite via direct `fs.writeFile`. | Add a cross-process lock around read-merge-write and atomic fsynced rename. | `web/tests/newsfeed-taxonomy.test.ts::concurrent processes preserve both tag additions and valid JSON` | C5 |
| BUG-036 | Options-conflict scoring is unreachable in production scans | `scripts/scanner.py` | Confirmed | Fetch sets `skip_options_flow=True`; scorer reads nonexistent `options_flow.combined_bias` rather than the provider `bias`. | Fetch options when scoring needs it and map the real schema, or expose a not-checked state and remove the penalty claim. | `scripts/tests/test_scanner.py::test_conflicting_options_bias_applies_penalty_in_production_path` | C4 |
| BUG-037 | Valid but malformed cache JSON can break portfolio performance | `scripts/utils/price_cache.py` | Confirmed | `verified_load` TypeError/schema errors escape; code calls `.get` without verifying an object or field types. | Strictly validate envelope/data types, quarantine invalid entries, and treat all schema failures as misses. | `scripts/tests/test_price_cache.py::test_valid_json_with_invalid_shape_is_quarantined_cache_miss` | C4 |
| BUG-038 | Event cap discards the newest reliability transitions | `web/app/api/admin/reliability/route.ts` | Confirmed | Query orders ascending before `LIMIT 5000`, selecting oldest rows in an oversized window. | Select newest rows descending in a subquery, reorder ascending for computation, and return `truncated`. | `web/tests/admin-reliability-route.test.ts::capped_history_keeps_newest_transitions_and_marks_truncated` | C6 |
| BUG-039 | Proxy timeout is shorter than the backend backtest budget | `web/app/api/backtest/[strategy]/route.ts`, FastAPI backtest endpoint | Confirmed | Next proxy aborts at 60 seconds while backend child timeout is 180 seconds and is not cancelled on disconnect. | Prefer async job/polling; minimally align proxy deadline above backend and propagate abort to child. | `web/tests/backtest-route.test.ts::proxy_budget_covers_backend_and_propagates_abort` | C6 |
| BUG-040 | Double-decoded route parameter can delete the wrong bookmark | `web/app/api/bookmarks/[post_id]/route.ts` | Confirmed | Handler calls `decodeURIComponent` on already-decoded Next params before its `try`. | Trim the supplied param directly; validate nonempty length and preserve literal percent sequences. | `web/tests/profile-bookmarks-watchlist-api.test.ts::delete_preserves_percent_containing_bookmark_ids` | C6 |
| BUG-041 | Calendar-date override defeats session-aware GEX staleness | `web/app/api/gex/route.ts`, `web/lib/gexStaleness.ts` | Confirmed | Route passes raw ET calendar today into `isGexDataStale`, bypassing the helper's latest-session default before the closed-market guard. | Omit the override or pass `mostRecentSessionDate()`. | `web/tests/gex-staleness.test.ts::weekend_and_preopen_route_use_most_recent_session` | C6 |
| BUG-042 | Generated GEX previews are unavailable on the demo deployment | `web/app/api/gex/share/content/route.ts`, demo FastAPI share endpoint | Confirmed | Generator writes a gitignored VM-local HTML file; Vercel content route calls `readShareCard` on its own filesystem. | Return allowlisted HTML through FastAPI or shared object storage; keep filename validation. | `web/tests/gex-share.test.ts::demo_content_is_proxied_from_remote_generator_storage` | C7 |
| BUG-043 | Demo users exhaust the WebSocket ticket allowance during normal reconnects | `web/app/api/ib/ws-ticket/route.ts`, demo rate-tier middleware | Confirmed | Ticket POST has no dedicated tier and inherits tier C's five requests/day despite two single-use-ticket clients with reconnects. | Add a per-user minute bucket sized for reconnect bursts plus a daily abuse ceiling. | `web/tests/demo-rate-tier.test.ts::websocket_ticket_reconnects_use_dedicated_rate_tier` | C6/C14 |
| BUG-044 | Skew cache loader selects the newest file regardless of query variant | `web/app/api/internals/route.ts`, `web/lib/internalsSkewCache.ts` | Confirmed | Loader sorts every `internals_skew_history_*.json` by mtime without deriving or validating query identity. | Share the FastAPI key derivation or validate ticker/timeframe/delta/expiry metadata before acceptance. | `web/tests/internals-skew-route-staleness.test.ts::default_query_rejects_newer_nondefault_cache_variant` | C6 |
| BUG-045 | Internals share output is always rejected by its content route | `web/app/api/internals/share/content/route.ts`, internals FastAPI share endpoint | Confirmed | Route accepts only `tweet-internals-*`; generator invoked by internals emits `tweet-regime-*`. | Make the internals generator emit its own allowlisted type or intentionally proxy through the regime content route. | `web/tests/share-report-path.test.ts::internals_generator_and_content_allowlist_use_same_type` | C7 |
| BUG-046 | Internals previews cannot cross the Vercel-to-VM filesystem boundary | `web/app/api/internals/share/content/route.ts`, demo FastAPI share endpoint | Confirmed | Content route reads Vercel-local reports while generation occurs on the remote VM. | Proxy validated content from the VM or shared object storage instead of passing a filesystem path. | `web/tests/share-report-path.test.ts::internals_demo_preview_reads_remote_generated_content` | C7 |
| BUG-047 | Weekday-only calendar requests nonexistent holiday snapshots | `web/app/api/menthorq/cta/route.ts` | Confirmed | Local expected-date logic treats every Monday-Friday as a session and can trigger holiday syncs. | Replace the local weekday helper with repository `isUsTradingDay`/latest-session logic. | `web/tests/menthorq-cta-route.test.ts::holiday_after_close_uses_previous_trading_session_without_sync_storm` | C6 |
| BUG-048 | WebSocket ticket request is outside the three-second timeout | `web/app/api/previous-close/route.ts` | Confirmed | `buildWsUrl()` awaits ticket fetch before the WebSocket timer is created and supplies no abort signal. | Give ticket fetch its own short abort deadline and fall back to configured WS URL on expiry. | `web/tests/previous-close-yahoo-daily-array.test.ts::ticket_fetch_timeout_falls_back_within_route_budget` | C6 |
| BUG-049 | Concurrent partial updates can overwrite profile fields | `web/app/api/profile/route.ts` | Confirmed | PUT reads a full profile, merges one partial body, then upserts both columns; concurrent disjoint updates carry stale fields. | Atomically update only supplied columns or add optimistic version checking. | `web/tests/profile-bookmarks-watchlist-api.test.ts::concurrent_partial_profile_updates_preserve_disjoint_fields` | C6 |
| BUG-050 | The enforced CSP blocks the served preview's interactive controls | `web/app/api/regime/share/content/route.ts`, `scripts/generate_regime_share.py` | Confirmed | Generated HTML contains an unnonced inline control script while middleware enforces nonce-only `script-src`. | Move actions to trusted React controls or emit a CSP-compatible external/nonced implementation. | `web/tests/regime-share.test.ts::served_preview_has_no_blocked_inline_control_script` | C7 |
| BUG-051 | Old FRED observations are labeled current and cached for 24 hours | `web/app/api/risk-free-rate/route.ts` | Confirmed | Parser returns the last numeric row without checking its observation date and sets public one-day cache headers. | Parse the date, enforce a weekend/holiday age allowance, and return labeled stale fallback otherwise. | `web/tests/risk-free-rate-route.test.ts::stale_numeric_fred_observation_is_not_marked_current_or_publicly_cached` | C6 |
| BUG-052 | Fresh database results carry disk-only freshness metadata | `web/app/api/scanner/strength/route.ts` | Confirmed | Source selection uses `result.timestampMs/result.fresh`, but response `cache_meta` is always built from disk mtime. | Build metadata from the selected result; use disk mtime only when disk won. | `web/tests/strength-confirmation-route.test.ts::fresh_turso_result_uses_database_freshness_metadata` | C6 |
| BUG-053 | Failed targeted scans can return unrelated cached results as success | `web/app/api/scanner/strength/scan/route.ts` | Confirmed | Any upstream error returns arbitrary disk cache with 200; request universe/tickers are not matched. | Permit transient fallback only for matching cache identity; otherwise preserve failure and expose `scan_succeeded:false`. | `web/tests/strength-confirmation-route.test.ts::failed_targeted_scan_rejects_mismatched_cache` | C6 |
| BUG-054 | Failed scans are reported as successful when any cache exists | `web/app/api/scanner/theta/scan/route.ts`, theta UI client | Confirmed | All upstream failures, including 4xx, fall back to cached 200; UI checks only `response.ok`. | Preserve validation/auth status, restrict stale fallback to transient matching requests, and require client success flag. | `web/tests/theta-harvester-route.test.ts::failed_scan_does_not_turn_any_cache_into_success` | C6 |
| BUG-055 | Expired skew snapshots are served as current data | `web/app/api/skew/route.ts` | Confirmed | `dbFirstRead` supplies `fresh:false`, but handler returns data whenever `result.ok`. | Reject expired data or return explicit stale envelope consumed by the panel. | `web/tests/skew-api.test.ts::expired_snapshot_is_not_returned_as_current` | C6 |
| BUG-056 | Expired two-day skew snapshots are served as current data | `web/app/api/skew2d/route.ts` | Confirmed | Handler discards `result.fresh` and exposes an arbitrarily old payload normally. | Apply the same stale contract as BUG-055 and show full stale timestamp if retained. | `web/tests/skew2d-api.test.ts::expired_snapshot_is_missing_or_explicitly_stale` | C6 |
| BUG-057 | UW and Exa requests can stall the route for minutes | `web/app/api/ticker/info/route.ts` | Confirmed | Three UW fetches and Exa fetch have no `AbortSignal`; cache fast/miss paths await them. | Apply bounded provider deadlines and preserve last-known-good cache on timeout. | `web/tests/ticker-info-cache.test.ts::provider_timeout_returns_last_known_good_within_budget` | C6 |
| BUG-058 | All news provider fetches lack response deadlines | `web/app/api/ticker/news/route.ts` | Confirmed | UW and both Yahoo fetches omit abort signals, so a stalled first provider blocks fallback. | Add per-provider abort deadlines and continue fallback on timeout. | `web/tests/ticker-news-route.test.ts::stalled_provider_times_out_and_advances_fallback` | C6 |
| BUG-059 | Webhook retries can reactivate revoked trials | `web/app/api/webhooks/clerk/route.ts`, `web/lib/demo/demoUsers.ts` | Confirmed | `svix-id` is verified but not persisted; every retry calls a provision upsert that resets status/timestamps. | Store processed event IDs transactionally and make duplicates no-ops; preserve revoked/expired state. | `web/tests/demo-svix-verify.test.ts::duplicate_user_created_event_cannot_reactivate_revoked_trial` | C6/C12 |
| BUG-060 | Truthy non-boolean values bypass strict order confirmation semantics | `web/app/api/workflow/run/route.ts` | Confirmed | `Boolean(body.confirm_order)` treats string `"false"`, arrays, and objects as confirmation. | Validate JSON type and accept only `body.confirm_order === true`. | `web/tests/workflow-api.test.ts::confirm_order_requires_literal_json_true` | C8 |
| BUG-061 | Unbounded, uncoalesced Yahoo requests can stampede on cache misses | `web/app/api/yield-curve/live/route.ts` | Confirmed | Two fetches have no deadlines; cache is populated after both and there is no in-flight/negative cache. | Add timeout, shared in-flight refresh, stale LKG, and short negative caching. | `web/tests/yield-curve-live-api.test.ts::concurrent_cache_misses_coalesce_and_timeout_to_stale_data` | C6/C14 |
| BUG-062 | Cancelling order confirmation still executes the workflow | `web/app/workflow/WorkflowComposer.tsx`, workflow API/executor | Confirmed | Declined `window.confirm` still calls `runWorkflow(false)`; server may run earlier nodes before the order gate. | Return immediately in UI and preflight the complete graph server-side before executing any node. | `web/tests/workflow-composer.test.ts::declined_order_confirmation_executes_no_nodes` | C8 |
| BUG-063 | Loaded workflows can receive duplicate node IDs | `web/app/workflow/WorkflowComposer.tsx` | Confirmed | Module counter starts at zero and is not advanced/checked after loading saved IDs. | Use `crypto.randomUUID()` or allocate against the current node set after load. | `web/tests/workflow-composer.test.ts::adding_after_load_never_duplicates_existing_node_id` | C8 |
| BUG-064 | The palette cannot construct an executable order workflow | `web/app/workflow/WorkflowComposer.tsx`, workflow node contracts | Confirmed | Palette provides scanner rows and a structure-only order node; required action, quantity, and option contract fields cannot be authored. | Add a validated order-config node with the complete contract, or remove order execution until supported. | `web/tests/workflow-composer.test.ts::palette_order_workflow_builds_valid_executable_contract` | C8 |
| BUG-065 | BPI fetch failures are presented as missing scan data | `web/components/BpiPanel.tsx` | Confirmed | `useBpi().error` is ignored; null data after failure renders the normal no-data timer message. | Render a distinct failure state before the successful missing-data state. | `web/tests/bpi-panel.test.tsx::initial_fetch_error_is_not_rendered_as_missing_scan` | C9 |
| BUG-066 | Missing COR observations are rendered as zero | `web/components/CorPanel.tsx`, minimap component | Confirmed | Minimap values map nullable COR observations with `?? 0`, unlike the gap-preserving main chart. | Permit nulls, exclude them from scale, and split the path at gaps. | `web/tests/cor-panel.test.tsx::minimap_preserves_null_observation_gaps` | C9 |
| BUG-067 | Live tooltip updates trigger complete SVG redraws | `web/components/CriHistoryChart.tsx` | Confirmed | Nonempty `liveValues` clones `chartData` every render; D3 effect depends on that identity while tooltip events set state. | Memoize data from history/live inputs and isolate tooltip state from the draw effect. | `web/tests/cri-history-chart-axis.test.ts::tooltip_motion_does_not_rebuild_svg` | C9 |
| BUG-068 | Pagination calculations use inconsistent post collections | `web/components/DashboardNewsFeed.tsx` | Confirmed | Page/range use `filteredPosts`, total uses `posts.length`, and lightbox page index uses image-only `navigablePosts`. | Use `filteredPosts` consistently for totals and target-page lookup. | `web/tests/dashboard-newsfeed-pagination.test.tsx::filtered_totals_and_lightbox_page_use_same_collection` | C9 |
| BUG-069 | Production client component consumes authless test switches | `web/components/DemoWelcomeModal.tsx` | Confirmed | Production client reads both authless-test environment flags and suppresses Clerk-backed behavior. | Remove flags from production component; inject/mock auth only from test harness. | `web/tests/demo-welcome-modal.test.tsx::production_component_has_no_authless_flag_branch` | C9/C14 |
| BUG-070 | Mixed IB and approximated delta legs are labeled as fully IB-sourced | `web/components/ExposureBreakdownModal.tsx`, `web/lib/exposureBreakdown.ts` | Confirmed | Producer sets source `ib` when any leg is IB; modal warns only when aggregate source equals `approx`. | Track `mixed` or `hasApprox` across every leg and warn whenever fallback contributes. | `web/tests/exposure-breakdown-modal-leverage.test.tsx::mixed_delta_provenance_shows_approximation_warning` | C9 |
| BUG-071 | Fill timestamps use the browser timezone instead of ET | `web/components/FillsModal.tsx` | Confirmed | `toLocaleTimeString` omits `timeZone`, while selection uses an ET day boundary. | Reuse the shared executed-fill ET formatter and handle invalid timestamps. | `web/tests/fills-modal-et-day-cut.test.tsx::fill_times_render_in_et_outside_et_browser_timezone` | C9 |
| BUG-072 | Mixed delayed and live quotes can appear entirely real-time | `web/components/FuturesStrip.tsx`, `web/components/WorkspaceShell.tsx` | Confirmed | Quote model has no per-cell provenance and global delayed badge appears only when every root lacks live data. | Carry source/delayed per quote and badge each fallback cell, or badge strip when any is delayed. | `web/tests/futures-strip.test.tsx::partial_live_outage_labels_each_delayed_fallback_quote` | C9 |
| BUG-073 | Missing gamma observations are plotted as fabricated values | `web/components/GammaRotationPanel.tsx` | Confirmed | `yFor` maps null/nonfinite values to fixed y=120 and path emits every row. | Emit contiguous finite path segments and omit missing samples. | `web/tests/gamma-rotation-panel.test.tsx::null_gamma_values_break_paths_without_fabricated_points` | C9 |
| BUG-074 | Gamma chart labels become incorrect outside the ±3σ range | `web/components/GammaRotationPanel.tsx` | Confirmed | Domain expands beyond ±3 but boundary labels remain hard-coded `+3σ/-3σ`. | Derive ticks from computed domain and render ±3 reference lines separately. | `web/tests/gamma-rotation-panel.test.tsx::labels_follow_expanded_zscore_domain` | C9 |
| BUG-075 | Day P&L modal discards the displayed fallback value | `web/components/MetricCards.tsx` | Confirmed | Card resolves client day-move fallback; modal receives raw `acct.daily_pnl` and shows unavailable. | Compute effective Day P&L once and share it between card and modal. | `web/tests/day-pnl-premarket-fallback.test.tsx::day_pnl_modal_matches_card_fallback_value` | C9 |
| BUG-076 | Fractional or exponent quantities are silently truncated | `web/components/ModifyOrderModal.tsx` | Confirmed | Quantity and ratios use `parseInt`, so `1.9` and `1e2` become integers before validation. | Parse with `Number`, require finite positive integer, and honor input step validity. | `web/tests/modify-order-close-pnl.test.tsx::fractional_and_exponent_quantities_are_rejected_not_truncated` | C9/C13 |
| BUG-077 | Expiration filter persists across ticker changes | `web/components/OptionsExposurePanel.tsx` | Confirmed | `expirationDates` state is not reset/reconciled when normalized symbol changes. | Reset or intersect selection with the new payload's expirations on symbol change. | `web/tests/options-exposure-panel.test.tsx::ticker_rerender_resets_incompatible_expiration_filter` | C9 |
| BUG-078 | Signed credit values can be discarded and labeled as debits | `web/components/PriceChart.tsx`, price-history resolver | Confirmed, dormant | Spread-net contract permits negatives, but resolver accepts only `last > 0`; zero/loading is labeled net debit. No production caller currently reaches it. | Pass value kind into resolver, allow finite signed spread values, and represent unavailable as null. | `web/tests/price-chart-spread.test.tsx::unmocked_negative_last_without_book_renders_net_credit` | C9/C13 |
| BUG-079 | Preset chart ranges stop following newly appended sessions | `web/components/RegimeRelationshipView.tsx` | Confirmed | Preset range is stored as absolute indexes; history-length effect only clamps out-of-bounds indexes. | Recompute active presets when length changes; preserve anchors only for custom brushes. | `web/tests/regime-relationship-zoom.test.tsx::preset_range_tracks_newly_appended_latest_session` | C9 |
| BUG-080 | Iframe policy blocks share-preview actions | `web/components/ShareReportModal.tsx` | Confirmed | Blob iframe sandbox has only `allow-scripts`; opaque origin lacks clipboard delegation and downloads lack `allow-downloads`. | Move copy/download actions into the trusted parent while keeping iframe origin isolation. | `web/tests/share-report-path.test.ts::share_actions_are_parent_owned_and_work_with_isolated_iframe` | C7/C9 |
| BUG-081 | Out-of-order WebSocket replies can replace the current search | `web/components/TickerSearch.tsx` | Confirmed | Multiple requests remain outstanding and handler accepts every `searchResults` without checking returned pattern against current query. | Track latest normalized pattern/sequence and discard stale replies. | `web/tests/ticker-search.test.ts::out_of_order_search_reply_cannot_replace_current_query` | C9/C14 |
| BUG-082 | Calendar-minute grouping conflates unrelated executions | `web/components/WorkspaceSections.tsx` | Confirmed | Fills group only by symbol plus truncated calendar minute and opening/closing direction. | Prefer backend orderRef/permId/parent ID; use true elapsed-time proximity only as fallback. | `web/tests/workspace-orders-implied.test.tsx::execution_groups_use_durable_order_identity_not_calendar_minute` | C9/C12 |
| BUG-083 | Multi-fill BAG orders display only the first fill price | `web/components/WorkspaceSections.tsx` | Confirmed | BAG group sums quantities but copies `bagFills[0].avgPrice` into `netPrice`. | Compute quantity-weighted average over all valid BAG fills and mark incomplete aggregates unavailable. | `web/tests/workspace-orders-implied.test.tsx::multi_fill_bag_uses_quantity_weighted_execution_price` | C9/C12 |
| BUG-084 | API failure is misclassified as a stopped gateway | `web/components/admin/Ib2faControls.tsx` | Confirmed | `apiUnreachable` unconditionally overwrites gateway state with `stopped`. | Represent control-plane failure as unknown, disable power actions, and infer stopped only from affirmative evidence. | `web/tests/ib2fa-controls.test.tsx::api_failure_shows_unknown_and_disables_power_actions` | C10 |
| BUG-085 | Failed or absent power callbacks produce successful optimistic state | `web/components/admin/Ib2faControls.tsx`, production callback owner | Confirmed | Callbacks are optional/void; resolved no-op or swallowed HTTP error still sets optimistic start/stop state. | Require explicit success/rejection contract and update optimistic state only after confirmed success. | `web/tests/ib2fa-controls.test.tsx::absent_or_failed_power_callback_never_changes_gateway_state` | C10 |
| BUG-086 | Missing freshness timestamp is reported as just updated | `web/components/admin/SystemStatusBar.tsx` | Confirmed | `fmtAgo(null)` returns `just now`, although null means no successful update. | Render freshness unknown/never updated and retain degraded tone until a poll succeeds. | `web/tests/system-status-bar.test.tsx::null_update_age_is_never_reported_as_just_now` | C10/C14 |
| BUG-087 | Scanner fetch failures are reported as valid empty scans | `web/components/dashboard/ScannerHero.tsx` | Confirmed | Both hooks expose errors, but component checks only loading/data and renders no-candidates for null after failure. | Render active scanner error before evaluating a successful empty result. | `web/tests/scanner-hero.test.tsx::scanner_error_is_distinct_from_zero_candidates` | C9 |
| BUG-088 | Pointer capture prevents drag-to-dismiss handlers from receiving events | `web/components/mobile/BottomSheet.tsx` | Confirmed | Parent `sheetRef` captures pointer while move/up/cancel handlers live on the child handle bar. | Capture on `event.currentTarget` or move handlers to capture owner; release and clear on up/cancel. | `web/tests/mobile-bottom-sheet.test.tsx::captured_drag_moves_and_dismisses_then_clears_state` | C10 |
| BUG-089 | Mobile journal ignores canonical total cost | `web/components/mobile/MobileJournalList.tsx` | Confirmed | Mobile entry metric reads `entry_cost` only while shared type/desktop prefer `total_cost`. | Resolve `total_cost ?? entry_cost ?? null`. | `web/e2e/mobile-blotter.spec.ts::mobile_entry_uses_canonical_total_cost` | C10/C12 |
| BUG-090 | Combo modification silently fails after an order refresh | `web/components/mobile/MobileOrderList.tsx`, parent row resolver | Confirmed | Sheet retains old row/orders array; parent matches current combo row by array reference equality. | Store stable permIds/row ID and resolve current row immediately before action; close if absent. | `web/tests/mobile-order-list-display.test.tsx::modify_resolves_refreshed_combo_by_stable_order_ids` | C10/C13 |
| BUG-091 | Existing futures holdings are ignored by risk calculations | `web/components/ticker-detail/FuturesOrderForm.tsx` | Confirmed | Linear risk input omits held long/short quantities and close-out basis even with portfolio data. | Match selected conId holdings and populate held quantities plus close-out data. | `web/tests/order-risk-linear.test.tsx::futures_sell_and_cover_use_existing_holding_quantities` | C11/C13 |
| BUG-092 | Ticker changes can display news for the previous symbol | `web/components/ticker-detail/NewsTab.tsx` | Confirmed | A component-wide `fetched` flag survives ticker changes; requests are not aborted or identity-checked. | Key/reset state by ticker and abort or generation-check stale requests. | `web/tests/news-tab.test.tsx::ticker_change_fetches_new_news_and_ignores_old_response` | C11/C14 |
| BUG-093 | Seasonality data is not invalidated when the ticker changes | `web/components/ticker-detail/SeasonalityTab.tsx` | Confirmed | `fetched` and month data persist across ticker prop changes; in-flight results have no ticker guard. | Reset ticker-scoped state and abort/generation-check requests. | `web/tests/seasonality-tab.test.tsx::ticker_change_invalidates_data_and_ignores_stale_response` | C11/C14 |
| BUG-094 | Missing months are counted as unfavorable observations | `web/components/ticker-detail/SeasonalityTab.tsx` | Confirmed | Missing months are padded with `years=0` and 0% positive, then every padded month enters `overallRating`. | Exclude nonobserved months and return insufficient-data/neutral below a sample threshold. | `web/tests/seasonality-tab.test.tsx::missing_months_do_not_count_as_unfavorable` | C11 |
| BUG-095 | Test flag can forge a healthy IB status outside tests | `web/lib/IBStatusContext.tsx` | Confirmed | Production provider reads authless flags and hard-codes authenticated/healthy/connected while skipping real polling. | Remove the production branch; inject a mocked provider from the test harness. | `web/tests/ib-status-context.test.ts::production_provider_cannot_forge_health_from_authless_flags` | C14 |
| BUG-096 | Duplicate resolution can discard the authoritative journal row | `web/lib/blotter/fromJournal.ts`, blotter query | Confirmed | Equal exec-part duplicates resolve by later array index; query order is filled-time only and omits write ordering, so richer rehydrate row can lose. | Rank authoritative P&L fields first, then explicit `written_at`/monotonic ID selected and ordered by SQL. | `web/tests/blotter-from-journal.test.ts::dedupe_prefers_authoritative_rehydrate_row_deterministically` | C12 |
| BUG-097 | Invalidation can be undone by an older in-flight read | `web/lib/dbCache.ts` | Confirmed | Invalidation deletes stored value only; pre-invalidation promise can later `store.set` and new callers can join it. | Add per-key generation and only join/store/delete the current generation's promise. | `web/tests/db-cache.test.ts::invalidation_prevents_older_inflight_read_from_repopulating_cache` | C14 |
| BUG-098 | Future timestamps are treated as fresh | `web/lib/dbFirstRead.ts` | Confirmed | Source ordering accepts future timestamps and freshness test only checks age below max, so negative age passes. | Reject timestamps beyond small allowed skew before source selection/freshness. | `web/tests/db-first-read.test.ts::future_database_and_disk_timestamps_are_invalid_or_stale` | C14 |
| BUG-099 | Duplicate webhook delivery can reactivate revoked trials | `web/lib/demo/demoUsers.ts`, `web/app/api/webhooks/clerk/route.ts` | Duplicate of BUG-059 | Provision conflict update resets timestamps/status/revocation; route calls it for every unpersisted signed retry. | Implement under BUG-059, including defense-in-depth preservation of revoked/expired rows in the upsert. | `web/tests/demo-users.test.ts::provision_retry_preserves_revoked_and_expired_trial_state` | C12/C6 |
| BUG-100 | Duplicate fills prevent correct re-open detection | `web/lib/entryDates.ts` | Confirmed | Signed-quantity walk includes every journal row without durable execution dedupe; duplicated open prevents net-flat boundary. | Carry execution identity and reconcile duplicates before episode walking. | `web/tests/entry-dates.test.ts::duplicated_open_then_close_then_reopen_starts_new_episode` | C12 |
| BUG-101 | Malformed fill price crashes toast processing | `web/lib/fillToasts.ts` | Confirmed | Non-null unvalidated `avgPrice` receives `.toFixed`; key is marked seen only after formatting. | Require finite numeric price before formatting; otherwise omit price text and still process safely. | `web/tests/fill-toasts.test.ts::malformed_avg_price_does_not_throw_or_repeat_forever` | C12 |
| BUG-102 | Seen-fill cap retains the oldest executions | `web/lib/fillToasts.ts` | Confirmed | Newest-first feed populates Set in that order, then persistence retains final 300 entries, which are oldest. | Maintain explicit recency order or reverse input before insertion and cap newest keys. | `web/tests/fill-toasts.test.ts::seen_cap_persists_newest_300_across_remount` | C12 |
| BUG-103 | CME holiday and early-close sessions are treated as open | `web/lib/futuresSession.ts` | Confirmed | `isGlobexOpen` models weekday/maintenance only and explicitly omits CME holidays; quote age is not coupled. | Use authoritative CME calendar and mark quotes stale when older than active session. | `web/tests/futures-session.test.ts::cme_full_holiday_and_early_close_are_closed` | C14 |
| BUG-104 | Invalid calendar dates are silently normalized | `web/lib/holdTime.ts` | Confirmed | Regex checks shape only; multi-argument `Date` normalizes impossible month/day combinations. | Validate ranges and round-trip constructed date components before acceptance. | `web/tests/hold-time.test.ts::invalid_date_only_values_are_rejected_not_normalized` | C12 |
| BUG-105 | Open journal rows with zero realized P&L are classified as closed | `web/lib/journal/rangePnl.ts` | Confirmed | `isClosedTrade` treats every finite `realized_pnl`, including zero on residual open rows, as closure evidence. | Require nonzero realized P&L or explicit close action/date/positive realized quantity. | `web/tests/journal-range-pnl.test.ts::zero_realized_pnl_open_row_remains_open` | C12 |
| BUG-106 | Future-ended report windows prematurely expire live positions | `web/lib/journal/realizedPnl.ts` | Confirmed | Expiry synthesis uses `opts.to` as as-of date even when later than current ET day. | Clamp synthesis to current ET day or separate validated `asOf <= today` from report end. | `web/tests/journal-realized-pnl.test.ts::future_report_end_cannot_synthesize_future_expiry` | C12 |
| BUG-107 | Sell imports add commission instead of subtracting it from proceeds | `web/lib/journalImport.ts` | Confirmed | `calcTotalCost` always computes gross plus commission; downstream treats sell `total_cost` as cash proceeds. | Make cash calculation action-aware: buy gross+commission, sell gross-commission; prefer explicit proceeds. | `web/tests/journal-import.test.ts::sell_total_cost_is_net_proceeds_after_commission` | C12 |
| BUG-108 | Gemini silently disables assistant tool calling | `web/lib/llm/provider.ts` | Confirmed | Gemini request omits tool declarations and text-only conversion drops tool-use/result blocks while returning a normal response. | Implement Gemini function declarations/call-response mapping, or reject tool-bearing Gemini requests to trigger fallback. | `web/tests/llm-provider.test.ts::gemini_tool_request_is_supported_or_explicitly_falls_back` | C12 |
| BUG-109 | Payload guard accepts malformed nested provider data | `web/lib/optionsExposure.ts` | Confirmed | Guard checks arrays but not nested expiration/level element shapes, cell numeric fields/indexes, units, or aligned lengths. | Validate the full nested schema before accepting the payload. | `web/tests/options-exposure-transform.test.ts::payload_guard_rejects_malformed_nested_provider_data` | C12 |
| BUG-110 | Partial combo closes use the full position cost basis | `web/lib/order/positionTrade.ts` | Confirmed | Combo close always passes full `resolveEntryCost(position)` regardless of close quantity versus held combo units. | Scale signed basis by `closeQuantity / heldComboUnits` for pure partial closes. | `web/tests/position-trade.test.ts::partial_combo_close_scales_entry_cost_basis` | C13 |
| BUG-111 | Any same-ticker position suppresses the short-locate warning | `web/lib/order/risk/OrderRiskGate.tsx` | Confirmed | Locate check is disabled when any portfolio position shares ticker, regardless of stock instrument, direction, or quantity. | Compute post-order net stock quantity and suppress only the fully covered portion. | `web/tests/order-risk-chokepoint.test.tsx::unrelated_option_or_insufficient_shares_do_not_suppress_locate` | C13 |
| BUG-112 | The gate updates parent state during render | `web/lib/order/risk/OrderRiskGate.tsx` | Confirmed | Component synchronously invokes `onState(state)` during render; callers pass parent setters. | Move notification to `useEffect([state, onState])`. | `web/tests/order-risk-chokepoint.test.tsx::gate_reports_state_after_commit_without_render_update_warning` | C13 |
| BUG-113 | Missing stock cost basis is treated as zero | `web/lib/order/risk/internal/computeOrderRisk.ts` | Confirmed | Missing/nonfinite `avg_cost` becomes zero while shares still provide covered-call coverage and dollar max-loss math. | Keep coverage boundedness but return indeterminate dollar max loss without verified finite nonnegative basis. | `web/tests/order-risk.test.ts::covered_call_with_missing_stock_basis_has_unknown_dollar_max_loss` | C13 |
| BUG-114 | What-if margin cache identity omits price and portfolio state | `web/lib/order/risk/internal/whatIfKey.ts`, `web/lib/order/risk/useWhatIfMargin.ts` | Confirmed; overlaps HIGH_BUG what-if price staleness | Key excludes signed premium and portfolio/account revision; caller caches/effects solely by this key. | Include signed limit price and stable account/portfolio revision; separately debounce and expire broker results. | `web/tests/whatif-margin.test.tsx::price_and_portfolio_revision_change_whatif_cache_identity` | C13 |
| BUG-115 | Credit-combo what-if requests are rejected downstream | `web/lib/order/risk/internal/whatIfKey.ts`, `scripts/ib_place_order.py` | Confirmed | Web preserves negative BUY-envelope combo price; Python validator rejects every `limitPrice <= 0` before `whatIfOrder`. | Accept finite nonzero signed BAG prices while retaining positive-only validation for non-combos. | `scripts/tests/test_ib_whatif_margin.py::test_credit_combo_accepts_negative_signed_limit_price` | C13/C4 |
| BUG-116 | Position-flipping orders are incorrectly labeled CLOSE | `web/lib/orders/orderDisplay.ts` | Confirmed | Intent compares action to held direction but never compares order quantity with matching held quantity. | Resolve held quantity and return CLOSE only within it; classify excess as MIXED/OPEN. | `web/tests/orders-display.test.ts::overclosing_order_is_mixed_not_close` | C13 |
| BUG-117 | Stock held in a covered-call position is ignored | `web/lib/orders/orderDisplay.ts` | Confirmed | `findStockDirection` skips every multi-leg position, excluding the stock leg inside covered calls. | Aggregate matching stock legs across positions with quantity/account scope. | `web/tests/orders-display.test.ts::covered_call_stock_leg_informs_stock_order_intent` | C13 |
| BUG-118 | Ticker-only map conflates multiple stock positions or accounts | `web/lib/portfolio/stockBasisDb.ts` | Confirmed | Basis map is keyed only by ticker and overwritten per position; account and quantity are discarded. | Key by account+ticker, retain share quantity, and reject ambiguous event matches. | `web/tests/stock-basis-db.test.ts::multi_account_basis_is_account_scoped_and_not_last_write_wins` | C12/C13 |
| BUG-119 | Portfolio contract conversion ignores per-leg expiry | `web/lib/pricesProtocol.ts`, Workspace subscription caller | Confirmed | `portfolioLegToContract` accepts position expiry only although each leg can carry its own expiry. | Prefer `leg.expiry` over position expiry and align with `legPriceKey`. | `web/tests/pricesProtocol.test.ts::calendar_and_diagonal_subscriptions_preserve_per_leg_expiry` | C13 |
| BUG-120 | Future timestamps are treated as freshly current | `web/lib/probeFreshness.ts` | Confirmed | `ageSecsFrom` clamps any negative age to zero, making arbitrary future timestamps fresh. | Allow small explicit skew only; otherwise return unproven/stale. | `web/tests/probe-freshness.test.ts::future_probe_timestamp_beyond_skew_is_not_fresh` | C14 |
| BUG-121 | Prior-session last can be labeled as a live trade | `web/lib/quoteTelemetry.ts` | Confirmed | Any non-null `last` makes `hasLiveQuote` true without timestamp/session/book freshness; relay can retain last while clearing stale bid/ask. | Include quote freshness/session context and label stale/closed values CLOSE. | `web/tests/quote-telemetry-fallback.test.ts::prior_session_last_without_fresh_book_is_labeled_close` | C14/C13 |
| BUG-122 | Market-state transitions falsely mark scheduled services stale | `web/lib/serviceHealthWindows.ts` | Confirmed | Staleness applies the new phase's short window to full wall-clock age immediately at premarket/open transitions. | Make windows schedule-aware with RTH-only extended rules and an open-bell grace based on elapsed active session time. | `web/tests/service-health-windows.test.ts::premarket_and_monday_open_transitions_do_not_false_stale_scheduled_jobs` | C14 |
| BUG-123 | Overlapping refreshes can restore stale alert rules | `web/lib/useAlerts.ts` | Confirmed | Each mutation starts unordered uncancelled loads; older GET may commit after post-mutation refresh. | Use request generation/abort or serialize mutations and canonical refreshes. | `web/tests/use-alerts.test.tsx::older_refresh_cannot_restore_pre_mutation_rules` | C14 |
| BUG-124 | Concurrent optimistic mutations can overwrite committed bookmark state | `web/lib/useBookmarks.ts` | Confirmed | Failure rollback restores a whole-cache snapshot; successful POST may join a pre-mutation in-flight GET. | Serialize or roll back only affected ID, generation-gate reads, and force a new post-mutation fetch. | `web/tests/use-bookmarks.test.tsx::failed_mutation_does_not_erase_concurrent_success` | C14 |
| BUG-125 | A transient initial fetch failure permanently marks bookmarks as loaded | `web/lib/useBookmarks.ts` | Confirmed | Initial fetch swallows errors and sets global `loaded=true` in `finally`; no error/retry is exposed. | Set loaded only on success, retain error, and provide bounded retry. | `web/tests/use-bookmarks.test.tsx::initial_failure_remains_retryable_and_not_loaded` | C14 |
| BUG-126 | Local storage is read during the initial client render | `web/lib/useColumnVisibility.ts` | Confirmed | Server initializer returns defaults while initial browser initializer reads persisted settings, producing different markup. | Initialize defaults on both sides and load/merge storage in a post-mount effect. | `web/tests/column-visibility.test.tsx::persisted_columns_hydrate_after_matching_initial_render` | C14 |
| BUG-127 | Fallback index quotes never refresh | `web/lib/useIndexQuoteFallback.ts` | Confirmed | Effect depends only on stable missing-symbol set, so each Yahoo fallback is fetched once during a prolonged relay outage. | Poll at a bounded interval and expire/clear stale fallback entries. | `web/tests/index-quote-fallback.test.ts::missing_index_quotes_refresh_and_expire_during_relay_outage` | C14 |
| BUG-128 | Failed and partial previous-close responses permanently suppress retries | `web/lib/usePreviousClose.ts` | Confirmed | Symbols enter `fetchedRef` before request; non-OK and omitted symbols are not cleared unless fetch/JSON rejects. | Reject non-OK and remove all missing/invalid requested symbols with bounded backoff. | `web/tests/use-previous-close-indexes.test.ts::http_failure_and_partial_response_leave_symbols_retryable` | C14 |
| BUG-129 | Concurrent watchlist mutations can restore stale state | `web/lib/useWatchlist.ts` | Confirmed | Whole-cache rollback can erase another success; post-mutation refresh can reuse a pre-mutation in-flight GET. | Serialize/generation-gate operations, roll back affected symbol only, and force fresh canonical read. | `web/tests/use-watchlist.test.tsx::late_failure_cannot_erase_concurrent_success_or_reuse_old_read` | C14 |
| BUG-130 | Yahoo candle fallback reports the latest bar as session OHLCV | `web/lib/yahooQuote.ts` | Confirmed | `latestPositive` is applied independently to intraday open/high/low/volume arrays instead of session aggregation. | Use first valid open, max high, min low, summed volume, and latest close. | `web/tests/previous-close-yahoo-daily-array.test.ts::intraday_candles_aggregate_to_session_ohlcv` | C14 |

## Reconciliation

- Total BUG findings inventoried: **130 / 130**.
- Dispositions: **128 confirmed** (including one confirmed dormant), **2 duplicates**, **0 false positives**, **0 already fixed**.
- Duplicate preservation: BUG-006 remains distinct from canonical implementation finding BUG-021; BUG-099 remains distinct from canonical end-to-end finding BUG-059.

## Dependency graph

| Task | depends_on | Scope |
|---|---|---|
| T-BUG-C1 | `[]` | BUG-001 to BUG-003, BUG-018 to BUG-020 |
| T-BUG-C2 | `[]` | BUG-004 to BUG-017, excluding implementation work owned by C3/C4/C5 |
| T-BUG-C3 | `[]` | BUG-021, BUG-022, BUG-027, BUG-028 |
| T-BUG-C4 | `[T-BUG-C1, T-BUG-C2]` | BUG-023 to BUG-030, BUG-036, BUG-037, BUG-115 Python boundary |
| T-BUG-C5 | `[T-BUG-C2]` | BUG-011, BUG-031 to BUG-035 |
| T-BUG-C6 | `[]` | BUG-038 to BUG-061 API changes |
| T-BUG-C7 | `[T-BUG-C6]` | BUG-042, BUG-045, BUG-046, BUG-050, BUG-080 |
| T-BUG-C8 | `[]` | BUG-060, BUG-062 to BUG-064 |
| T-BUG-C9 | `[T-BUG-C6]` | BUG-065 to BUG-083 and BUG-087 |
| T-BUG-C10 | `[]` | BUG-084 to BUG-090 |
| T-BUG-C11 | `[T-BUG-C13, T-BUG-C14]` | BUG-091 to BUG-094 |
| T-BUG-C12 | `[]` | BUG-059/099 and BUG-096, BUG-100 to BUG-110, BUG-118 |
| T-BUG-C13 | `[T-BUG-C4, T-BUG-C12]` | BUG-076, BUG-078, BUG-090/091, BUG-110 to BUG-121 |
| T-BUG-C14 | `[]` | BUG-043, BUG-081, BUG-086, BUG-092/093/095, BUG-097/098, BUG-120 to BUG-130 |
| T-BUG-INTEGRATION | `[T-BUG-C1, T-BUG-C2, T-BUG-C3, T-BUG-C4, T-BUG-C5, T-BUG-C6, T-BUG-C7, T-BUG-C8, T-BUG-C9, T-BUG-C10, T-BUG-C11, T-BUG-C12, T-BUG-C13, T-BUG-C14]` | Full focused suites, full Python/JS suites, Playwright/browser verification for UI changes |

## HIGH_BUG dependencies and overlaps

- Coordinate BUG-008 with HIGH_BUG **Lookback query eventually freezes forecasts on the oldest observations** in `radon-forecast-nightly.service`; both change the nightly contract and tests.
- Coordinate BUG-011 with HIGH_BUG **A crashed Playwright browser leaves the service permanently alive but unusable** in `radon-newsfeed.service`; media versioning must not obscure service restart health.
- Coordinate BUG-023 with HIGH_BUG **Edge evaluation passes after mandatory data-fetch failures** in `scripts/evaluate.py`; fix mandatory-data gate state before final pending/exit semantics.
- Coordinate BUG-024/025 with HIGH_BUG **Malformed or incomplete API responses are reported as healthy** in `scripts/health_service/probes.py`; use one trust-classification model.
- Coordinate BUG-026 with both HIGH_BUG `scripts/ib_sync.py` findings; NAV locking must not widen multi-account or sentinel-price changes.
- Coordinate BUG-029 with HIGH_BUG **Torn WAL tail corrupts the first finding written after resume** in `scripts/lib/checkpoint.py`; implement one validated recovery format.
- Coordinate BUG-044 with HIGH_BUG **NQ-SPX skew output contains absolute NQ skew and mixes incompatible metrics** in `web/app/api/internals/route.ts`.
- Coordinate BUG-048/128 with HIGH_BUG previous-close findings in `web/app/api/previous-close/route.ts` and `web/lib/usePreviousClose.ts`; settle session identity, timeout, and retry as one contract.
- Coordinate BUG-069/095 with HIGH_BUG **Test-only flags can disable realtime authentication in production** in `web/lib/RealtimeAuthContext.tsx`; remove production authless branches through one test-harness seam.
- Coordinate BUG-076 with the HIGH_BUG `ModifyOrderModal.tsx` risk/sign/quote fixes so numeric parsing is validated before risk construction.
- Coordinate BUG-082/083 with HIGH_BUG **Heuristic combo groups become destructive live-order targets** in `WorkspaceSections.tsx`; durable grouping identity is prerequisite to safe modify targets and BAG aggregation.
- Coordinate BUG-091 with both HIGH_BUG `FuturesOrderForm.tsx` findings; portfolio-known submit blocking and conId reset precede held-quantity close risk.
- Coordinate BUG-096 with HIGH_BUG **Short-stock opens and covers are classified in the wrong direction** in `fromJournal.ts`; establish deterministic dedupe before action classification.
- Coordinate BUG-106 with both HIGH_BUG `realizedPnl.ts` findings; assignment/exercise and unmatched-expiry semantics precede as-of clamping.
- Coordinate BUG-107 with both HIGH_BUG `journalImport.ts` findings; execution identity/correction reconciliation precedes sell cash accounting.
- Coordinate BUG-110 with both HIGH_BUG `positionTrade.ts` findings; per-leg expiry and over-close classification precede partial-basis scaling.
- Coordinate BUG-113 with all three HIGH_BUG `computeOrderRisk.ts` findings; collateral reuse, expiry-aware netting, and put interior losses share the risk engine.
- BUG-114 is the key-level counterpart of HIGH_BUG **What-if margin remains stale after order-price changes** in `useWhatIfMargin.ts`; implement as one cache-identity patch including portfolio revision.
- Coordinate BUG-118 with HIGH_BUG **Historical realized P&L uses the latest portfolio basis** in `stockBasisDb.ts`; account-scoped historical basis requires one schema/query decision.

## Risks and blockers

- No inventory blocker. All 107 referenced primary files exist at the scanned commit.
- Highest merge-risk surfaces are order-risk (`computeOrderRisk`, `positionTrade`, `ModifyOrderModal`), journal accounting, previous-close freshness, and share-card storage because HIGH_BUG work touches the same contracts.
- Calendar patches require separate authoritative NYSE/options and CME calendars; do not reuse the equity calendar for Globex.
- Share-card split-deployment fixes require an explicit storage/transport choice before implementation; local filesystem changes alone cannot satisfy demo topology.
- BUG-064 requires a product choice: complete the order-workflow authoring contract or remove/disable the unusable palette node.
